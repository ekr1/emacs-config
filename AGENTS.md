- Make sure no file contains a personally identifyable user name (for
  example /home/name.surname/...) or identifyable customer or project
  names.

- Everything must work on Linux and MacOS; only also make it work on
  Windows if easily possible.