- No file must contain a personally identifyable user names (for
  example /home/name.surname/...) or explicit customer or project
  names.

- Everything must work on Linux and MacOS; only also make it work on
  Windows if easily possible.

- Verify changes to the init files by byte-compiling.