#+TITLE: straight.el remote diffs

- Repos scanned :: 111
- Fetch         :: yes
- Detached HEADs :: 0
- With changes   :: 65

* acp.el (main)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/acp.el
:BRANCH: main
:END:
** Remote-only commits
- 3ddfa90 Adding a settings group #22
- c8ee1d7 Bumping version
- c84b6d7 Merge pull request #23 from Scott-Guest/forever-loading
- b2a1a07 Fail pending requests when agent process exits
- e31f726 Extract request failure callback dispatch
- 1ecec37 Extract internal error helper
- 661af51 Bumping version https://github.com/xenodium/agent-shell/pull/553
- 4f3b094 Merge pull request #20 from martenlienen/tramp
- 6f897ca Allow ACP connections via TRAMP
- 03099fa Merge pull request #18 from systemfreund/add-session-set-config-option
- a42e81a Adds acp-make-session-set-config-option-request
- c32fbf8 Bumping version
- 863f2d6 Removing rogue request id increment (not needed for incoming requests) #17
- 7b2b380 Adds acp-make-session-fork-request
- 9737a96 Bumping version
- a651378 Fixes ACP parsing on Windows https://github.com/xenodium/agent-shell/issues/401
- b51ad43 Don't query on exit for acp client processes
- f7e20ce Cap debugger values so we can get some data from debug-on-error https://github.com/xenodium/agent-shell/issues/360
- 49de56f Merge branch 'main' of github.com:xenodium/acp.el
- 4a8dce5 Merge pull request #14 from timvisher-dd/cap-log-buffer-size-and-disable-traffic-and-log-buffer-undo
- 6e7e744 Fixing fake traffic loading regressions
- 5676046 Disable Traffic Buffer Undo
- 9800f88 Disable Log Buffer Undo
- 65538c0 Cap ACP log buffer size

* agent-shell (main)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/agent-shell
:BRANCH: main
:END:
** Remote-only commits
- ee4f259 Merge pull request #707 from eliferrous/main
- 693d15d Add Hermes to default agent configs
- 41a2fa7 Removing compat dependency for with-work-buffer #643
- 77a8a44 Bind RET in diff buffer to jump to file at location #347
- ff72ee9 Jump to file location where change would apply #347
- b06f6f7 Handle multi-diff requests #580
- 14c1690 Remember viewport peek location when going back and forth to compose buffer
- 5675fc6 Fixing CI build
- b33815e Don't use icon if display is not supported #495
- 90d492a agent-shell-other-buffer improvements
- 4f05fcf Adds support for system-sleep #644
- ef4605e Linking to https://github.com/alberti42/agent-shell-math-renderer
- 0904a24 Use with-work-buffer for temp buffers #643 Thanks @shipmints
- c5b1fd2 agent-shell-other-buffer (to shell) now moves to current interaction
- c11d121 Fixes C-c C-o opening viewport at second to last interaction #433
- 64cd83b Fixing stale comments
- c195435 Summarize lines added/deleted in section titles #163
- 0280fdd Adds M-x agent-shell-copy-source-block-at-point #239
- bd44a54 Fixes permission dialog failing when reading non-string filepaths #680
- 369c6a2 Adds M-x agent-shell-copy-link-url-at-point #669
- 891e8dc Expose markdown via M-x agent-shell-copy-as-markdown #641
- 8e85d5f Linking to agent-shell-links.el #662
- 8c7eea8 Merge branch 'main' of github.com:xenodium/agent-shell
- 882065b Don't render images inside section title to avoid odd shaping artifact
- 720cecd Merge pull request #698 from jcubic/fix-mouse-copy
- 68e7be4 Merge pull request #696 from liaowang11/strip-source-buffer-props
- a2335c4 Preserve source faces in region previews
- a7107f9 add test
- bcf6764 fix mouse-copy inside formatted text
- 8d97dd4 Merge pull request #697 from liaowang11/fix-send-dwim-double-insert
- 56f69ba Fix agent-shell-send-dwim double-inserting context with prefix arg
- 14c3722 Strip source-buffer properties from grabbed region context
- 8e6ad47 Merge pull request #695 from jcubic/resize-header
- e1a1d11 fix unexpected resize the window header. fixes #557
- c91b20c Migrates Cursor to official ACP/CLI #386
- cd2e6a6 Codex migration to agentclientprotocol/codex-acp server
- 550d88b Fixes "Always allow" buttons losing border
- 078c100 Merge branch 'main' of github.com:xenodium/agent-shell
- b6e2da3 Link to BE episode 14
- b835a03 Merge pull request #647 from bcc32/get-region-context-avoid-extra-line
- c88b305 Merge pull request #664 from liaowang11/fix-transcript-heading-separator
- dd3bc5a Merge pull request #665 from liaowang11/fix-completion-bounds
- def6731 Merge pull request #666 from liaowang11/preserve-viewport-edit-draft
- 3308cd6 Merge pull request #672 from arthurgleckler/narrow
- 5772771 Merge branch 'main' of github.com:xenodium/agent-shell
- 7ee6ff9 Make markdown helpers public #674
- 3f8fa30 Merge pull request #693 from nhojb/cline-fixes
- 3f08c72 Merge pull request #687 from alberti42/transcript-session-id
- 71e411c Add autoloads to .gitignore
- 05dc34f Match category options by :id
- cdd78fe Add default model and session-mode defcustoms for cline
- 1ef0e4e Guard agent-shell--refresh-session-title by session-list capabililty
- bf12314 Also record model ID in transcript header
- a7b618e Record session ID in transcript header
- a48a5e6 Handle session_info_update notifications #690
- a894020 Expose :inline-code-ranges context to custom markdown renderers #674
- 5f4dff5 Fixes table column border alignment
- 03a9dd4 Merge branch 'main' of github.com:xenodium/agent-shell
- ba0918f Honor agent-shell-markdown-frozen when styling source blocks #674
- 7833f37 Merge pull request #686 from jcubic/patch-1
- 7a2c2fc Fix command to pull Qwen2.5 model in README
- 773f41f Bumping version
- a66e828 Fixing warning
- 57209d5 Namespace out-of-turn agent_message_chunk to get a new fragment #678
- 9e0d084 Handle out of turn agent_message_chunk with fresh prompt user input #678
- 96f33d6 Render out of turn agent_message_chunk notifications #678
- b0de9d8 Fixing when-let if-let Emacs 31 deprecation warnings
- 7b8b4b2 Extend agent-shell-preferred-agent-config to preselect
- 1a2e0d8 Enable custom markdown renderers to claim source blocks #674
- 4010093 Adds partial needs for custom markdown renderers #674
- a2915b8 Tweak out of turn messaging
- 0c54d5b Use agent-shell-ui--body-invisible-p for render-skip guards #667
- 8adc58e Merge pull request #667 from arthurgleckler/table-fix
- 5209859 Fix MD rendering for non-streamed bodies
- 7dfc627 Bumping shell-maker version requirement
- 4f70550 Bumping version
- 92386ef Render audio and binary resource content blocks #676
- 96343cf Render resource_link/resource content blocks; open binary links externally #676
- 064c040 Add support for inline rendering of remote images #676
- df1b2ea Add support for rendering base64 image content #676
- 66c9c7e Merge pull request #676 from melito/render-image-content-blocks
- b8ce2fb Render agent image content blocks inline
- c59a0a9 Merge pull request #675 from lgmoneda/codex/revert-codex-acp-repo
- 58f4d66 Revert "Update codex-acp repository links"
- 1d4265f Merge pull request #673 from lgmoneda/codex/update-codex-acp-repo
- 4c8db2c s/is-temp-shell/has-cleanup-hook #661
- e1533a6 Merge pull request #661 from arthurgleckler/main
- e47ac55 Update codex-acp repository links
- 46a3fc2 Add `agent-shell-narrow-to-block'.
- b5c7e84 Don't delete temp dir on agent-shell-restart.
- b6ecb1a Preserve viewport edit draft when queued request executes
- 2a9df53 Don't trigger completion on path separators in file paths
- 3743eb9 Fix transcript section heading glued to interrupted agent message
- 131b9be Merge branch 'main' of github.com:xenodium/agent-shell
- 1d8ed35 Catch errors while dispatching events #663 (thanks @candera)
- a6ed39e Maintain line-prefix and wrap-prefix for images
- bb1bcfa Merge pull request #631 from tychoish/patch/add-omb-support
- ff94a15 fix: respond to pr comments
- 66afc57 Merge pull request #659 from phairoh/ci/native-ert-junit
- 94ad84d Post test results as a check run instead of a job summary
- d858bd3 Use built-in ERT JUnit reporting instead of ert-junit
- c03acf2 Fixing merge and tests
- 6b3799c Bumping version
- f3fd61b Merge branch 'main' of github.com:xenodium/agent-shell
- 185ae49 Adding missing require
- e70750f Merge pull request #658 from liaowang11/wip/viewport-page-navigation
- 2c28fef Merge pull request #656 from liaowang11/wip/previous-item-fragment-heading
- 20bc0a5 Fix viewport previous-page navigation and stale position header
- 9988873 Fix backward block navigation skipping the current fragment
- 3ffc362 Merge pull request #653 from timfel/patch-2
- 828718b Add a reference to agent-shell-desktop.el
- e382e41 Merge pull request #652 from vellvisher/patch-1
- cb64c13 Fix file paths in README for model registration
- 40ebb09 Drop trimming and rendering prompt markdown for the time being (part 2)
- 7fdd9aa Merge pull request #651 from phairoh/ci/test-reporter-fork-fix
- 06f7657 Adding pen line to agent-shell-artist.el
- 6a5ab9c Drop rendering prompt markdown for the time being
- abaeebd Adds agent-shell-artist.el (entry point M-x agent-shell-artist-insert-text-drawing)
- 39ce8b9 ci: post test results on fork PRs via workflow_run
- e9930a0 Remove busy guards for cases when queuing prompts is possible
- 6a2184f Linking to v0.55 blog post
- a07f1fd Ensure animation finalizes in "other" buffer (shell vs viewport)
- 7bd3913 Fix hermes location in agent list
- fb6278f Merge branch 'main' of github.com:xenodium/agent-shell
- ecacbaa ci: allow test-reporter to fail without failing the workflow
- 9bb92cb Merge pull request #648 from liaowang11/fix/intraword-underscore-italics
- 3acfac0 Don't italicize intraword underscores
- 72d3dc8 Adjust line counting of region end when at the beginning of a line
- 0b32cad Fixing binary check test
- 6162e23 Skip test when display-graphic-p is not available
- 29c1ece Fixing session loading verbosity tests
- f28c2fd Fixing session loading verbosity tests
- 62e987e Fixing label tests
- a139cd3 Fixing markdown tests
- dffbcfc Alerting user of degraded restore verbosity #605
- c276aa5 ci: enable manual workflow dispatch
- e9cfb4f Merge pull request #633 from phairoh/add-ci-workflow
- ac35224 Inlining agent-shell-set-session-config-option completion annotation/metadata
- cba56a9 Merge pull request #613 from catern/main
- d354db5 Bumping version
- bb84f47 Fixes viewport navigation for interactions generated by loaded sessions
- 8850efd Merge branch 'main' of github.com:xenodium/agent-shell
- 183b307 Render 'first-last verbosity as shell interactions #605
- ce50ab1 Merge pull request #639 from Gleek/fix/restart-preserve-window
- 41e9cec Preserve window position when restarting
- 6af6d6c Land point at title bol when collapsing from body #608
- 1406698 Merge branch 'main' of github.com:xenodium/agent-shell
- 1720573 Consolidating fragment-toggling commands #608
- eff0646 Merge pull request #629 from phairoh/fix-gitignore-use-git-exclude
- dfaeee4 Merge pull request #608 from codeluggage/feat/fragment-folding
- 992876b Improve buffer selection across commands and add agent-shell-switch-buffer
- d72f4b7 Lazy render markdown in collapsed nodes (based on @AdamNiederer's #585)
- e9111a7 Merge remote-tracking branch 'upstream/main' into feat/fragment-folding
- 13ce65c Leave agent-shell-hermes-default-session-mode-id unset by default #627
- 2667434 Merge pull request #627 from yitang/hermes-session-mode-id-clean
- 718d338 Extend non-standard tools rendering to permission dialog #632 #636
- 9c9de19 Rename ACP param to use acp- #632
- 8999d9c Merge branch 'main' of github.com:xenodium/agent-shell
- 0b2af31 Merge pull request #632 from martenlienen/render-tool-call-params
- 8a9cb78 Improve surfacing/rendering of issues
- e9dbe52 Merge pull request #634 from TamsynUlthara/update-hermes-ascii-art
- daf4a14 Update art generated by `agent-shell-hermes--ascii-art`
- 2f7e8a9 Add GitHub Actions workflow for ERT tests
- 97b2823 Render tool call parameters for non-standard tools like MCP calls
- e58909d Rename -session-restore-strategy to …-restore-verbosity and retire 'new-deferred #605
- c3e3abf feat: add implementation/support for oh-my-pi (omp)
- 54baa95 Write .agent-shell/ to .git/info/exclude instead of .gitignore
- 080522b Change default status renderer to a more compact one (w/ icon)
- 30df7fe r binding (reply) and blockquotes now works in shell too
- c908de7 hermes: add default-session-mode-id to match other agents
- bfc3e72 Don't trim auto-included regions #587
- a64839e Make tab/shift-tab navigate tables cells also
- 8301ff3 Auto expand truncated regions over the wire #587
- b2badff Merge pull request #625 from phairoh/fix-source-block-face-inheritance
- bfd93f6 Fix source-block face background not rendering when theme loads after package
- a0b24c6 Adds agent-shell-list-edit-mode for viewport editing buffers
- 03f8be4 agent-shell-markdown-source-block now inherits bg from org-block #597
- e81dad4 Enabling inline markdown renderer and source highlighting by default #597
- 8757b5c Tidying up text menu for viewport and include help binding #474
- aa6a744 Merge branch 'main' of github.com:xenodium/agent-shell
- 76459c2 Enable viewport flow to queue prompt requests #474
- 7531b41 Merge pull request #621 from bcc32/reindent
- e511129 Merge pull request #623 from nohzafk/agent-shell-hud
- d957dfb Adds agent-shell-hud to related packages
- 0b1567c Consider content and locations when rendering permission dialogs
- a1435ff Adds agent-shell-tramp to related packages
- 2b09e4c Fixes fake loading
- 2387eec Document pasting images requirements
- 86a4318 Fixes table alignment multi-line cells with inline code
- b3066ae Fixes copying source blocks from viewport buffer (inline md renderer)
- 0324fc0 Readding set-buffer-multibyte #515
- 314d824 Keep svg handling and base64-encode-string's no-line-break #515
- 87e0ab8 Merge pull request #515 from martenlienen/binary-files
- 877c98a Update permission dialogs on tool call updates
- cbc8a65 Reindent Elisp files
- f03c280 Encode binary files as binary resources
- 91c28fe Make detection of binary files more robust
- 5e22461 Merge pull request #618 from deftsp/shihpin
- bb0983a fix: header not show session mode name
- 45c75fd Improve completion for set-session-config-option
- d6605ef Merge pull request #605 from nhojb/f/restore-context
- 8e4903f Rename `agent-shell-restore-context` -> `agent-shell-session-restore-strategy`
- 5b5c685 Add `agent-shell-restore-context` defcustom
- e01e577 Moving progress indicator to graphical header's second line
- 2c21fcd Removing Emacs 31 warnings (use of when-let)
- bd24cb4 Tidy up graphical header (move position and status to second line)
- 2bd245a Rename "Start new shell" -> "New shell"
- b576fcd Bumping version
- a8c04fb Merge upstream/main into feat/fragment-folding
- 6f2e6dc Merge pull request #597 from xenodium/inline-markdown
- de0a951 Adds agent-shell-markdown-render-function to pick a renderer
- 0fec197 Merge pull request #611 from TamsynUlthara/fix-hermes-acp-command
- 083c7d3 Fix `agent-shell-hermes-acp-command`
- 8425b6c tests: cover DWIM toggle through `--surgical-replace-body'
- 7bf5080 ui: re-align text-property plists to inline-markdown convention
- 270ad55 add new test coverage for UI, fix code style
- 86e6f5b comments about dedup, fix some formatting, rip out
- 831136d ui: align fragment fold commands with project conventions
- 8fd978c ui: add fragment fold DWIM + global cycle commands
- 38b62b5 Fixing │ table alignment cases
- 047f34c Fixing some CONTRIBUTING.org items
- 04860d5 Removing surgical from function names
- 51d65fe Make code block padded background non-trimmeable
- 0ce2970 Fixing tests
- 3e6b816 Add markdown avoiding diff test #597
- ed6c15a Update markdown rendering test #597
- cc9fea4 Fix rendering diff #597
- 33b663c Echo "Press RET to copy" on snippet label point entering
- 8289506 Make code block label actual buffer text (not display only) #597
- 47f2760 Fix body-invisible-p for whitespace-only bodies #597
- 1d10802 Getting rid of snippet background color and all the padding
- 03fe433 Fixing incomplete table header recognition while streaming
- 067cbe8 Fixing code block test supporting labels + button
- e154851 Add copy buttons to snippets
- 34b98ca Render code block language
- 4b97625 Improve code block handling
- ef4d6f3 Strip rendered properties when pasting copied text
- 9948e25 Fixing blockquote test
- 1da98b9 Render blockquotes
- 9307d20 Fixes headings partially rendered when partially streamed
- 0017376 Improving streaming performance
- 1a45150 agent-shell-markdown.el perf improvements
- ea00ede Add padding and background color to source blocks
- fc0f619 Favor map.el
- 8c705cc Removing cl-position-if usage
- 76213b7 Bundle experimental markdown renderer
- 359507f Fixing inline markup rendering regression
- 88eb93a Fixes updating fragment labels
- 8e53972 Getting rid of cache to avoid regeneration. Always append.
- e8184d8 Initial markdown-text integration
- dcc3e87 Add basic svg icons for tool status
- ebff3c6 Fix completion to ignore config icons
- e63f1ca Fix loading downloaded icons without extensions (ie. Droid)
- aa54cbd Merge pull request #609 from martenlienen/no-tool-scroll
- 99dca4b Prevent accidental auto-scrolling in viewport buffers on tool calls
- 2e28832 Bump acp.el version dependency
- 89bd6e1 Add settings menu to graphical header
- b9a1d65 Consolidate #498 into a constructor with defaults
- 2b24078 Merge pull request #498 from CsBigDataHub/main
- 516392b C-u agent-shell now exposes temp and Downloads shells
- cded168 Merge pull request #483 from illidan127/main
- aa23a6c Merge pull request #601 from martenlienen/thought-level
- d14e97d Merge pull request #602 from dvictori/main
- cbc171e Update README with Opencode and Ollama setup instructions
- d8dde2b Expose thought level in header, mode line and keymaps
- 58458b1 Binding C-c C-s to agent-shell-set-session-config-option #436
- ed6a1af Merge pull request #600 from Gleek/fix/handled-permission-events
- 321f438 Add CodeBuddy icon and link official install docs.
- f770935 Handle missing CodeBuddy icon and non-string image paths
- 1ed96ad Add CodeBuddy agent support
- 3ccb4b8 Do not emit permission-request for handled permissions
- 530620e Merge branch 'main' of github.com:xenodium/agent-shell
- 222f588 Render blockquotes when replying in viewport and use yank-handler
- 152fd84 Merge pull request #595 from Gleek/feat/queue-request-completion
- 00562f1 Fix / completion regression in viewport buffers
- 3f3f91e Merge pull request #590 from nhojb/fix-context-limit-header-color
- c71bd6b Merge pull request #489 from timfel/timfel/session-strategy-snapshot
- 7681296 Fix context-limit header color (emacs-31)
- 7485abb Emit an event when config options changed #553 #436
- 2cc8263 Bumping version requirements #553 #436
- 04e1faf Follow up to #553 #436
- dcf789d Merge pull request #553 from greggroth/greggroth/session-config
- 5a35322 Merge branch 'main' of github.com:xenodium/agent-shell
- 25b86dd Linking to Fedora Wiki
- 5dbb371 Merge pull request #589 from martenlienen/fix-trim
- 47c2656 Only trim response region when region is active
- 5c373ee Merge pull request #513 from martenlienen/quote-reply
- dab347b Trim whitespace from reply region to avoid empty quote lines
- d0a29fd Add a command to quote the complete response
- 33cfb8e Merge pull request #588 from nhojb/fix-header-foreground-color
- 1fe0aa6 Fix header foreground color (emacs 31)
- 1d88a0e Merge pull request #551 from Silex/codex/avoid-gitignore-for-external-data-dir
- 1f9dd3e Merge pull request #565 from Gleek/main
- 67babec Merge pull request #583 from yitang/main
- 71e5561 remove trailing newline and whitespace
- 9b38cc9 Merge branch 'xenodium:main' into main
- 9ac2b15 add hermes agent support
- 4662314 Merge branch 'main' of github.com:xenodium/agent-shell
- 58addb1 agent-shell-toggle should not change window configuration https://github.com/xenodium/agent-shell-js/issues/25
- a57b87a Merge pull request #582 from tychoish/fix/initialization
- 9c704f2 fix: declaration style
- 8e5a13e fix: forward declare
- 5ca5dcb fix: add declare-function and require for native-compiler warnings
- 91e7b00 Fixes regression and all failing tests
- 6a383f0 Bumping version #548
- 6d9d9a4 Avoid including markup when copying text #548
- 6babf80 Preserve point and window-start across streaming rerenders
- bb4468b Enable @ and / completion when reading queued prompts
- fbb6fd8 Ensure minimal state set while setting up to start #577
- 7e74d62 Merge branch 'main' of github.com:xenodium/agent-shell
- 655b566 Adding shx FAQ entry #543
- 88b50a3 Merge pull request #552 from smagnuso/pr/pause_scroll
- 712209c Linking to articles
- a34838f Derive 'ready directly from shell-maker-busy
- 4834c7e Merge branch 'main' of github.com:xenodium/agent-shell
- 7cf56d5 Adds agent-shell-status (returns 'busy 'blocked or 'ready)
- 30fe4bd Merge pull request #571 from Scott-Guest/kill-buffer
- 6b9c44d Apply existing pattern to #572
- dfd74e9 Merge pull request #572 from repelliuss/main
- aee3a4c Merge pull request #574 from lgmoneda/patch-1
- 2de642a Add agent-shell-pet link to README
- 571f6fd add clipboard image support for Windows
- d31b9cb Clean up on mode change to prevent process leak
- cc5033e agent-shell--clean-up: no-op rather than error if mode changed
- 85398ad Merge pull request #446 from kohnish/droid-fix
- cd8a9ed Extract config option helpers to agent-shell-config.el
- e71b033 Add ACP session config options support
- 411c904 Merge pull request #555 from vermiculus/sa/agent-shell-list-support
- 6f9400d Merge branch 'main' of github.com:xenodium/agent-shell
- 6f5d637 Fixing break (based on #563 by @Ossifragus)
- ce44237 feat: track `:last-activity-time` as a new piece of state
- daa47e3 fix: clear `:permission-request-id` after responding to permission
- c944152 refactor: use shared model/mode helpers in `make-header-model`
- 6064888 style: reindent agent-shell.el
- 152277f Merge pull request #545 from timfel/keep-buffer-name
- 3e967d3 Merge pull request #550 from Marx-A00/add-agent-recall-to-related
- 67ba4a2 Merge branch 'main' of github.com:xenodium/agent-shell
- dbc1702 Applying @smagnuso's :title follow-up
- 1498394 Merge pull request #561 from jeff-phil/feat-pi-logo-upd
- 31e201c Update Pi coding agent logo to look more official
- 6e03b78 Follow-up to merged #559 primarily adhering to CONTRIBUTING.org
- d48c239 Merge pull request #559 from smagnuso/pr/topic
- f3e9dee Expose topic in agent-shell
- 85bd9c4 Flatten emitting events #554
- 178dbb2 Merge pull request #554 from Gleek/main
- 8cbe8bb Fix error when cancelling session selection prompt
- 9368925 Merge pull request #530 from rudolf-adamkovic/simple-busy-indicator
- e3827bd Add simple blinking circle as busy indicator
- 31bc664 Use helper function from shell-maker rather than eob
- 8fb444a Avoid gitignore update for external data dirs
- 1008b16 Add agent-recall to related projects
- 47194ca Droid: use native acp client and support default model and mode
- be9b069 Keep buffer name on agent-shell-reload and -restart
- 0882a02 Merge pull request #539 from eddof13/docs/add-ob-agent-shell
- 0a4cfa1 Add ob-agent-shell to related projects
- 056f7b7 Merge remote-tracking branch 'upstream/main'
- 758ad20 Merge pull request #523 from lllShamanlll/add-agent-shell-org-transcript
- 438e766 Merge pull request #529 from smagnuso/pr/sessions_sort
- 643aae1 Merge branch 'main' of github.com:xenodium/agent-shell
- ed008a5 Improve yt links
- 6eb9f8e Merge pull request #536 from eddof13/ob-agent-shell-integration
- 0290d04 Remove inline comments from docstring example
- b3e1597 Drop agent-shell-query and agent-shell--extract-last-response; fix indent
- 9d9f22d Adds agent-shell-goto-last-interaction and agent-shell-interaction-at-point #536
- 7a62017 Enable custom timeouts per idle events #509 #508
- b9c40e8 Add agent-shell-shell-buffer, agent-shell-query, and agent-shell--extract-last-response
- 21a8c8b Merge pull request #528 from nicolaisingh/kimi-code
- d46dcf3 Merge pull request #532 from Gleek/main
- 877ba53 Introducing idle event. Use agent-shell-subscribe-to as usual #509 #508
- 027e4cd Merge pull request #509 from arthurgleckler/main
- 46f147f Updating PULL_REQUEST_TEMPLATE
- fc9d5ce Fix removing queued messages
- d104a72 Sort the session list based on recency
- 424ecd3 feat: Add support for Kimi Code CLI using ACP
- 023ad9c Linking to other videos
- a2b3e5d Add agent-shell-org-transcript to Related projects
- eb5a379 Fixing README Claude dependencies/instructions
- fd6029f Add `agent-shell-notify`.
- 7f106a3 Use explicit svg checks #443
- 73718e2 Mention skills in README
- ef9cfa6 Merge pull request #492 from Gleek/main
- 11e25d1 Merge pull request #503 from cxa/main
- 5f8fd5c Add agent-shell-macext to related projects
- de268f2 mcp tests
- d264956 fix MCP in ACP transport
- 1634238 Update claude-agent-acp repository
- a950340 Snapshot startup session strategy in new shells
- 1e5d175 Fall back to "unknown" when type is not known #477
- c8a691c Merge pull request #477 from Gleek/main
- c46cf37 Handle non-text content in user_message_chunk during session load
- 429d191 Merge branch 'main' of github.com:xenodium/agent-shell
- 52a1006 Adding agent-shell-bookmark to related packages section
- 63f6477 Merge pull request #473 from Gleek/main
- b843a80 Display key bindings in menu tooltips #448
- ce0288e Caching project files completions for improved performance
- 5470fd9 Shortening some agent names
- 9b509be Adding README entry for slash commands
- 9515f83 Merge branch 'main' of github.com:xenodium/agent-shell
- 6572431 Fixes refocus after diff regression #466
- b0315e9 Merge pull request #469 from martenlienen/fix-wl-paste
- 2cc5fe5 Do not create a file if no image in Wayland clipboard
- 68ce6b5 Replacing or + when-let* with if-let* #463
- fff9ddb Merge pull request #463 from ftlio/fix/header-font-size-zero
- 7061e1f Fix header text invisible when font-get :size returns 0
- 0a9f59b Merge branch 'main' of github.com:xenodium/agent-shell
- 983f2d3 Ensure button border does not leak into subsequently inserted text
- c7e5f63 Merge pull request #340 from chemtov/oauth-anth-support-doc
- 4d7db3a Merge pull request #450 from zackattackz/restart-keep-default-dir
- b7b27b7 Merge pull request #461 from martenlienen/wl-paste-image-handler
- e06e65f Fixing checkdoc warning
- a60a3b9 Text header/modeline improvements #448
- 2afe383 Add wl-paste as a Wayland image handler
- 74a59ec Merge pull request #460 from 0x6362/bug/455-ignore-unknown-messages
- 5fd9794 Enable expanding region/context text for editing #459
- e607416 Make agent-shell--format-plan more forgiving #438
- 444ca12 Merge pull request #438 from timfel/main
- 794cb1c Fixes #455: unhandled method returns an error, unblocking client
- 9d667de Merge pull request #457 from martenlienen/xdg-cache-dir
- c134d45 Merge branch 'main' of github.com:xenodium/agent-shell
- 158304f Fixing test
- 1709386 Prefer cache directory over tmp for caching
- 844af9e Fix restart using wrong default-directory
- 82a7fff Merge pull request #445 from bcc32/header-project-name
- ef2f187 Use project-name instead of default-directory in header
- 39188e6 Merge branch 'main' of github.com:xenodium/agent-shell
- 4ce2ce5 Make id more evident for available models and modes #452
- 4491cca Merge pull request #453 from martenlienen/byte-compile-viewport
- 27f58fe Ensure that viewport compiles
- 209c413 Merge branch 'main' of github.com:xenodium/agent-shell
- 0a75dba Adds agent-shell-new-downloads-shell and agent-shell-new-temp-shell
- 5b1ed12 Merge pull request #442 from Embedded-Focus/dev-add-agent-circus-to-readme
- 865fe56 Add related project `agent-circus` to README.org
- 270c334 Adding https://github.com/zackattackz/agent-shell-notifications to README
- 0b24754 Show activity indicator while receiving push and ignore out of bound notification
- de0db12 Reject session/push requests when busy
- 19ee469 Fix for structured input from toolCall.rawInput.plan
- 2d96e98 Adding more to CONTRIBUTING.org
- e5e383a Renaming experimental session/pushPrompt to session/push
- 62f12d0 Disable image pasting when running in tui #435
- 2e91a81 Adding experimental incoming session/pushPrompt
- 746ee17 Adds agent-shell-mock-agent.el (needs mock-acp binary installed)
- a2597ad Try to include file name in permission title if missing #415
- 697b975 Fixing auth-source-pass-get usage in README #434
- cc2d541 Update Claude Code icon
- 68b8c39 Bumping version
- 22e23d7 Bumping version
- 287c3b4 Improve layout for available slash commads, modes, and models
- aea9eea Renaming "Thought process" to "Thinking"
- 520cad3 Hide Loading... spinner on any error or cleanup #431
- d7bf9ef Fixes clipboard image pasting into shell when viewport is preferred
- 693c70b Adds session forking support #334
- 8e8c1e9 Merge pull request #429 from nhojb/viewport-attachment-fix
- 92f6ec5 Ensure attachments are discarded when cancelling viewport composer
- d4ded8a Ensure attachments are inserted into the viewport composer when active
- 3dc0093 Ensure console attachments are cleared when invoking `agent-shell-prompt-compose`
- c974b58 Bumping version
- 8374c80 Making agent-shell-session-strategy default to 'prompt
- 699fd17 Bumping version
- 30e7237 Ensure section's vertical whitespace is read-only #426
- 4fbaef9 Merge pull request #308 from ElleNajt/fix/heartbeat-nil-guard
- 74eabc3 Renaming claude-code-acp references to claude-agent-acp
- e61208a Adds agent-shell-reload #421
- 89aacce Extend agent-shell-restart to consider strategy and viewport #421
- 7766b11 Merge pull request #421 from Makesesama/Makesesama/add-agent-shell-clear
- a32b363 Rename agent-shell-clear to agent-shell-restart
- 4594c16 Merge pull request #425 from jinnovation/readme-claude-code
- c96e148 README: Update all references to Claude Code
- f9f4387 Merge pull request #424 from emil-e/add-confirm-interrupt-option
- 81512da Add agent-shell-confirm-interrupt option
- 0f4684f Add agent-shell-clear
- 31ac12b Merge pull request #420 from jinnovation/rename-claude-code
- a763f99 replace all references to "Claude Code Agent" with "Claude Agent"
- 20c5bb2 Merge branch 'main' of github.com:xenodium/agent-shell
- 5bfd2c1 Ensure point is moved to permission buttons even if buffer is in background
- 1a613d5 Merge pull request #418 from emil-e/defer-mode-hook
- 95a0554 Add defvar for agent-shell-mode-hook + test
- e9e7af3 Linking to https://github.com/xenodium/agent-shell-knockknock
- 3288737 Defer running agent-shell-mode-hook(s) until state is available #414
- 540d5a8 Merge pull request #413 from emil-e/fix/diff-buffer-lifecycle
- 051c42d Prefer map-nested-elt and in general map.el functions
- 483e007 Track and manage diff buffers for each permission request
- 0eaf46a Make agent-shell-show-context-usage-indicator default to detailed #397
- 07ef6d6 Merge pull request #397 from emil-e/context-indicator-detailed
- fb448fb Fixing docstring warning
- 0644667 Adding "continue" auto-reply and extending help menu
- 2081b9c Updating CONTRIBUTING.org
- 264dc76 Merge pull request #408 from timfel/patch-1
- 2a1f7d2 Use diff-command for diffing
- ff93549 Merge pull request #405 from robjgray/main
- 2b11a8a Test style adjustment (Openai)
- b32f0fa Added Codex defaults for session mode and model
- 117d287 Merge branch 'main' of github.com:xenodium/agent-shell
- c588328 Bumping version
- 5eb796b Fixing checkdoc warnings
- a298d4e Adds support for resuming an existing session by id #332
- fbb8225 Merge pull request #398 from emil-e/expose-outgoing-request-decorator
- a8730a1 Make viewport layout newline cursor-intangible (not read-only) #400
- 92f99ad Enable deleting images without selection #399
- 76d8189 Bumping version
- 6e4bdc4 Fixes viewport reply shortcuts for (setq agent-shell-header-style nil) #400
- 617b126 Improving viewport compose help menu
- 8f02d59 Improving viewport view-only help menu
- b3e556c Bumping version
- 7e98937 Fixes C-u C-u agent-shell
- a7908cb Fixing warning
- 3982575 Adding news link
- 71dacee Bumping version
- 7c8e5e4 Don't triggler completion when there are no commands available
- fd898b9 Adds "a" binding for agent-shell-viewport-reply-again
- 49fc2e1 Expose outgoing-request-decorator as a defcustom
- 364d989 Add detailed context usage indicator mode
- 4561f5b Non agent-shell-permission-responder-function return avoids rendering permission #395
- 6be078e Merge branch 'main' of github.com:xenodium/agent-shell
- 195cf50 Strip text properties from viewport copied text also #383 #254
- 700cbb2 Merge pull request #383 from liaowang11/wip/viewport-context-on-first-open
- 5518d1a Merge branch 'main' into wip/viewport-context-on-first-open
- 586528b Merge branch 'main' of github.com:xenodium/agent-shell
- c1518cc Enable programmatically respond to permission requests #51
- 9ef717b Merge pull request #388 from fritzgrabo/apply-dir-local-variables
- b164195 Add a comment to hack-dir-local-variables-non-file-buffer usage
- 18872db Merge pull request #382 from emil-e/subscription-events
- 23598ec Merge branch 'main' of github.com:xenodium/agent-shell
- 6761394 Bumping version and requirements
- 58fde29 Make agent-shell-yank-dwim work with delete-selection-mode
- 884edeb Declare modes for interactive commands
- c34531d Adds next/previous/search-history to viewport/compose buffer
- 1d1e1f8 Adds digit bindings to agent-shell-viewport-reply-* to viewport buffer
- 12be437 Fixes duplicate prompts appearing for user_message_chunk while sending prompt
- 259bf36 Apply dir-local variables in agent-shell buffer
- 41c4b21 Merge pull request #385 from bcc32/patch-1
- 94000a5 Avoid leading space in agent-shell-select-config prompt in terminal frames
- c72ff6e Bumping version requirements
- 4eb67eb Carry context into first viewport open
- e877ecf Adding missing viewport command counterparts
- 5ed1d33 Add permission-request and turn-complete subscription events
- 07f9130 Bumping versions
- 68b09a4 Improving viewport performance
- 08b0b54 Merge branch 'main' of github.com:xenodium/agent-shell
- 45f0107 Adding agent-shell--active-requests-p #346
- 01e4d42 Merge pull request #374 from systemfreund/mdindent
- c8fdd01 Merge pull request #378 from zackattackz/dot-subdir-custom-func
- edd842c document dot subdir one-time ignore behavior
- 540befb Make dot-subdir storage location configurable
- 773251a Merge pull request #376 from zackattackz/fix-test-file-skip-dot
- 8c3d39f Fix file skip check to use filename only, not full path
- 4fcc247 Merge pull request #370 from xar7/fix/permission-ui-stays-even-if-status-update-ooo-comes
- 5668f1f Indent markdown headers in transcript to preserve hierarchy
- ca49395 Make permission-ui reacts on status failed/completed
- 898fb88 Fixes switching mode while there's an in-flight request
- b2c2a62 Adding agent-shell-workspace to README #368
- de2e0d6 Reverting premature commit
- 958a18e Automatically git ignore .agent-shell directory when created #366
- 6273727 Merge pull request #363 from Cy6erBr4in/feature/configurable-session-columns
- 1e353cb Fixing tests
- e11a31f Add optional session ID display in header and session selection
- 5875c0c Add configurable session selection columns and optional session ID
- dc78f1e Offer auto-scrolling in viewport buffers also
- 1632f61 Consolidating tool status shortening and introducing kind shortening
- 4ad3dc9 Fixes loadSession regression #361
- 75cc736 Improving tool call update display / UX
- b6a0c78 Add reminder to keep checkboxes when filing bugs or sending PRs
- be00708 Bumping version
- 9449160 Adds image rendering support #355
- 17d8009 Fixing warning #350
- e5c8cd4 Flattening when block #353 #352
- a79bd09 Merge pull request #353 from chemtov/xclip-x11-fix-blocking
- d4a0bb2 Merge pull request #351 from zmjones/kiro-compatibility-fixes
- 03f43f4 Fix xclip handler blocking on X11 when clipboard has no image
- 0523645 feat: Add Kiro CLI ACP support
- 53045ab Revert "Replace switch-to-buffer usage with agent-shell--display-buffer #349"
- a540070 Merge branch 'main' of github.com:xenodium/agent-shell
- ed36fbe Guard fallback display via acp-logging-enabled #351
- ceb4c3c Merge pull request #350 from fritzgrabo/prepare-devcontainer-package-extraction
- d7e65a8 Bumping version #349
- 5dde2f4 Replace switch-to-buffer usage with agent-shell--display-buffer #349
- 234b61c Fully qualify ACP data (easier to understand where data comes from)
- 726da03 Extract Devcontainer support into separate package
- 2813508 Rename `-container-command-runner` to `-command-prefix`
- a78f0e9 Fixing test
- fb89325 Removing let-alist to flatten code and make usage more explicit
- e2505ae Avoid inserting out of turn notifications #345
- f6023c6 Bumping version
- cb8a6a2 Merge branch 'main' of github.com:xenodium/agent-shell
- 4b9c07a Adds initial Cline support #341
- 1a5b780 Stop heartbeat on errors
- e377c65 Add documentation about the AOuth Anthropic authentication
- f2f7d5f Merge pull request #339 from chemtov/oauth-anth-support
- d17d10d Merge pull request #335 from Azkae/fix_plan_from_request_permission
- 7bedf1f Add OAuth token support to Anthropic authentication
- 26e9a9a fix plan not displaying from session/request_permission
- 7f9e67d Merge pull request #333 from xenodium/more-reliable-tool-upgrade2
- 858c7e1 Merge branch 'more-reliable-tool-upgrade2' of github.com:xenodium/agent-shell into more-reliable-tool-upgrade2
- a032710 Trying out more reliable tool title upgrade #309
- 852f7cd Trying out more reliable tool title upgrade
- 6a33d6a First pass at consolidating bootstrapped prompts (via agent-shell-session-strategy 'prompt) #314
- a1803cd Bumping version
- 7b16d6a Add C-c C-c to reject all changes in diff #328
- 5ec181e Bring "Start a new session" to the top #313
- 492cbb1 Merge pull request #313 from catern/main
- 46fb6c1 Re-applying #316 for #328
- 0c29080 Adding agent-shell-styles.el
- e438c5f prompt-select-session preserves sorting
- f880e4a Ensure ert is loaded for agent-shell-run-all-tests
- 54d7835 Follow-up to #329
- 96a0b6d Merge pull request #329 from timvisher-dd/fix-tests-2026-02-23T00-24-35+0000
- da6e8df Merge pull request #328 from wmedrano/wmedrano
- 5ad57d9 Switch modes in agent-shell-diff
- 6674b1a Silence byte-compilation warnings
- a96222d Stub viewport--buffer in send-command tests
- b206b0b Pin session-date test to UTC for deterministic results
- db5f386 Guard binary-file test with display-images-p
- f9d367c Rename duplicate transcript-tool-call-entry test
- e995ad6 Update headerline based on set bindings
- 89fe2eb Adds agent-shell-cwd-function #323
- ce9894d Fixing warning
- 4453cfb "y" and "m" now automatically respond "yes" and "more" from viewport
- a3ff933 Drop redundant text from tool call title (compacting stuff)
- 49eb7b6 Making status reporting more compact
- 7a5c087 Renaming agent-shell-status-short-labels to agent-shell-status-labels
- 63b9c3f Merge branch 'main' of github.com:xenodium/agent-shell
- 3cf8391 Shorten status labels
- 7f6c3b5 Linking to emacs-skills
- 24bdb2f Merge pull request #326 from systemfreund/main
- cd655a5 Merge pull request #325 from Idorobots/log-agent-thoughts
- db71f8d Add Thought process contents to the conversation transcript.
- 4edef4c Transcript: fix nested markdown backtick fences in tool call output
- c61a903 Merge pull request #318 from systemfreund/main
- befa666 Convert fragment's right label starting with backticks to source blocks #220
- b3c2d89 Fixing fake traffic loading regressions
- 769ccaa Adding more details on how to file issues with helpful data
- 74c5874 Adding details on how collaborate/contribute in this project
- abe76a6 Flatten lets #319
- 2634280 Merge pull request #319 from eddof13/fix/terminal-font-get-unspecified
- 7f45af2 Merge branch 'main' of github.com:xenodium/agent-shell
- 09356e7 Reverting agent-shell--make-expandable-text #315
- 9aa3566 Fix Wrong type argument: font, unspecified in terminal Emacs
- 81db27a adjustments
- 362feb1 Include tool call parameters in transcript (opt-in)
- e616326 Merge pull request #312 from lgfang/main
- 1141686 Bumping version
- 4cbf1e8 Upgrade tool title more generically #309
- 186633e Merge branch 'main' of github.com:xenodium/agent-shell
- 6cc3bd2 Make permission multiline text expandable #315
- ace3074 Accept function value for agent-shell-anthropic-default-model-id #311
- 108a3e7 Fix heartbeat nil value crash in timer and busy indicator

* aidermacs (main)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/aidermacs
:BRANCH: main
:END:
** Remote-only commits
- 2e804a3 Add missing variable declarations to fix CI compilation errors
- 750b5f7 Improve prompt detection & notifications
- 7496878 Update package headers to 1.8
- 8785222 Release 1.8
- ae72726 Refactor model pricing to use local litellm data instead of API queries
- 9772db3 Release 1.7

* bind-key (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/bind-key
:BRANCH: master
:END:
** Remote-only commits
- 6ff8788 Update bind-key

* casual (main)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/casual
:BRANCH: main
:END:
** Remote-only commits
- f2977bc Bump version to 2.16.2
- 4fbd635 Merge pull request #468 from kickingvegas/merge-development-to-main-20260609_093757
- 4f4ead2 Merge pull request #467 from kickingvegas/466-casual-dired-errors-with-transient-setup-wrong-type-argument-characterp-nil-when-filter-returns-no-files
- 28ec81e Fix predicate casual-dired-marked-p
- fb4e280 Bump version to 2.16.2-rc.1
- 72fc243 Bump version to 2.16.1
- 40fba54 Merge pull request #459 from kickingvegas/merge-development-to-main-20260512_112327
- f14a9b3 Merge pull request #458 from kickingvegas/455-add-separate-edebug-command
- f784e8f Support direct Edebug command in casual-elisp-tmenu
- f268e8d Bump version to 2.16.1-rc.1
- 6a3fbb4 Bump version to 2.16.0
- 135610a Merge pull request #451 from kickingvegas/merge-development-to-main-20260415_181010
- cd27e4f Merge pull request #450 from kickingvegas/438-add-screenshots-to-readmeorg
- cbc3c8c Update Casual Documentation
- bd18df9 Merge pull request #449 from kickingvegas/380-support-context-sensitive-behavior-in-dired
- 8f8351e Support Dired Context Sensitive Behavior
- a5f42df Merge pull request #448 from kickingvegas/437-update-gh-actions-for-casual
- ed2508c Update to use v4 GH actions
- 459ab40 Merge pull request #447 from kickingvegas/443-fix-keep-and-flush-lines-in-editkit
- d7b6724 Remove inapt logic for flush and keep lines
- bb32f9b Merge pull request #446 from kickingvegas/444-reformat-elisp-vectors
- 0566489 Re-indent codebase with updated vector handling
- ee73317 Merge pull request #445 from kickingvegas/403-support-dired-marked-file-navigation
- 8ff3126 Merge branch 'development' into 403-support-dired-marked-file-navigation
- 73a508d Add support for marked file navigation
- 9f24b42 Bump version to 2.15.2-rc.1
- d646190 Bump version to 2.15.1
- a007936 Merge pull request #441 from kickingvegas/merge-development-to-main-20260410_163238
- 08d6c88 Merge pull request #440 from kickingvegas/439-c-g-doesnt-work-anymore
- db4679f Fix suffix `casual-lib-quit-one` due to Transient update
- d536c78 Bump version to 2.15.1-rc.1
- 81e032d Bump version to 2.15.0
- ed3fad0 Merge pull request #435 from kickingvegas/merge-development-to-main-20260406_163010
- 83cf635 Merge pull request #434 from kickingvegas/431-make-package-lint-target-in-makefile
- cfa567e Refactor Makefile module targets to support package-lint
- 48bd60b Merge pull request #433 from kickingvegas/287-add-ispell-related-commands-to-editkit
- e9364ec Add Casual Ispell
- 72ad12c Merge pull request #432 from kickingvegas/422-add-bookmark-jump-to-help-compile-menu
- 35c202d Add bookmark jump to help and compile menus
- acd12d1 Merge pull request #429 from kickingvegas/40-add-ibuffer-do-isearch-command-to-casual-ibuffer
- 85b8dcf Add support for ibuffer-do-isearch
- 50f8356 Merge pull request #428 from kickingvegas/421-add-nongnu-elpa-badge-to-readmeorg
- ad8b104 Add NonGNU ELPA Badge
- e71af6f Fix for updating version of casual.texi
- 65df9bb Bump version to 2.14.5-rc.1
- c8d6ec4 Bump version to 2.14.4
- 1db3c72 Merge pull request #426 from kickingvegas/merge-development-to-main-20260320_145303
- 09acd09 Merge pull request #425 from kickingvegas/420-fix-versioning-of-info-documentation
- 5f53bcd Fix versioning of Casual Info documentation
- efd228e Merge pull request #424 from kickingvegas/423-fix-reverse-dired-sort-by
- 45d2037 Fix bug in reverse Dired sort-by
- 9d1da60 Bump version to 2.14.4-rc.1
- 0b04ad7 Bump version to 2.14.3
- 6bf9303 Merge pull request #418 from kickingvegas/merge-development-to-main-20260302_153414
- 5834248 Merge pull request #417 from kickingvegas/414-fix-customizable-variables-that-pollute-info-and-dired-groups
- 57f73de Move Casual custom variables to casual group
- cc32b44 Merge pull request #416 from kickingvegas/412-distribute-casual-on-nongnu-elpa
- 3f77f98 Amendments per input from pkal
- 956a5e9 Request to distribute Casual packages on NonGNU ELPA
- b034fc2 Amend Monnier Changes for NonGNU ELPA Distribution
- 77b2caa Request to distribute Casual packages on NonGNU ELPA
- 1226a47 Bump version to 2.14.3-rc.1
- a68d056 Bump version to 2.14.2
- 938f54b Merge pull request #410 from kickingvegas/merge-development-to-main-20260223_153247
- 29b1b35 Merge pull request #409 from kickingvegas/404-settings-label-in-casual-org-tmenu-needs-menu-annotation
- 92964ef Fix menu annotation for Org "Settings›"
- 76cfde9 Merge branch 'development' into 404-settings-label-in-casual-org-tmenu-needs-menu-annotation
- 512763d Merge pull request #408 from kickingvegas/cc/org-mode-98-moar
- a48c3ac Support casual-org-toggle-images
- d98e8bc Fix symbol for Settings
- 43d9102 Merge pull request #406 from kickingvegas/405-support-org-mode-98
- 0aea7c7 Update to support Org mode 9.8
- 54973dc Bump version to 2.14.2-rc.1
- ec52aca Bump version to 2.14.1
- 3cb01a0 Merge pull request #400 from kickingvegas/merge-development-to-main-20260220_112634
- b3d6e6a Merge pull request #399 from kickingvegas/398-add-install-section-to-readmeorg
- c6726b3 Update README.org to have Install section
- 4de8519 Bump version to 2.14.1-rc.1
- 8c9a494 Bump version to 2.14.0
- 4db1fcc Merge pull request #396 from kickingvegas/merge-development-to-main-20260219_134434
- 70cbc05 Merge pull request #395 from kickingvegas/cc/more-tuning
- ed1fb6e Minor Tuning
- 9517bc1 Merge pull request #394 from kickingvegas/393-avoid-warnings-when-switching-windows-when-casual-org-is-raised
- 1d5d9de Avoid triggering warnings from casual-org-tmenu
- 8869166 Merge pull request #392 from kickingvegas/384-misc-info-updates
- 38c3cf0 Miscellaneous tuning for Casual Org
- c7bfcf3 Merge pull request #391 from kickingvegas/386-support-item-description-in-casual-org
- bd0505f Support Casual Org item description
- 483600a Merge pull request #390 from kickingvegas/385-support-org-at-drawer-p-in-casual-org
- 63f6ef0 Support more structure predicates
- bf26341 Merge pull request #389 from kickingvegas/388-support-transient-non-suffix-in-casual-org-menus
- eefc16d Support transient-non-suffix in menus
- 5871743 Merge pull request #387 from kickingvegas/383-add-delete-duplicate-lines-command-to-editkit
- 439e5c5 Add delete-duplicate-lines to EditKit Delete menu
- db9e647 Merge pull request #382 from kickingvegas/371-build-casual-org
- 6847308 Add Casual Org Support
- e330177 Merge pull request #381 from Stebalien/steb/tzdir
- d0c408c casual-timezone: Obey the TZDIR environment variable
- ca90763 Bump version to 2.13.3-rc.1

* closql (main)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/closql
:BRANCH: main
:END:
** Changelog diff (=CHANGELOG=)
#+BEGIN_SRC diff
diff --git a/CHANGELOG b/CHANGELOG
index fbfeb40..a149eb3 100644
--- a/CHANGELOG
+++ b/CHANGELOG
@@ -1,4 +1,8 @@
 # -*- mode: org -*-
+* v2.4.1    2026-06-01
+
+- Use ~llama~ package.
+
 * v2.4.0    2026-01-01

 - Neither ignore nor assume ~eieio-backward-compatibility~.  #11
#+END_SRC

* compat (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/compat
:BRANCH: master
:END:
** Remote-only commits
- 28cbe79 Update compat
- ab5ab0e Update compat
- ac70b4d Update compat
- 6768841 Update compat
- d837d0c Update compat
- d6ec45d Update compat
- 7cc1b4b Update compat
- c9fb4ff Update compat
- f79d382 Update compat
- c1308d9 Update compat

* cond-let (main)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/cond-let
:BRANCH: main
:END:
** Remote-only commits
- c48600d Release version 1.1.3
- ba75950 Fix typo in documentation
- 21b9e98 Release version 1.1.2
- c05079b Fix cond-let-test--105-expand--and$
- 1804968 cond-let--when$: Fix docstring pasto
- 7ec5ace Release version 1.1.1
- 6a133d7 cont-let--when$: Revert accidental change to indent declaration
- 3408e4c Release version 1.1.0
- 49cf45f Update changelog
- 14a30b2 Simplify trivial debug declarations
- fdf0df8 Update advertised shorthands
- ee63a74 cond-let--and$: Accept more than two arguments
- d87c492 cond-let--thread$: New macro
- 0b21b80 Consistently use multiple lines for declare forms
- f11b8bd cond-let--while-let: Set debug declaration directly
- 680cd26 cond-let--while-let*: Fix debug declaration
- 903a5e3 cond-let--when$: Fix debug declaration
- 93768a2 cond-let--{and$,and>}: Declare indentation
- 03e35d7 cond-let--and>: Fix docstring pasto
- 11ad847 Fix name of Author library header
- 28d3b03 Release version 1.0.0
- 2cd0cd5 ci: Reduce permissions and add zizmor setting

* copilot-chat.el (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/copilot-chat.el
:BRANCH: master
:END:
** Remote-only commits
- 28432c5 Merge pull request #248 from chep/dependabot/github_actions/actions/checkout-7
- 2683f7f build(deps): bump actions/checkout from 6 to 7
- 3cbef0c Merge pull request #247 from chep/lint-pass
- 8fa51a7 docs: doc comment widened to 80 chars
- a96ab51 style: apply elisp-autofmt
- d2c9d1e chore: rename all functions and variable with gh- prefix
- 444c7a5 fix(chat-responses): remove unused `top_p` parameter from payload
- 07aa0bc Merge pull request #236 from chep/insert-commit-message-no-clobber
- e90506c Merge pull request #235 from chep/drop-retry
- 1edf908 Merge pull request #237 from chep/dependabot/github_actions/purcell/setup-emacs-8.0
- 1ecba19 Merge pull request #238 from chep/fix-commit-message-prompt
- 80039d4 fix(prompts): restrict commit prompt to generator-only instructions
- 2088399 build(deps): bump purcell/setup-emacs from 7.0 to 8.0
- a0b62fb style: reindent let binding for readability in copilot-chat-git.el
- 47a1f18 fix(git): ignore verbose diff below scissor line in message detection
- bd76600 docs: add copilot-chat-insert-commit-message-no-clobber to README
- 3d7e42b docs: clarify behavior of commit message insertion function
- 39d2974 feat(git): add `copilot-chat-insert-commit-message-no-clobber`
- 8fb3632 fix: remove retry logic from commit message generation

* copilot.el (main)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/copilot.el
:BRANCH: main
:END:
** Changelog diff (=CHANGELOG.md=)
#+BEGIN_SRC diff
diff --git a/CHANGELOG.md b/CHANGELOG.md
index a6da9d9..4870ad8 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -1,23 +1,171 @@
 # Changelog

-## master (unreleased)
+## main (unreleased)
+
+### Changes
+
+- `copilot-login` waits longer for the language server to authenticate with GitHub, and when it times out it points you at proxy/TLS setup (`copilot-network-proxy`, `NODE_EXTRA_CA_CERTS`) instead of only reporting `Authentication failure: Timed out`, which reads like a credential problem when it is usually a TLS-inspecting proxy or firewall.
+- `copilot-login` now stops early with a clear message when the language server returns an empty device code (a sign the request to GitHub was blocked by a proxy or firewall), rather than copying an empty code and timing out later.
+
+## 0.9.0 (2026-07-06)
+
+### New Features
+
+- Add `copilot-chat-compose` (bound to `C-c C-e` in the chat buffer) to draft a multi-line message in a dedicated writable buffer, sending it with `C-c C-c` (as a new conversation or a follow-up, just like `copilot-chat`) or cancelling with `C-c C-k`.
+- Add `copilot-chat-display` (bound to `C-c C-d` in the chat buffer) to show the existing chat buffer without sending a message.
+- Add commands for acting on a chat response, bound in the `*copilot-chat*` buffer:
+  - `copilot-chat-copy-response` (`C-c C-w`) copies the most recent reply to the kill ring.
+  - `copilot-chat-next-turn` (`C-c C-n`) and `copilot-chat-previous-turn` (`C-c C-p`) move between exchanges.
+  - `copilot-chat-retry` (`C-c C-r`) regenerates the answer to the last request, or with a prefix argument pre-fills it for editing before re-sending.
+  - `copilot-chat-rate-response` (`C-c C-t`), along with `copilot-chat-thumbs-up` and `copilot-chat-thumbs-down`, sends good/bad feedback about the current response to GitHub.
+- Show an animated thinking indicator under the `Copilot:` label while a reply is being prepared, so the gap before the first streamed chunk no longer looks dead; it disappears the moment the reply starts (or the turn ends, is cancelled, or errors) and is disabled with `copilot-chat-show-thinking-indicator`.
+- Keep manual scrollback sticky while a reply streams: a chat window is only auto-scrolled to follow new output when it is already at the bottom, so scrolling up to re-read an earlier part of a long response no longer fights the stream.
+- Add `copilot-panel-accept-completion` to insert a solution from the `*copilot-panel*` buffer back into the buffer that requested it. Move to a suggestion and press `RET` or `C-c C-c`; previously the panel could only be read, never accepted from.
+- Add `copilot-chat-compact` (in the `copilot-menu` chat section) to compact the current chat conversation on demand: it asks the server to summarize the discussion so far, reclaiming token budget while the conversation continues. The visible transcript is left as-is, and it reports how many turns were compacted along with the resulting context usage.
+- `copilot-chat-select-model` now flags premium and policy-locked models in the completion list, and selecting a policy-locked one prompts to accept its terms and enables it on the server (via `copilot/setModelPolicy`) before switching, instead of leaving you to hit an error on the next message.
+- Add Bring Your Own Key (BYOK) support, letting you use your own model-provider API keys (Anthropic, OpenAI, Gemini, Groq, OpenRouter, Azure) in chat:
+  - `copilot-chat-byok-add-key` saves a provider API key (read without echoing and held by the language server, never stored or shown by copilot.el), and `copilot-chat-byok-add-model` registers a model to use with it.
+  - `copilot-chat-byok-add-custom-provider` registers a custom OpenAI-compatible endpoint (any provider name that is not built in, with its API type and key); `copilot-chat-byok-add-model` then registers models for it (each with its deployment URL), and `copilot-chat-byok-list-custom-providers` shows the ones you have configured.
+  - `copilot-chat-byok-list`, `copilot-chat-byok-remove-model`, and `copilot-chat-byok-remove-key` manage what you have registered.
+  - `copilot-chat-select-model` lists your registered BYOK models (tagged `[BYOK: PROVIDER]`) alongside Copilot's own, and selecting one routes chat turns to that provider using your key.
+  - Using a BYOK model needs a BYOK-eligible Copilot account; otherwise the server rejects the turn.
+
+### Changes
+
+- `copilot-mode` now binds `TAB` (accept the suggestion), `C-TAB` (accept by word), and `M-n`/`M-p` (cycle suggestions) in `copilot-completion-map`, so inline completion works out of the box. The bindings act only while a suggestion overlay is visible; rebind or clear the map to change them.
+- `copilot-chat-insert-commit-message` now uses the language server's native commit-message generator, which also sees your recent commit subjects (so the result matches your repository's style) and the repository's commit instructions. On a server too old to provide it, it falls back to the previous one-shot chat, so `copilot-chat-commit-message-prompt` still applies there.
+
+## 0.8.0 (2026-07-04)
+
+### New Features
+
+- Add `copilot-chat-select-mode` to choose the chat mode reported by the server (`Ask`, `Agent`, `InlineAgent`, and any custom project modes) instead of only the agent on/off toggle. `InlineAgent` is an agent-kind mode with a restricted tool set aimed at inline editing; the tool-call confirmation, tool registration, and status header apply to it as they do to `Agent`. The selection takes effect on the next new conversation, and without one the existing `copilot-chat-use-agent-mode` toggle still decides between Agent and Ask.
+- Add `copilot-chat-presets`, named bundles of chat settings (model, agent mode, auto-approved tools) that `copilot-chat-apply-preset` switches between in one step. A preset only touches the settings it lists; its model takes effect on your next message and its agent mode on the next new conversation.
+- Show a status header line in the chat buffer with the chat mode (Agent or Ask), the active model, and in agent mode the number of available tools; disable it with `copilot-chat-show-status-header`. ([#509](https://github.com/copilot-emacs/copilot.el/discussions/509))
+- Add one-shot chat task commands that send the active region (or the defun at point) with a canned prompt, with the answer streaming into the regular chat buffer. The prompts are customizable via `copilot-chat-task-prompts`, and `copilot-chat-task` picks a task with completion:
+  - `copilot-chat-review`
+  - `copilot-chat-fix`
+  - `copilot-chat-doc`
+  - `copilot-chat-optimize`
+  - `copilot-chat-write-tests`
+- Add `copilot-chat-rewrite` to rewrite the active region according to a free-form instruction, with the proposed change previewed as a diff and applied only after confirmation. The instruction preamble is customizable via `copilot-chat-rewrite-prompt`, re-indentation of the result can be disabled with `copilot-chat-rewrite-indent`, and a pending rewrite is cancellable with `copilot-chat-stop`.
+- Add native Copilot Code Review support (the same reviewer behind GitHub's code review, via the language server's `copilot/codeReview` methods), with the resulting comments rendered in the chat buffer with file, line, and suggested changes:
+  - `copilot-chat-review-changes` reviews the uncommitted changes (staged and unstaged)
+  - `copilot-chat-review-region` reviews the selected code
+- Add `copilot-chat-insert-commit-message` to generate a commit message from the staged changes and insert it at point (handy in a Magit or `git-commit` buffer), with the instruction sent to Copilot customizable via `copilot-chat-commit-message-prompt` and a pending generation cancellable with `copilot-chat-stop`.
+- Optionally persist chat sessions across Emacs restarts: with `copilot-chat-save-history` enabled (off by default) the transcript is saved after each turn to a per-workspace file under `copilot-chat-history-directory`, `copilot-chat-restore` brings the saved conversation back with its full context, and `copilot-chat-clear-history` deletes the saved file.
+- Handle the server's `copilot/codingAgentMessage` request, so updates from GitHub's cloud coding agent (e.g. the pull request it opens to continue delegated work) are echoed and recorded in the `*copilot-coding-agent*` buffer instead of being dropped.
+- Add `copilot-menu`, a transient (magit-style) menu that gathers the most common Copilot commands (completions, chat, agent mode, account and usage info, and server management) and shows the current state of `copilot-mode`, agent mode, and the selected chat model. Requires the `transient` package (bundled with Emacs 28.1 and newer) but only when the menu itself is used.
+- Add an `edit` choice at the agent-mode tool-confirmation prompt for the tools copilot.el runs itself, letting you tweak the input before it runs: the shell command of `run_in_terminal`, the content of `create_file` (in a temporary buffer), or the URLs of `fetch_web_page`. Server-executed and MCP tools keep the plain yes/no/always prompt, since the server never reads a modified input back.
+- Raise a desktop notification when a chat turn finishes, if it ran at least `copilot-chat-notify-after-seconds` (10 by default; nil disables) and the chat buffer is not the one you are looking at. The backend prefers D-Bus, falls back to `osascript` on macOS and to `message` elsewhere, and is overridable via `copilot-chat-notify-function`.
+- Add `copilot-chat-frontend`, letting you render the `*copilot-chat*` buffer as Org instead of the default GitHub-flavored Markdown. Set it to `org` to write each exchange as foldable Org headings (`* You` / `** Copilot`) with Org highlighting; this needs the `org` library. The choice applies to new chat buffers, and the streamed assistant body stays markdown-ish (fenced code blocks and all) in either frontend.
+
+### Bug Fixes
+
+- Send the selected chat model on every conversation turn, not just the first one, so follow-up messages no longer fail with "A model id is required" on recent language-server versions. The model is now sent via the modern `modelInfo` field, with the deprecated `model` field kept as a fallback for older servers. ([#508](https://github.com/copilot-emacs/copilot.el/issues/508))
+
+### Changes
+
+- Rename the public-code matching feature to use "references" consistently (matching GitHub's own "code references" terminology): `copilot-show-code-citations` is now `copilot-show-code-references` and `copilot-list-code-citations` is now `copilot-list-code-references`. The old names from 0.7.0 still work as obsolete aliases. ([#496](https://github.com/copilot-emacs/copilot.el/pull/496))
+- Give user-facing messages a consistent `Copilot:`/`Copilot Chat:` prefix, and describe attached chat context as "context" throughout (the `copilot-chat-add-file-reference` family keeps its names, which match the server's `references` field).
+- Warn when starting an agent-mode chat with a model that can't call tools, so it's clear why Copilot describes commands to run instead of running them itself. ([#509](https://github.com/copilot-emacs/copilot.el/discussions/509))
+
+## 0.7.0 (2026-06-26)
+
+### New Features
+
+Agent mode tooling:
+
+- Add MCP (Model Context Protocol) server support for agent mode via the new `copilot-mcp-servers` option. Configured servers are forwarded to the language server and their tools become available in `copilot-chat`, each prompting for confirmation like the built-in tools.
+- Add `copilot-chat-list-mcp-tools` to see the connected MCP servers, their status, and their tools, and warn when an MCP server fails to start (previously silent).
+- Preview file changes in a temporary buffer before confirming an agent-mode edit tool (`create_file`, `insert_edit_into_file`, `replace_string_in_file`), so you can see what will be written before approving. Controlled by `copilot-chat-preview-tool-edits` (default on).
+- Offer an `always` choice at agent-mode tool confirmation prompts, alongside yes and no, to approve a tool for the rest of the conversation instead of confirming each call. The choice is remembered until a new conversation starts.
+- Run the agent-mode `run_in_terminal` tool asynchronously instead of blocking Emacs for the whole command. The editor stays responsive, the language-server connection keeps flowing, `C-g` aborts a running command, and `copilot-chat-terminal-timeout` (default 30s) kills runaways. The command's exit status is now reported back to the model.
+- Show a status line in the chat buffer when Copilot runs one of its own built-in or MCP tools that asks for confirmation (`read_file`, the edit tools, etc.), so those tool calls leave a trace, not just the ones copilot.el executes locally.
+
+Workspace access for agent mode:
+
+- Answer the server's `workspace/findFiles` and `workspace/findTextInFiles` requests so agent mode can search the project by file glob and by text/regex (via ripgrep), a step toward whole-workspace questions.
+- Answer the server's `workspace/readFile` and `workspace/readDirectory` requests so agent mode can read files and list directories across the project.
+- Add opt-in semantic search (`copilot-chat-enable-semantic-search`): declare the `watchedFiles` capability and answer the server's `copilot/watchedFiles` request with the project's file list, so the server can build a workspace index for whole-codebase questions.
+
+Chat usability:
+
+- Add `copilot-chat-slash-command` (`C-c /`) to pick and send a chat slash command (`/explain`, `/fix`, `/tests`, `/doc`, etc.) fetched from the server, with optional arguments.
+- Act on chat code blocks: `copilot-chat-insert-code-block` (`C-c C-i`) inserts the block at point into the source buffer, and `copilot-chat-copy-code-block` (`C-c M-w`) copies it to the kill ring, instead of leaving chat output as read-only text.
+- Attach extra context to a chat message: `copilot-chat-add-file-reference` (`C-c C-f`) and `copilot-chat-add-region-reference` send specific files or selections along with the next message, and `copilot-chat-clear-references` drops anything pending.
+
+Account and usage:
+
+- Track Copilot usage quota: surface the server's quota warnings and add `copilot-quota` to show how much of your chat, completion, and premium-request allowance is left.
+- Announce when a suggestion matches public code, and collect the matches (with licenses and reference URLs) in a buffer shown by `copilot-list-code-citations`. Controlled by `copilot-show-code-citations` (default on). ([#471](https://github.com/copilot-emacs/copilot.el/issues/471))
+
+### Changes
+
+- Show readable agent-mode confirmation prompts for the server's own tools (`read_file`, `insert_edit_into_file`, `replace_string_in_file`, etc.) instead of a raw plist dump, falling back to the server-provided message, then the raw input, for any tool we don't recognize. `copilot-chat-auto-approve-tools` is matched exactly so it never auto-approves a same-named tool from a different namespace.
+
+### Bug Fixes
+
+- Fix agent-mode tool confirmations always failing (so Copilot Chat couldn't read files, edit, etc.) by returning the `(:result "accept")`/`(:result "dismiss")` shape the server expects instead of a bare string. ([#483](https://github.com/copilot-emacs/copilot.el/issues/483))
+- Resolve a concrete default chat model from the server when `copilot-chat-model` is nil (preferring the server's designated chat default, then an `auto` model) instead of leaving the model unset, which some servers answer with an empty reply. The lookup runs once per session, only against an already-running server. ([#473](https://github.com/copilot-emacs/copilot.el/issues/473))
+- Surface a turn-level error reported by the server at the end of a chat turn, instead of leaving only an empty reply. ([#473](https://github.com/copilot-emacs/copilot.el/issues/473))
+- Stop the `copilot--infer-indentation-offset` warning from firing while generating chat context, so chatting from a buffer whose mode has no configured indentation offset no longer nags. ([#473](https://github.com/copilot-emacs/copilot.el/issues/473))
+- Report Flycheck diagnostics for the agent-mode `get_errors` tool, not just Flymake, so Flycheck users get real diagnostics instead of "no diagnostics available".
+
+## 0.6.0 (2026-06-22)
+
+### New Features
+
+- Hide regular `copilot-mode` completions while a NES suggestion is pending, so the two no longer overlap on screen. ([#477](https://github.com/copilot-emacs/copilot.el/issues/477))
+- Add experimental Agent mode for Copilot Chat (`copilot-chat-use-agent-mode`). When enabled, Copilot can run client-side tools (`run_in_terminal`, `create_file`, `get_errors`, `fetch_web_page`); each invocation prompts for confirmation unless listed in `copilot-chat-auto-approve-tools`. ([#441](https://github.com/copilot-emacs/copilot.el/issues/441))
+- Add native binary installation support. `copilot-install-server` now falls back to downloading precompiled binaries when npm is unavailable, removing the Node.js requirement on supported platforms. A new `copilot-install-server-native` command is also available for explicit native installation.
+- Add `copilot-chat-select-model` for interactively choosing a chat model, since many more models are available for chat than for completions. ([#465](https://github.com/copilot-emacs/copilot.el/discussions/465))
+
+### Changes
+
+- NES now detects buffer edits via `track-changes` instead of a hardcoded list of editing commands, so it reacts to edits from any command (including `evil` and other non-listed commands). ([#477](https://github.com/copilot-emacs/copilot.el/issues/477))
+- `copilot-chat` now only attaches the originating buffer as context when it is a file-visiting buffer, so invoking chat from an unrelated buffer (dired, `*scratch*`, etc.) no longer sends it as context. ([#470](https://github.com/copilot-emacs/copilot.el/issues/470))
+
+### Bug Fixes
+
+- Dismiss a pending NES suggestion immediately when the buffer is edited (e.g. after accepting a `copilot-mode` completion), instead of leaving a misplaced overlay behind. ([#477](https://github.com/copilot-emacs/copilot.el/issues/477))
+- Clear NES overlays before applying an accepted suggestion so the ghost text no longer lingers over the edit until the next command. ([#477](https://github.com/copilot-emacs/copilot.el/issues/477))
+- Tell the user to enable `copilot-mode` when `copilot-nes-mode` is turned on without it, since NES relies on `copilot-mode` to start and sync the server. ([#477](https://github.com/copilot-emacs/copilot.el/issues/477))
+- Fix Emacs hanging on exit when shutting down the Copilot server (the blocking `jsonrpc-shutdown` is now skipped at `kill-emacs` time). ([#469](https://github.com/copilot-emacs/copilot.el/issues/469))
+- Fix NES insertion text never rendering because its zero-width overlay was marked `evaporate` and got deleted immediately. ([#451](https://github.com/copilot-emacs/copilot.el/issues/451))
+- Suppress "Request was canceled" error messages in the echo area. ([#464](https://github.com/copilot-emacs/copilot.el/pull/464))
+
+## 0.5.0 (2026-03-16)

 ### New Features

 - Add Copilot Chat support (`copilot-chat`, `copilot-chat-send`, `copilot-chat-send-region`, `copilot-chat-reset`) using the `conversation/*` LSP methods. ([#446](https://github.com/copilot-emacs/copilot.el/pull/446))
 - Add Next Edit Suggestions (`copilot-nes-mode`) via `textDocument/copilotInlineEdit`. NES predicts edits anywhere in the file based on recent editing patterns. ([#447](https://github.com/copilot-emacs/copilot.el/pull/447))
+- Add `copilot-chat-stop` command to cancel in-flight streaming.
+- Show mode-line streaming status indicator for chat.
+- Display follow-up suggestions after chat responses.
+- Show errors inline in the chat buffer.
+- Use `markdown-mode` GFM font-lock in the chat buffer when available.
 - Add Chat, NES, Panel Complete, Clear Overlay, Select Completion Model, and Reinstall Server entries to the Copilot mode menu.
+- Add GitHub issue templates for bug reports and feature requests.

 ### Changes

 - Add a default error handler to `copilot--async-request` so async failures (e.g. cancelled completions) are logged to `*Messages*` instead of being silently swallowed.
 - Remove unused `copilot-server-log-level` defcustom (dead code — nothing read it).
 - Improve `copilot-log-max` docstring to explain the `*copilot events*` buffer.
+- Convert `copilot--satisfy-predicates` from macro to function for simplicity.

 ### Bug Fixes

 - Fix chat `conversation/create` sending `allSkills` as a string array instead of a boolean, which caused a schema validation error on `copilot-language-server` v1.436.0+. ([#452](https://github.com/copilot-emacs/copilot.el/issues/452))
 - Fix `copilot-complete` being immediately cancelled when called from wrapper commands (lambdas, user-defined functions) whose name doesn't start with `copilot-`. ([#453](https://github.com/copilot-emacs/copilot.el/issues/453))
+- Fix chat not displaying replies from newer server versions. ([#459](https://github.com/copilot-emacs/copilot.el/issues/459))
+- Fix `:json-false` treated as truthy for `external` in `window/showDocument` handler.
+- Normalize `:json-false` busy flag in `didChangeStatus` handler. ([#457](https://github.com/copilot-emacs/copilot.el/issues/457))
+- Guard against `:json-null` in server responses.
+- Clear `copilot--completion-initiated-p` flag in idle timer path to prevent stale state. ([#454](https://github.com/copilot-emacs/copilot.el/issues/454))
+- Suppress `copilot-max-char` warning when `copilot-max-char-warning-disable` is non-nil.

 ## 0.4.0 (2026-02-26)
#+END_SRC

* corfu (main)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/corfu
:BRANCH: main
:END:
** Changelog diff (=CHANGELOG.org=)
#+BEGIN_SRC diff
diff --git a/CHANGELOG.org b/CHANGELOG.org
index 4c8f36e..6f8def1 100644
--- a/CHANGELOG.org
+++ b/CHANGELOG.org
@@ -2,6 +2,15 @@
 #+author: Daniel Mendler
 #+language: en

+* Version 2.10 (2026-05-19)
+
+- Improve deduplication of candidates.
+- Require Compat 31.
+
+* Version 2.9 (2026-03-09)
+
+- Optimize frame repositioning.
+
 * Version 2.8 (2026-01-18)

 - Auto completion code factored out to =corfu-auto.el=.
#+END_SRC

* csv-mode (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/csv-mode
:BRANCH: master
:END:
** Remote-only commits
- b7eca73 Update csv-mode

* deadgrep (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/deadgrep
:BRANCH: master
:END:
** Remote-only commits
- 8525723 Update deadgrep--arguments test for --hidden and --glob=!/.git
- 9ba86ac Address review feedback on context increment/decrement helpers
- 1ce8b70 Add functions to increment/decrement context

* dumb-jump (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/dumb-jump
:BRANCH: master
:END:
** Remote-only commits
- cf06b4c Fix compiler warnings (#548)
- 9ce4598 Fix stray colon in Elixir defprotocol rule causing severe slowdown (#549)

* el-get (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/el-get
:BRANCH: master
:END:
** Remote-only commits
- 447b7efc Merge: migrate-type-go (#3021), elpa-repo-prio (#3022), remove-nxhtml (#3023), ac-php-deps (#3024)
- b1750d31 Split off ac-php-core.rcp & company-php.rcp from ac-php.rcp
- aff3e510 Let :repo entries in recipes override package-archive-priorities
- c8dc1ad0 nxhtml.rcp: Remove
- edae6caf Make :type elpa tests more consistent
- 5860242d Deprecate :type go recipes
- f51b5343 go-def.rcp: Same as go-rename
- 024c2687 go-company.rcp: Delete
- 71a489a5 go-imports.rcp: Same as go-rename
- 7a7baf6c go-flymake.rcp: Switch to :type github
- c5c85450 go-errcheck.rcp: Switch to :type github
- 28b957dc go-rename.rcp: Switch to :type github
- b608a290 go-lint.rcp: Delete
- 08d9e598 Merge branch 'more-explanation' (#3020)
- 16fc1de3 Update el-get.info
- 619db1bd README.md: Reference info manual Setup & Authoring Recipes
- c863c129 README.md: Don't reference el-get-bundle
- 6eb16ad1 More docs and better warning about double-activation
- 3f6f76e8 Update documentation for earlier changes
- c9a73802 Merge branch 'installer-updates' (#3019)
- d3582d60 Drop support for Emacs before 24.5 (#3018)
- 6c286691 Default installer to shallow clone
- 6dceb026 Fix for Emacswiki bot check
- eeb48603 Merge branch 'doc-updates' (#3017)
- ff01583c Remake el-get.info
- b030b161 el-get.texi: el-get-sources docstring -> recipe format
- fa6dfd56 Explain :build/system-type nil vs (progn nil)
- 4e6692ab Document el-get-recipe-path-elpa
- d9f978df Document that recipe files may be named example.el
- dfc31d6f Merge branch 'rcp-update' (#2264)
- 057d98d8 test.yml: Add paths-ignore for push & pull_request
- 57458f7f Mark package <op> buttons as visited after activating them
- f8728396 test/el-get-test: fix test
- 1e65365c Fix byte-compile warning, assume 24.3+
- e7b88664 Fix use of make-text-button for Emacs 28+
- 76734f7d Add tests for el-get-compute-new-status
- f0820270 Add buttons to warning for update cached recipes
- 239f6aa2 Always update safe props into status.el file
- 645b4afb Move button defs and insertion to el-get-core
- ec3b0a05 Merge 'docfix' (#3014), 'recipes' (#3015) and 'test-update-load-path'
- bd01a862 Fix el-get-bundle-parse-name (#3012)
- d49dac4a Add test for update of load-path
- 0ec91c44 ws-butler.rcp: Moved to non-GNU ELPA
- f329c530 ecb.rcp: New upstream, req Emacs >= 29 per its README
- 239232bb mu4e.rcp: fix BSD build
- b044f0a2 Skip tests also for .md files
- b4489764 Fix MacOSX install-info link
- 4fdd0250 Merge pull request #3013 from npostavs/testing-etc
- 93cd729d test.yml: Some final tweaks to caching and logging
- 7bb28a7c Remove fundamental-mode-hook binding, needless ignore calls
- cf2b7063 Ignore doc file changes, we have no tests for them
- 6ee9bc4c Add 24.5, release-snapshot; cache snapshots for only a week
- f0df549e test/github-actions.sh: make it executable
- fcbaabea Move COMMIT_RANGE computation to .github/...yml
- 7503d7d1 Use dorny/paths-filter, simplify github-actions.sh
- d31d4329 Don't require el-get files from el-get-byte-compile.el!
- a20d248e .github/workflows: Add caching
- 4eed2417 Byte compile each file individually; fix resulting warnings
- 23649555 Clean up test scripts
- cb7a1922 Make it easier to run github-actions locally

* elisp-tree-sitter (release)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/elisp-tree-sitter
:BRANCH: release
:END:
** Changelog diff (=CHANGELOG.md=)
#+BEGIN_SRC diff
diff --git a/CHANGELOG.md b/CHANGELOG.md
index c31290f..8e33622 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -5,6 +5,21 @@ The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

 ## [Unreleased]

+## [0.19.4] - 2026-01.16
+- perf: Use hash table instead of alist
+
+## [0.19.3] - 2025-12.23
+- Push tag
+
+## [0.19.1] - 2025-12.23
+- Remove unused el files
+
+## [0.19.0] - 2025-12.22
+- Make button without newline (#259)
+- feat: Support android
+- Fix error on NetBSD (#256)
+- Upgraded `tree-sitter` crate to 0.26.3
+
 ## [0.18.0] - 2022-02-12
 - Added APIs to traverse the syntax tree: `tsc-traverse-do`, `tsc-traverse-mapc`, `tsc-traverse-iter`. The traversal is depth-first pre-order.
 - Improved syntax tree rendering's performance in `tree-sitter-debug`.
@@ -149,7 +164,14 @@ The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).
 ## [0.1.0] - 2020-01-27
 Initial release

+
+<!-- Links -->
+
 [Unreleased]: https://github.com/emacs-tree-sitter/elisp-tree-sitter/compare/0.18.0...HEAD
+[0.19.4]: https://github.com/emacs-tree-sitter/elisp-tree-sitter/compare/0.19.0...0.19.4
+[0.19.3]: https://github.com/emacs-tree-sitter/elisp-tree-sitter/compare/0.19.0...0.19.3
+[0.19.1]: https://github.com/emacs-tree-sitter/elisp-tree-sitter/compare/0.19.0...0.19.1
+[0.19.0]: https://github.com/emacs-tree-sitter/elisp-tree-sitter/compare/0.18.0...0.19.0
 [0.18.0]: https://github.com/emacs-tree-sitter/elisp-tree-sitter/compare/0.17.0...0.18.0
 [0.17.0]: https://github.com/emacs-tree-sitter/elisp-tree-sitter/compare/0.16.1...0.17.0
 [0.16.1]: https://github.com/emacs-tree-sitter/elisp-tree-sitter/compare/0.16.0...0.16.1
#+END_SRC

* emacs-async (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/emacs-async
:BRANCH: master
:END:
** Remote-only commits
- 5faab28 Merge pull request #199 from jwiegley/fix-issue-165-encoding-comments
- 9919943 Clean up encoding comments after utf-8-auto fix
- 9cc1e80 Load cl-lib in format checks for consistent indentation
- 475177f Fix indentation in async-bytecomp, async-package, and dired-async
- b048bd6 Fix CI failures: indentation check and Windows test timing
- 77a63ec Rewrite README and update packaging metadata
- d9db5a1 Add pre-commit hooks, Makefile targets, and CI
- e04f113 Add Nix flake with dev shell and CI checks
- c9ad8bc Fix X-URL header and Eask test script

* emacs-libvterm (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/emacs-libvterm
:BRANCH: master
:END:
** Remote-only commits
- 9a32a4a feat: add DEL for terminal Emacs, handle C-DEL
- 9495966 fix: use current tramp method instead of default
- d8157e7 feat: show vterm directory in buffer listings
- 92add72 fix: remove unused resizing flag.
- 4c07415 fix: prevent metadata array corruption on reflow-triggered sb_pop.
- 028a2a1 fix: avoid resize metadata corruption with reflow
- 995e68e feat: enable reflow feature from vterm 0.3+.
- 54c29d1 Fix backtrack on doubleclick in vterm

* emacsmirror-mirror (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/emacsmirror-mirror
:BRANCH: master
:END:
** Remote-only commits
- c05ac94 Update Emacsmirror mirror
- 714f999 Update Emacsmirror mirror
- 870c469 Update Emacsmirror mirror
- 942e073 Update Emacsmirror mirror
- 8871599 Update Emacsmirror mirror
- 14a740f Update Emacsmirror mirror
- d293479 Update Emacsmirror mirror
- e0af077 Update Emacsmirror mirror
- ae4a5fc Update Emacsmirror mirror
- 6688905 Update Emacsmirror mirror
- 65d0c26 Update Emacsmirror mirror
- cfbb8eb Update Emacsmirror mirror
- 32d22e4 Update Emacsmirror mirror
- eda595e Update Emacsmirror mirror
- f2922ce Update Emacsmirror mirror
- 081d81a Update Emacsmirror mirror
- b8cce7f Update Emacsmirror mirror
- c6fe16d Update Emacsmirror mirror
- 04b45f0 Update Emacsmirror mirror
- 880a5bc Update Emacsmirror mirror
- 13c43f7 Update Emacsmirror mirror
- 0fd4b97 Update Emacsmirror mirror
- c4d4ac4 Update Emacsmirror mirror
- daef71e Update Emacsmirror mirror
- 012315a Update Emacsmirror mirror
- a7802bc Update Emacsmirror mirror
- 3c952ad Update Emacsmirror mirror

* emacsql (main)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/emacsql
:BRANCH: main
:END:
** Remote-only commits
- d811bbe Release version 4.4.1
- d45f3de Adapt to changes in pg v0.60
- af71afd Release version 4.4.0
- 182e662 emacs-pg.el: Remove pre-Emacs-28 kludge
- f668110 Bump minimal Emacs release to 28.1
- 7ee5c9d Use string-replace
- 2fe6d45 Release version 4.3.6
- f1bf30a ci: Prevent template injection
- 5fdfe57 ci: Pin third-party actions to oids and add zizmor settings

* expreg (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/expreg
:BRANCH: master
:END:
** Remote-only commits
- 80e31a7 Update expreg

* flycheck (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/flycheck
:BRANCH: master
:END:
** Remote-only commits
- 96f1852c Add asciidoc-mode support to AsciiDoc checkers
- bf620f70 [Fix #2170] Use LC_MESSAGES=C instead of LC_ALL=C for checkers
- 7992f4f2 [Fix #2163] Disable native compilation in emacs-lisp checker
- 9cfc75b1 [Fix #2166] Stop awk-gawk flagging valid scripts
- c8c96b91 [Fix #2169] Parse Ruff's severity-tagged output format
- 0e5eb830 Add neocaml-opam-mode support to the opam checker

* forge (main)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/forge
:BRANCH: main
:END:
** Changelog diff (=CHANGELOG=)
#+BEGIN_SRC diff
diff --git a/CHANGELOG b/CHANGELOG
index 46882bf..46340b9 100644
--- a/CHANGELOG
+++ b/CHANGELOG
@@ -1,4 +1,49 @@
 # -*- mode: org -*-
+* v0.6.7    2026-07-01
+
+- Adjusted to breaking change in ~magit-branch-p~.  f3ad5910
+
+- Fixed a regression when visiting blob or copying its URL.  #858
+
+* v0.6.6    2026-06-01
+
+- Fixed a regression in interaction with Ghub.  #848
+
+- No longer error if ~ssh~ is missing when looking up a host in
+  ~forge-alist~.
+
+- ~forge-pull-this-topic~ is now bound to ~G~ in ~forge-topic-mode-map~.
+
+* v0.6.5    2026-05-01
+
+- Fixed a pagination bug, which could completely prevent pulling API
+  data for certain Github repositories.  #843
+
+- Fixed username lookup to work as documented.  #796
+
+* v0.6.4    2026-04-08
+
+- Improved the fallback default when reading the target branch for
+  a pull-request.  57b015f6e
+
+- Fixed updating the prompt and listing of available choices after
+  expanding the set of completion choices.  5c00a3820, 68eabd05c
+
+- Added ~forge-mark-completed-topics-as-done~ to the topic and topics
+  menus.  c27fcfe36
+
+- Beside ~C-u RET~, ~C-u C-RET~ can now also be used to visit the topic
+  at point and at the same time display the topic menu.  b372fadba
+
+- Added a kludge to work around an issue in Helm.  That has been
+  fixed in Helm as well, but no version containing that fix has
+  been released yet.  https://github.com/emacs-helm/helm/issues/2744
+
+- Fixed fetching individual topics.  #830
+
+- When reading a topic and lifting the selection limit based on state,
+  also lift the limit based on absolute number of topics.  7d06a0ddc
+
 * v0.6.3    2026-01-01

 - By default ~forge-fork~ now only forks the default branch on Github.
#+END_SRC

* ghub (main)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/ghub
:BRANCH: main
:END:
** Changelog diff (=CHANGELOG=)
#+BEGIN_SRC diff
diff --git a/CHANGELOG b/CHANGELOG
index fdb218d..70dc17c 100644
--- a/CHANGELOG
+++ b/CHANGELOG
@@ -1,4 +1,35 @@
 # -*- mode: org -*-
+* v5.2.2    2026-07-01
+
+- Raised shields.
+
+* v5.2.1    2026-06-01
+
+- Increased robustness and backward compatibility or ~ghub--token~
+  and ~ghub--username~.
+
+* v5.2.0    2026-05-01
+
+- Fixed username lookup to work as documented.
+
+* v5.1.0    2026-04-01
+
+- Loading ~ghub~ no longer loads ~ghub-legacy~ and the functions defined
+  in the latter are now declared obsolete.  a81e83c
+
+- Callbacks and errorbacks no longer have to accept four mandatory
+  arguments, they can also just accept one.  42ebf09
+
+- The debugger no longer falsely assumes all GraphQL queries come
+  with a payload/variables.  f1cd5be
+
+- GraphQL errors can now be ignored, which allows processing the
+  data received for the subqueries that succeeded.  3866c82
+
+* v5.0.4    2026-03-01
+
+- Thoughts and whitespace.
+
 * v5.0.3    2026-01-01

 - Various build improvements.
#+END_SRC

* gnu-elpa-mirror (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/gnu-elpa-mirror
:BRANCH: master
:END:
** Remote-only commits
- 1a75d55 Update mirror list
- 49ddb26 Update mirror list
- 733b67f Update mirror list
- 10bab98 Update mirror list
- fcb6b61 Update mirror list
- e964a28 Update mirror list
- efea39e Update mirror list
- 9253373 Update mirror list
- 498425e Update mirror list
- a87bfef Update mirror list

* go-mode.el (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/go-mode.el
:BRANCH: master
:END:
** Remote-only commits
- 3a71d28a docs: fix a typo
- 8aaaa9d2 Don't store execution result to a unused `content-buf` var
- 33c5144d go-mode.el: fix "unused variable num" waning
- 144dc7d3 Fix missing "lexical binding directive" warnings
- bf04f831 Fix many "wrong quoting in docstring" warnings

* highlight-indent-guides (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/highlight-indent-guides
:BRANCH: master
:END:
** Remote-only commits
- 08a0859 Merge pull request #137 from bumblepup/dependabot/github_actions/actions/checkout-7
- 667106f Bump actions/checkout from 6 to 7

* json-mode (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/json-mode
:BRANCH: master
:END:
** Remote-only commits
- 466d5b5 Drop a spurious space.
- 376f3f6 Reference the existence of jsonc-mode in the README
- 77125b0 Bump version to 1.9.2
- 9a46569 Fix font-lock of jsonc-mode.
- 72f340d Bump version to 1.9.1
- 7e7934d Fix setting font-lock-defaults on load
- bfd1557 Fix patterns of skip-chars-backward
- b1b56c3 Enable lexical binding
- a93a0c7 Bump version to 1.9.0
- 481d5d9 Make forward-sexp skip dot in float.
- 20b9c21 Fix byte-compile warning
- be84ab4 Fix checkdoc error
- 8844263 Fix json-increment-number-at-point to respect prefix arg
- f3fdc40 Merge pull request #70 from purcell/package-tidy-up
- dc2f34b Merge pull request #71 from tareefdev/master
- 8487a1a Update README about indent width.
- 013e531 Merge pull request #73 from VlachJosef/master
- 6f34dca More checkdoc fixes
- 9a3570d Fix package-lint issues
- e6bbab1 font-lock-keywords is always buffer-local
- 3015fbf Don't use key sequence reserved for user customisations
- 0df861c Custom group name should not contain the -mode suffix
- 5d39117 Fix indentation issues
- 5519171 reflect using built-in json.el

* jsonrpc (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/jsonrpc
:BRANCH: master
:END:
** Remote-only commits
- d8143c5 Update jsonrpc
- 0135c78 Update jsonrpc
- 89042d6 Update jsonrpc
- 70dc180 Update jsonrpc

* kubel (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/kubel
:BRANCH: master
:END:
** Changelog diff (=CHANGELOG.md=)
#+BEGIN_SRC diff
diff --git a/CHANGELOG.md b/CHANGELOG.md
index f3d8df8..91bed6f 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -3,13 +3,11 @@ All notable changes to this project will be documented in this file.

 ## [Unreleased]
 - more unit tests
-- `q` to exit resource details (`quit-window` or `kill-buffer`)
 - show diff and confirmation before applying a change
 - easily get logs for a previous pod instance
 - top po command + sort by usage
 - safe way to help with rollbacks?
 - run `kubectl describe pod`
-- `kubectl` apply a buffer to current context/namespace

 ## [3.1.0] -
 ### Added
@@ -20,22 +18,29 @@ All notable changes to this project will be documented in this file.
 - Support for `eat` terminal emulator
 - `kubel-set-namespace` support for optional parameter to refresh the namespace cache
 - Custom default namespace `kubel-default-namespace` with default to `"default"`
+- Better error handling
+- Buffer tracking with dired-style `q` to go back and `Q` to clean up everything.
+- Namespace listing and improved namespace selection

 ### Changed
 - Remove suffix "(default)" when acting on resources of StorageClasses
 - Better parsing of the kubectl version string via JSON
 - Fix describe resources not going to top of buffer
-- using -- for all kubectl exec uses
+- Using -- for all kubectl exec uses
 - Fix `kubel--exec` doesn't work on remote systems
 - Fix `dir-prefix` for TRAMP without explicitly specifically the username
-- vterm sessions to use their own buffers
-- case consistent resource names ("pods)
+- Vterm sessions to use their own buffers
+- Case consistent resource names ("pods)
 - Fix: don't assume that kubectl will use the namespace default implictely
+- Better buffer behavior
+- Lazy context initialization to avoid kubectl calls at load time
+- Better behavior for marking/unmarking resources
+- Fixed TRAMP method clobbering when switching context/namespace

 ### Removed
 - `kubel-kubernetes-version` no longer cares about version patch
-- support for kubernetes v1.13 and below
-- support for emacs 25 and below
+- Support for kubernetes v1.13 and below
+- Support for emacs 25 and below

 ## [3.0.0] - 2022-09-09
 ### Added
#+END_SRC

* kubernetes-el (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/kubernetes-el
:BRANCH: master
:END:
** Remote-only commits
- 2a64e83 build(deps): bump actions/checkout from 6 to 6.0.2 (#410)
- 3b735cd build(deps): bump codecov/codecov-action from 5 to 6 (#407)
- f4c471c test: fix format-seconds compat for Emacs 31 (Bug#75849) (#408)
- 54ad1b1 docs: update changelog for 0.19.0 (#404)
- da6f585 Remove redundant hook field overrides for actionlint (#403)
- c969838 Add zizmor pre-commit hook for workflow security analysis (#402)
- 9947a70 Add actionlint pre-commit hook for GitHub Actions linting (#401)
- 63df263 chore: remove unused PR workflow and update CI badge reference (#400)
- 1f7a1b3 ci: replace PR comment with GitHub step summary for snapshot test failures (#399)
- aaff467 Create a separate workflow to test
- 727b1dd ci: rename workflow name from 'Kubernetes-el CI' to 'CI' (#395)
- 15e140e fix(ci): move permissions to job level in PR workflow (#392)
- 480307b ci: update PR workflow name and branch triggers (#391)
- 44347a4 fix(ci): simplify workflow trigger syntax in PR workflow (#389)
- 770de96 fix(ci): correct GitHub Actions permissions configuration (#388)
- 101c742 ci: enhance security and dependency management (#387)
- 56ca1bf Fix Github Actions permissions (#386)

* lab.el (main)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/lab.el
:BRANCH: main
:END:
** Changelog diff (=CHANGELOG.org=)
#+BEGIN_SRC diff
diff --git a/CHANGELOG.org b/CHANGELOG.org
index de2df24..6cb01b9 100644
--- a/CHANGELOG.org
+++ b/CHANGELOG.org
@@ -1,3 +1,23 @@
+* 3.8.0
+
+- Improve project path extraction, fixes #21.
+- Add ~lab-list-participated-merge-requests~. This command shows you the latest merge requests that you have participated in some way, such as approving them, commenting on them, or being assigned to them.
+- Show the associated merge request title in ~lab-list-project-pipelines~ for pipelines triggered by a merge request (thanks to @jasonatwell).
+- Show the merge request title (or trigger source and ref) in ~lab-list-watched-pipelines~ via a new "Trigger" column (thanks to @jasonatwell).
+- Fix some byte-compile warnings (~{if,when}-let~ → ~{if,when}-let*~).
+
+* 3.7.0
+
+- Add ~lab-config~ for supporting multiple GitLab hosts/tokens.
+- Add tabulated view for currently watched pipelines (~M-x lab-list-watched-pipelines~).
+- Automatically select the sole item when there is only one candidate in selection prompts (thanks to @JaidynReiman).
+- Fix byte-compile warning related to MR variables.
+- Fix some potential host resolution errors.
+
+* 3.6.0
+
+- Added /Merge Request Overviews/, a similar view to "Overview" tab of the GitLab Merge Request view.
+
 * 3.5.0

 - Bunch of merge request review improvements:
#+END_SRC

* let-alist (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/let-alist
:BRANCH: master
:END:
** Remote-only commits
- a331a39 Update let-alist
- 7b945f8 Update let-alist
- bed0f0e Update let-alist

* llama (main)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/llama
:BRANCH: main
:END:
** Remote-only commits
- 4d40240 Release version 1.0.5
- e6d2127 Fix name of Author library header
- 57a8988 Use compat-31
- b6ce42a llama--{,match-and-}fontify: Improve compile kludge
- 03379ce ci: Reduce permissions and add zizmor setting

* magit (main)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/magit
:BRANCH: main
:END:
** Changelog diff (=CHANGELOG=)
#+BEGIN_SRC diff
diff --git a/CHANGELOG b/CHANGELOG
deleted file mode 100644
index 7de08f05..00000000
--- a/CHANGELOG
+++ /dev/null
@@ -1,617 +0,0 @@
-# -*- mode: org -*-
-* v4.5.0    2026-01-01
-
-- Removed support for loading Magit extensions from ".git/config".
-  Please [[https://github.com/magit/magit/commit/d662ec107e74a05a0348ca75c89ebd127d658656][use ~.dir-locals.el~ instead]].  d662ec107e
-
-- Reworked how the global ~magit-auto-revert-mode~ is, or is not,
-  enabled at startup.  Enabling ~global-auto-revert-mode~ now disables
-  this mode.  38a916f555..de766d2968 et al.
-
-- Simplified ~magit-wip-mode~.  This involves removing all other wip
-  modes (which allowed using only a subset of the protection offered
-  by ~magit-wip-mode~) and using the new ~magit-before-change-functions~
-  and ~magit-after-apply-functions~ hooks.  98ef107037..fa629ad5b5
-
-- Added experimental support for calling Lisp hooks from Git hooks,
-  and added a few such hooks: ~magit-common-git-post-commit-functions~,
-  ~magit-git-post-commit-functions~, ~magit-git-post-merge-functions~
-  and ~magit-git-post-rewrite-functions~.
-
-- Replaced option ~magit-section-visibility-indicator~ (singluar) with
-  new option ~magit-section-visibility-indicators~ (plurarl) to better
-  support emacs sessions that have both terminal and gui frames.
-  2d8f43e681
-
-- ~mouse-set-point~ is now remapped to Magit's variant.  fa5906621a
-
-- ~magit-autoloads~ is now loaded exactly once.  3d1a008f18
-
-- Fixed custom type of ~magit-blame-styles~.  84c5c8c8c6
-
-- Fixes regression in ~magit-rebase-autosquash~.  It no longer excluded
-  the merge-base.  54e460a701
-
-- A few hook functions were not autoloaded.  #5464
-
-- Some refinements were calculated for the diff in the commit
-  message buffer, despite not actually being displayed.  #5483
-
-- Commands for showing logs for wip branches were no longer displayed
-  in the log menu, despite ~magit-wip-mode~ being enabled.  #5488
-
-- Added new option ~magit-wip-debug~.  655bc502a3
-
-- Various build improvements.
-
-* v4.4.2    2025-10-06
-
-- Addressed harmless compiler warning.
-
-* v4.4.1    2025-10-06
-
-- Added new option ~magit-log-trailer-labels~.  #5432
-
-- Added new command ~magit-delete-shelved-branch~.  31c0ae17d3
-
-- Added new variable ~magit-save-repository-buffers-predicate~.  #5450
-
-- Fixed saving the value of the menus ~magit-diff~, ~magit-log~,
-  ~magit-diff-refresh~ and ~magit-log-refresh~.  c7ba53b4b4
-
-- A form from the ~cond-let~ library made it into the autoloads file,
-  where that library isn't being loaded.  #5451
-
-- ~magit-revert-rev-file-buffer~ did not respect a value of ~nil~ for
-  ~enable-local-variables~.  5eb31a419a
-
-- Improved commands for jumping to specific sections in revision
-  buffers.  #5442
-
-- Improved consistency of scroll command bindings.  #5453
-
-- It is possible again to exit ~magit-completing-read-multiple~ with
-  no input if REQUIRE-MATCH is ~t~.  b5066e5fa4
-
-- Diffs that appear in logs could not be parsed in some edge cases.
-  #5454
-
-* v4.4.0    2025-09-09
-
-- At least Emacs 28.1 is required now.  Emacs 30.1 was released
-  earlier this year, so Emacs "oldoldstable" is still supported.
-
-- Code for tracking a position within a file across versions and
-  diffs, and the commands that build on that to allow jumping to
-  the same location in another version or diff, are undergoing
-  heavy refactoring and improvements.  This is paying off technical
-  dept and prepares for upcoming user-level improvements as well
-  as for new features.  Some changes in behavior already appear in
-  this release, as described below.
-
-- For staged and unstaged changes, ~magit-diff-visit-file~ (~RET~) now
-  always visits the blob actually corresponding to the line at point,
-  as it already did for committed changes.  Previously it failed to
-  visit the index blob from an unstaged removed line when that file
-  has staged changes; and it failed to visit the index blob from a
-  staged added line.
-
-  Visiting the file in the worktree is very useful, but, instead of
-  ~RET~, I recommend using ~C-<return>~ (~magit-diff-visit-worktree-file~).
-  The new option ~magit-diff-visit-prefer-worktree~ allows restoring
-  the old behavior.  See [[https://docs.magit.vc/magit/Visiting-Files-and-Blobs-from-a-Diff.html][Visiting Files and Blobs from a Diff]].
-
-- "Blob navigation" is now aware of index blobs.  It is possible to
-  navigate between such blobs, and the HEAD blob or the file on disk,
-  using the same key bindings used to time travel from one committed
-  blob to another.  See [[https://docs.magit.vc/magit/Minor-Mode-for-Buffers-Visiting-Blobs.html][Minor Mode for Buffers Visiting Blobs]] and
-  [[https://docs.magit.vc/magit/Commands-for-Buffers-Visiting-Files.html][Commands for Buffers Visiting Files]].
-
-- When quitting a blob-visiting buffer with ~q~, it is no longer
-  killed, iff it is being displayed in an additional window.  The
-  behavior can be changed by binding ~q~ in ~magit-blob-mode-map~ to
-  one of the commands ~magit-bury-or-kill-buffer~, ~magit-bury-buffer~,
-  or ~magit-kill-this-buffer~.  The former two are new commands.
-  731642756f
-
-- ~magit-status-here~ now places the cursor within the diff of unstaged
-  changes, if the current line in a file-visiting buffer has unstaged
-  changes.  If it does not, then it falls back to going to the
-  corresponding staged change.  #4814
-
-- Even though ~auto-revert-mode~ is enabled, if that has not yet kicked
-  in and the user visits a file that needs reverting, they used to be
-  asked to confirm the revert.  efaf91d8af
-
-- ~magit-completing-read~ and ~magit-completing-read-multiple~ can now
-  require non-empty input.  If REQUIRE-MATCH is ~any~, then the user
-  can exit with input that does not appear in the collection, but
-  is not allowed to exit with zero input.  #5422
-
-- ~magit-completing-read-multiple~ now adds the default choice to the
-  set of valid choices.  7167f70665
-
-- ~magit-read-branch-or-commit~ can now exclude more than one possible
-  choice from the set of valid choices.  10c366ed52
-
-- Improved minibuffer input for worktree commands, including adding
-  new option ~magit-read-worktree-directory-function~.
-
-- Section visibility indicators can now be displayed in the left
-  margin.  #5424
-
-- References in the ~refs/pullreqs~ namespace are no longer offered
-  as completion candidates.  87ddd8f7c4
-
-- Improved font-lock in ~git-rebase-mode~.  3857b8bce9..9654f5e094
-
-- Added new face ~magit-diff-conflict-heading-highlight~.  2e76ec9337
-
-- Added new command ~magit-revision-jump~, a menu for use in
-  ~magit-revision-mode~, bound to ~j~.  acf71f7eb4
-
-- ~magit-dired-jump~ now supports ~magit-repolist-mode~.  8c4a903886
-
-- Fixed a regression ~magit--refresh-buffer-set-positions~, added in
-  v4.3.7, which messed up the window-start.  406a3094a8
-
-- ~magit-setup-buffer~ and ~magit-refresh-buffer~ take two new keyword
-  arguments INITIAL-SECTION and SELECT-SECTION.  Two arguments of
-  ~magit-setup-buffer-internal~ are now keyword arguments.
-  ced0984540, c4def6d99a
-
-- All text inserted into Magit buffers that previously used ~face~
-  now use ~font-lock-face~ and ~magit-section-mode~ can therefore set
-  ~font-lock-defaults~.  Previously if some third-party packages used
-  ~font-lock-add-keywords~, some of Magit's own fontification would
-  be lost.  Third-party packages that use ~magit-section-mode~, might
-  need to be adjusted accordingly.  7de0f1335f,  b4768acb1f
-
-- Daring to use my new Cond-Let package and other shorthands.
-  08dafc1732..1c48327a06, b377adb68c
-
-* v4.3.8    2025-07-05
-
-Bugfixes:
-
-- In v4.3.7 we started to restore the display-start positions of the
-  windows displaying a buffer that is being refreshed, but in log
-  buffers that is undesirable.  After creating a new commit we want
-  that to immediately be displayed at the top of windows displaying
-  log buffers.  #5403
-
-- Refreshing was skipped after discarding all untracked files.
-
-- When sub-directories contain ".gitignore" files, then invoking
-  ~magit-discard~ on the "Untracked files" section did not necessarily
-  remove the same set of files as listed in that section.  (At least
-  it did show the files, which would be removed, in the confirmation
-  prompt.)  #5405
-
-* v4.3.7    2025-07-01
-
-- Refreshing a buffer causes its content to be recreated, which can
-  result in scrolling.  Now we attempt to restore the display-start
-  positions of the windows displaying the buffer.  #5403
-
-- When running ~git~ for side-effects and that signals an error, we
-  did not augment the error message to inform the user that the full
-  output can be found in the process buffer.  When the error is not
-  signaled, we did already append that hint to the message.  Also
-  added a new option, ~magit-show-process-buffer-hint~, for people who
-  are already aware of the process buffer and would like to avoid
-  the noisy reminder.  #5396
-
-Bugfixes:
-
-- Some instructions inserted into rebase sequence buffers were not
-  prefixed with ~comment-start~, instead # was used unconditionally.
-  #5388
-
-- Fixed a regression in v4.3.6, which caused a conflict between the
-  overlays used for section highlighting and section selection, if
-  both of these faces relied on the same face property (likely the
-  foreground color).  #5389
-
-- Hunks could remain painted as selected when navigating away.  #5393
-
-- The log arguments "--grep" and "--graph" are no longer mutually
-  exclusive, but we still dropped the latter when the former was
-  used. #5391
-
-- ~magit-save-repository-buffers~ errored on older Emacs releases,
-  if the value of the variable by the same name is ~ask~.  #5399
-
-- ~magit-after-save-refresh-status~ did not honor ~magit-inhibit-refresh~.
-  d9d2f6c312
-
-* v4.3.6    2025-06-01
-
-- Generalized and heavily refactored the code, previously used only to
-  highlight diff-related sections and to paint hunks, so that it can
-  be used for other sections as well.  The new implementation should
-  also be a bit faster.  cfe4faaaf6^..c556fee1bd
-
-- A single-section selection now requires a non-empty region.
-  6c4c16942a
-
-- When creating a stash, "On <branch>: " is now offered as a second
-  future history element.  75c6191999, 9b81df36b4
-
-- Added new commands ~git-rebase-drop~, ~git-rebase-alter~ and
-  ~git-rebase-squish~.  479c467080, 9674c4755a
-
-Bugfixes:
-
-- Fixed issues concerning date handling in log margins.  #5373
-
-- The list of rebase actions in the status buffer could contain
-  invalid elements derived from comments in Git's list of such
-  actions.  91806dc729
-
-- Applying the region used to fail, if some part of the same hunk but
-  outside the region has conflicts, even if the part in the region did
-  not.  9e551d9eb7
-
-* v4.3.5    2025-05-14
-
-Bugfixes:
-
-- Fixed a v4.3.3 regression in inserting rebase actions into the
-  status buffer when stopping at the last commit.  #5365
-
-* v4.3.4    2025-05-13
-
-- Arguments, that are normally set by cycling through the possible
-  values displayed in the menu, can now also be set using completion,
-  by using a prefix argument.  #5362
-
-Bugfixes:
-
-- Matching references were no longer displayed on the first line of
-  revision buffers.  48b158500d
-
-- Fixed a v4.3.3 regression in inserting rebase actions into the
-  status buffer when ~--rebase-merges~ is specified.  #5365
-
-- On the "Untracked files" section, ~k~ no longer worked as intended.
-  #5366
-
-Also contains code and documentation cleanups and improvements.
-
-* v4.3.3    2025-05-01
-
-- ~magit-section-show-level~ now acts on all selected sections.  #5354
-
-- Inserting the list of commits being rebased into the status buffer
-  is now much faster.  8e72767262
-
-- All rebase actions are now inserted into the status buffer.
-  69b310e109
-
-- While editing the list of commits and actions to be rebased
-  ~git-rebase-kill-line~ (~k~) on a commented line now uncomments it.
-  ff44ee1bc3
-
-- Added new variants of commands that deal with files for use in Dired
-  buffers, and improved existing variants.  In Dired, these commands
-  are available from ~magit-file-dispatch~ (~C-c M-g~).  542c2f8a75 et al.
-
-- ~magit-branch-shelve~ now prepends the date to the refname, and
-  ~magit-branch-unshelve~ removes such prefixes.  78ffd1a389
-
-- The new function ~magit-insert-shelved-branch~ can be added to
-  ~magit-refs-sections-hook~ to list shelved branches.  d6b7784547
-
-- Added new command ~magit-wip-purge~, which removes old branches
-  created by ~magit-wip-mode~.  d5e0f3a639
-
-- ~magit-blame-styles~ now supports inserting truncated commit hashes.
-  #4057
-
-- Added new command ~magit-mouse-set-point~.  3c9e519197
-
-Also contains code and documentation cleanups and improvements.
-
-* v4.3.2    2025-04-01
-
-- Removed legacy options ~magit-wip-*-mode-lighter~.  225ea6fd00
-
-- ~magit-log-current~ now falls back to displaying the log for ~HEAD~ if
-  no branch is checked out, and the now redundant ~magit-log-head~ is
-  no longer displayed in the ~magit-log~ menu by default.  c8b1e12bd5
-
-- Renamed ~magit-merge-into~ to ~magit-merge-dissolve~ and changed the
-  key binding in the ~magit-merge~ menu from ~i~ to ~d~.  The description
-  in that menu already was "dissolve".  Do this to make it more
-  obvious that this command deletes the source branch after it has
-  been merged into the target branch.  #4386
-
-- Added new option ~magit-process-apply-ansi-colors~ (but discourage
-  its use).  #5348
-
-- Support for Ido has been moved out into a new package ~magit-ido~.
-  6aec967ee4
-
-Bugfixes:
-
-- ~magit-after-save-refresh-buffers~ did not respect
-  ~magit-inhibit-refresh~.  c0a8e694b9
-
-- When washing of a section was delayed and it ends in an empty line,
-  that line was not always made part of the section.  f6f25e6566
-
-- In some cases section specific key bindings were not made available
-  as intended.  6ce1ece580, 3f79700f1b
-
-- In some cases ~magit-toggle-buffer-lock~ has to uniquify buffer names
-  but failed to do so.  #5330
-
-- After applying a hunk, the buffer was refreshed twice.  #5343
-
-- The diff shown by ~magit-diff-paths~ was not washed. #5093
-
-Also contains more code and documentation cleanups and improvements.
-
-* v4.3.1    2025-03-02
-
-- Added new option ~magit-format-file-function,~ and two functions to
-  optionally prefix file names with icons, with the help of either
-  ~all-the-icons~ or ~nerd-icons~.  #5308
-
-- Added new commands ~magit-previous-reference~ and ~magit-next-reference~,
-  with entry point ~C-c C-r~.  Enable ~repeat-mode~ to keep navigating with
-  ~p~ and ~n~.  #5310
-
-Bugfixes:
-
-- ~magit-commit-revise~ failed if no arguments were used.  #5306
-
-- Some arguments were missing from diff menus when invoked from
-  the status buffer.  #5309
-
-- In some menus the bindings for ~--signoff~ conflicted with those for
-  other arguments.  #5312
-
-- Fixed unlikely issue in ~magit-git-mergetool~.  66e3ddffe4
-
-- Unknown Git trailers resulted in a display error while writing
-  commit messages.  8c27c910ca
-
-- When the word at point matched the name of a branch, that was
-  unconditionally treated as the commit-at-point.  This should only be
-  done when that word is shown using an appropriate face.  2b3f2cb9ad
-
-- Fixed bug in ~magit-section-cycle-diffs~.  #5319
-
-- ~magit-stage-untracked~ was a bit fragile.  #5325
-
-* v4.3.0    2025-02-04
-
-- Added new option ~magit-refs-show-branch-descriptions~.  42ed6c1966
-
-- When a stash cannot be applied using the trivial method, the user is
-  offered some fallback methods.  The presentation of those has been
-  improved.  #5253 a08b4dd513
-
-- Added new hook options ~magit-revision-wash-message-hook~ and
-  ~magit-log-wash-summary-hook~, and populate them with new and
-  existing highlighting functions, making it easier to remove default
-  highlighting and to add custom highlighting.  This also increases
-  consistency between how commit summaries are shown in logs and when
-  displaying complete commit messages.  f54fce0ecc..b86fe009e2
-
-- ~amend!~ markers are now highlighted like ~fixup!~ and ~squash!~ markers.
-  #5261
-
-- ~magit-commit-create~ no longer amends to HEAD when called with a
-  prefix argument.  The ~magit-commit~ menu offers four amend commands.
-  That should be good enough.  5e60aa72e5
-
-- ~magit-commit~ no longer features the obscure ~magit-commit-reshelve~ by
-  default, but it can quickly be reinstated, using the level mechanism.
-  20eb323b47
-
-- Added new commands ~magit-commit-alter~ and ~magit-commit-revise~,
-  completing the already extensive set of "fixup" commands.  #5261
-
-- Improved commit menu, documentation and implementation details.
-  #5261
-
-- The branch at point is detected in more contexts now, i.e., when
-  there is not actually a branch at point, but one can unambiguously
-  be derived from the thing at point.  4876f1921e
-
-- Reworked ~magit-process-password-prompt-regexps~ to be more permissive
-  and better structured.  Hopefully that means we have to extend it
-  less frequently going forward, when users run into new prompts.
-  #5288
-
-- Speed up listing untracked files in the status buffer, simplify how
-  the list is configured, and give up on optionally using a tree.
-  #5284
-
-- Argument ~--signoff~ is now available in all menus that create commits.
-  However, it is no longer shown in any menu by default.  See the end
-  of [[https://docs.magit.vc/transient/Enabling-and-Disabling-Suffixes.html][Enabling and Disabling Suffixes]] to learn how to enable it in all
-  menus at once in a single action.  #5297
-
-- Began using the ~##~ macro from the ~llama~ package.  0a64982100
-
-- Stopped depending on the ~dash~ package.  e40e8f1994
-
-Bug fixes:
-
-- When applying a stash, it was not always discovered when the trivial
-  method was unsuccessful, and so the user was not offered the use of
-  a fallback method.  #5253 929eb4dca5
-
-- ~git-commit.el~ did not require ~magit-process~, which was only a
-  problem when it is loaded without also loading the rest of Magit.
-  #5280.
-
-- The use of an external diff drivers was not prevented in some
-  places.  #5282
-
-- ~magit-blame-maybe-show-message~ did not protect against interpreting
-  % in commit messages as %-specs.  d0e795f423
-
-- Parts of commit message headers lost the intended background color.
-  46c3d8b0ad
-
-- The confirmation prompt of ~magit-worktree-delete~ failed to name the
-  affected worktree.  #5286
-
-- The wrong suffix color was used for ~magit-commit-absorb~ and
-  ~magit-commit-autofixup~.  bfadd41079
-
-- ~magit-stash-index~ did not use ~magit-stash-read-message-function~.
-  #5295
-
-- Fixed an error that occurred when creating ~magit-hunk-section-map~
-  and the user has disabled ~smerge-command-prefix~.  The same bug
-  exists in Emacs since 29.1, so this will only help users stuck
-  on Emacs 28.  #5300
-
-- When the value of a diff or log menu was being initialized from the
-  arguments in the current buffer and the diff/log was already limited
-  to a set of files, then all other arguments were discarded.  #5304
-
-* v4.2.0    2025-01-01
-
-- At least Git 2.25.0 is required now.  033a0c0cdc
-
-- At least Emacs 27.1 is required now.  c1a86066e8
-
-- Added new command ~magit-toggle-profiling~.  f637dd1877
-
-- Added new command ~magit-toggle-subprocess-record~.  ec1f403af1
-
-Bug fixes:
-
-- Fixed a regression in ~transient-init-value~.  5b4c4aea1b
-
-- Fixed setting ~fill-paragraph-function~ in
-  ~git-commit-setup-changelog-support~.  139e0fcff3
-
-- ~magit-log-refresh~ lacked the ~--since~ and ~--until~ arguments, which
-  were already available in ~magit-log~.  3ecebe8d11
-
-- Enabling verbose output in ~magit-commit-absorb~ caused an error.
-  #5272
-
-- In logs, no longer strip ~heads/~ prefix from branch names if a tag
-  with the same name exists.  5cb3492464
-
-- ~magit-list-special-refnames~ returned nonsense.  #5277
-
-* v4.1.3    2024-12-06
-
-- For most important sections, if an error occurs while inserting the
-  section, the error message is now displayed in the section body.
-  #5255
-
-- ~magit-submodule-populate~ now supports ~--recursive~.  #5191 #5256
-
-- Improved ~magit-process-password-prompt-regexps~.  #5257
-
-Bug fixes:
-
-- ~magit-stash-pop~ and ~magit-stash-apply~ sometimes installed conflicts
-  for the user to resolve that are more complicated than they need to
-  be.  #5253
-
-- ~magit-stash-push~ placed ~--~ before other arguments.  #5260
-
-- ~magit-autorevert~ failed to require ~magit-process~.  #5263
-
-* v4.1.2    2024-11-02
-
-- Add various minor process logging improvements:
-  5b30c05d3a magit--git-insert: Collapse process section if appropriate
-  b11524120e magit--git-insert: Optionally always log to process buffer
-  cd6cf89d6a Use different face for debug-only process sections
-  bba06845de magit-process-insert-section: Improve file-path comparison
-  f2a6133443 magit-run-git-async: No longer clutter ~*Messages*~ buffer
-
-Bug fixes:
-
-- If the left margin was in use before ~magit-blame-mode~ started using
-  that margin, then the old width was not restored when the mode was
-  disabled.  #5236
-
-- Prior to Tramp being loaded, setting ~magit-tramp-pipe-stty-settings~
-  to ~nil~ resulted in an error, due to ~tramp-pipe-stty-settings~ not
-  being bound yet.  #5240
-
-- ~magit-copy-section-value~ no longer did anything for most section
-  types.  #5244.
-
-- Global git arguments often got added twice to the list of arguments
-  ultimately passed to git.  914285a5e8
-
-- Inserting the headers of status buffers involves temporary changes
-  to ~magit-insert-section-hook~.  These changes were not restricted to
-  the current buffer, causing errors when ~magit-git-debug~ is enabled
-  and we thus insert sections in the process buffer, while the status
-  buffer is being refreshed.  11e13640c4
-
-- Some ~git~ errors were not logged despite ~magit-git-debug~ being
-  enabled.  874fb0fede
-
-- ~magit-browse-thing~ and ~magit-visit-thing~ tried to turn anything
-  at point into an URL.  Now the bail if there is no URL at point.
-  7c842b8ac0
-
-* v4.1.1    2024-10-01
-
-- Avoid unnecessary work when ~auto-revert-remote-files~ is ~nil~.  #5222
-
-- Improved default choice offered by ~magit-branch-reset~ and
-  ~magit-reset-*~.  #5230
-
-Bug fixes:
-
-- Added a workaround for a regression in Git v2.46.0.  #5212
-
-- Section-specific bindings were removed when a section was expanded
-  whose body is not inserted until the expansion takes place.
-  9395de2c94
-
-- Addressed an incompatibility with Eglot.  #5226
-
-- Adapted to a change in ~define-globalized-minor-mode~ in Emacs 30,
-  which caused ~diff-hl-mode~ to be enabled in blob buffers.  #5229
-
-- When adding the commit at point to the completion defaults, it was
-  assumed that ~minibuffer-default-add-function~ cannot be nil.
-  6d0075f523
-
-- ~magit-blame--format-string-1~ didn't handle a list of faces
-  correctly.  5395798301
-
-- Addressed an incompatibility with Indent-Bars.  #5233
-
-* v4.1.0    2024-09-01
-
-- The library ~git-commit.el~ is no longer distributed as a separate
-  package, ~git-commit~, but as part of the ~magit~ package.
-
-- Improved ~magit-tag-release~'s consistency and handling of arguments.
-  #5102
-
-- Updated tooling and other housekeeping.
-
-Bug fixes:
-
-- Only use an explicit range in ~magit-insert-recent-commits~, when also
-  using ~--graph~.  With ~--graph~ it increases performance noticeably,
-  but without it decreases performance somewhat.  #5075
-
-- ~magit-completing-read-multiple~ now shows the default choice in the
-  prompt, if a completion framework is used, for which that is useful.
-  #5205.
-
-* Older releases
-
-See ~docs/RelNotes/~.
diff --git a/CHANGELOG b/CHANGELOG
new file mode 120000
index 00000000..676ade62
--- /dev/null
+++ b/CHANGELOG
@@ -0,0 +1 @@
+docs/CHANGELOG.4
\ No newline at end of file
#+END_SRC

* marginalia (main)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/marginalia
:BRANCH: main
:END:
** Changelog diff (=CHANGELOG.org=)
#+BEGIN_SRC diff
diff --git a/CHANGELOG.org b/CHANGELOG.org
index 599826f..954f08a 100644
--- a/CHANGELOG.org
+++ b/CHANGELOG.org
@@ -2,6 +2,10 @@
 #+author: Omar Antolín Camarena, Daniel Mendler
 #+language: en

+* Version 2.11 (2026-05-19)
+
+- Require Compat 31.
+
 * Version 2.10 (2026-03-09)

 - Avoid slowdown due to Tramp in ~abbreviate-file-name~.
#+END_SRC

* markdown-mode (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/markdown-mode
:BRANCH: master
:END:
** Changelog diff (=CHANGES.md=)
#+BEGIN_SRC diff
diff --git a/CHANGES.md b/CHANGES.md
index 3640ac3..64e1c5a 100644
--- a/CHANGES.md
+++ b/CHANGES.md
@@ -7,7 +7,15 @@
 *   New Features:

 *   Bug fixes:
+    - `markdown-table-align` now handles fullwidth characters correctly [gh-937][]

+*   Improvements:
+    - `markdown-preview` displays the buffer name as the page title
+    - skip export tests if export command is not installed
+    - reduce memory allocations in property checking functions
+    - improve performance to check properties in range by using c functions
+
+  [gh-937]: https://github.com/jrblevin/markdown-mode/pull/937

 # Markdown Mode 2.8
#+END_SRC

* mcp.el (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/mcp.el
:BRANCH: master
:END:
** Remote-only commits
- 2d17280 Merge pull request #86 from 4lex-Levy/fix/list-items-null-params
- f88c81e Send empty object {} instead of null for */list params
- f10768e fix: Remove unused `item-uri` variable from content handler
- 69b4edb ci: Add workflow_dispatch trigger to CI workflow
- 2ab5d1e feat(mcp-hub): add resource reading capability
- e1cc488 feat(mcp-hub): add imenu support and resource URI property
- 52fa16f feat(mcp-hub): add dedicated detail mode with outline navigation
- fd3542b Merge branch 'master' into feature/mcp-server-details-ui
- 53f61cb feat(mcp-hub): improve prompt display formatting
- 84c8dca feat(mcp-hub): Add server detail view with 'd' key

* melpa (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/melpa
:BRANCH: master
:END:
** Remote-only commits
- 35e65c9e Add recipe for denote-solo (#10076)
- 7b5d551c Update recipe for org-incoming (#10072)
- 20175420 Add recipe for srs (#10067)
- d771cf49 Add recipe for browsel (#10066)
- 17017ad0 Add recipe for hutch (#10065)
- 537d8f60 Add recipe for kusanagi-theme (#10063)
- 7a34c963 Add recipe for ol-locate-file (#10058)
- 16eb6bd8 Add recipe for jal (#10046)
- 4e939f55 Fix recipe for dicom to include missing shell scripts (#10077)
- 36648d80 Update "Getting started" documentation
- 9c190316 Capitalise MELPA in badges
- 48a1640e Use the familiar colours for the snapshots/releases web pages
- a534c388 Prefer [beta] to (Beta) in new site titles
- 82895d2a Update upstream of loopy-dash
- c078f84e Update upstream of loopy
- d4fc9674 Add recipe for fzfa (#10041)
- d26ac835 Add recipe for youtube-music (#10061)
- cf0a2b5b Add recipe for musicbrainz-interactive (#10054)
- cd76e9b4 Add recipe for git-overleaf (#10047)
- 14479a3c Customize web pages for the (now) 4 sites, and provide a switcher dropdown
- 9673611d Specify cert-name explicitly for certbot
- ae6f0233 Use same format and colors for badges on experimental channels
- fadd025b Update domain list for certbot
- 389b2d9b Fix fetching just once per cycle
- e0a2cd69 Remove test subdomains, and respond to acme challenges for new subdomains
- cbf597e4 Update docker image versions to v3.
- 8f601695 builder: Remove redundant setting
- 1074a6c3 builder: Fix fetching just once per cycle
- eb3e8e0c Link to all packages directories the same way
- e6e0ba08 Fix symlinks to package directories of new channels
- 5d548cab Merge Package-Build 998f1d92a
- 7e50ce3b Squashed 'package-build/' changes from 47a50e759..998f1d92a
- 66c40bfa Merge branch 'publish-channels'
- 7c0d33a9 Publish "snapshots" and "releases" channels (#10057)
- b9d109b8 Prepare to publish "releases" channel
- c76b93a6 Prepare to publish "snapshots" channel
- 4634ffc7 Remove recipe for ede-compdb
- 7a51fd6e Update upstream of tengo-mode
- 166bc08a Update upstream of popterm
- 28dfcdf4 Update upstream of counsel-fd
- 94c60de3 Add recipe for clatter (#10049)
- 2eb97279 Add recipe for notmuch-multi (#10044)
- f5d525e6 Add recipe for countdown-modeline (#10043)
- 20a81a26 Add recipe for leadkey (#10032)
- 6f4c3e13 Update upstream of plain-theme
- e1786f70 ci: Use ubuntu-26.04 image for Emacs 30.1
- c01159a8 docker/nginx/default.conf: Use same SSL settings for all channels
- 4813c1ad docker/nginx/default.conf: Use consistent whitespace
- 36f143f3 docker/syncer.run.sh: Cosmetics
- 6df4b934 ci: Bump actions/checkout
- 7a591840 builder: Fix typo
- c2aa6694 Get secretaria from emacsattic
- 7a493d69 Add recipe for pure-light-theme (#9973)
- 8d43ade3 Add recipe for shexc-ts-mode (#10051)
- 469e38ed Update upstream of zeno-theme
- f5dc52f2 Update upstream of exotica-theme
- a5308691 Update upstream of bitbake
- 227657f5 Squashed 'package-build/' changes from c072cbce6..47a50e759
- 5db2cf30 Merge Package-Build 47a50e759
- 0a978eaf make: Update name of a lisp variable
- 0b31954d Squashed 'package-build/' changes from dd951e217..c072cbce6
- 428e00eb Merge Package-Build c072cbce6
- 5c4189d3 Get magit-popup from emacsattic
- fefa3d4f Update recipes for bracket-face and parenthesis-face
- fa2e546d Add recipe for desert-theme (#10038)
- 4e8c83c9 Rename recipe from oai to cui (#10034)
- 65dce69c Add recipe for sleek-modeline (#10029)
- 091b5d89 Update recipe for ai-code (#10028)
- 100e7d9b Add recipe for bluesky (#10021)
- bbf2574e Fix `url` that should be `repo`.
- e5b57543 Update bibtex-capf and consult-notes to use `repo` for codeberg!
- c333f3b3 Fix the codeberg info for tabspaces.
- fd2082f1 Update upstream of gemini-write
- 099c58c6 Update upstream of tabspaces
- 0ed44190 Update upstream of consult-notes
- 2afff308 Update upstream of bibtex-capf
- 5c484500 Include githooks directory in magit package
- aec4a76e Clean up recipes for my packages
- adbdf035 Add recipe for lsp-ltex-plus (#10014)
- 3bf8f443 Add recipe for mac-ime (#10027)
- f4276c94 Update recipes for consult-notes, tabspaces, bibtex-capf (#10026)
- 94e6b48c Update recipe for unicode-math-input (#10023)
- 15ade73f Add recipe for f90-ts-mode (#10019)
- de274a2c Add recipe for sysml-mode (#10009)
- 1e983188 Add recipe for ox-zola (#10006)
- 763d5a95 Add recipe for phony (#9986)
- 523f6f9e Add recipe for elot (#9411)
- 1d888e12 Move dieggsy package sources to sourcehut (#10025)
- cae9f7dd Squashed 'package-build/' changes from ba841867c..dd951e217
- 1ef16a62 Merge Package-Build dd951e217
- 19ea4983 make: Add json to BUILD_TARGETS
- 8a46fb29 make: Support erroring if a build fails
- 9cc42ced make: Support configuring CHANNELS
- bcf0684d make: Support changing message prefix
- 7d1f8a1a make: Clean up
- 141f7312 Add recipe for meshtastic (#10017)
- 7869ef9d Add recipe for fzf-native (#10012)
- 28893a37 Add recipe for use-package-x (#10008)
- 08801af1 Add recipe for clutch (#10000)
- d3e16dbc Remove recipe for org-movies
- f4da28e6 Remove recipe for tox
- 60746f36 Remove recipe for isgd
- 9e136e4a Remove recipe for gotest-ts
- 1e1e825c Update upstream of sexy-theme
- 447bea53 Squashed 'package-build/' changes from de12829d7..ba841867c
- a7b39c6a Merge Package-Build ba841867c
- 3cdcfc7c make: Indent then/else blocks
- 317b214b make: Rename docker targets
- 3972bc88 make: Support showing commands run by make
- 5cc3f07d make: Silence more targets
- c9f9b741 make: DOCKER_INHIBIT_PACKAGE_PULL use lisp boolean
- 865d2994 make: Rename USER_CONFIG to CONFIG
- bd650cff make: Add new build targets
- 04461b65 make: Improve consistency of channel variable names
- 7a922e92 make: Improve and fix path variables
- 8c79ff67 make: Collect EMACS* variables in one place
- b13a2b4b make: No longer support EMACS_COMMAND variable
- bc7ca4bd make: Rearrange
- 9f259d9f make: Default to help if targets are omitted
- ee0c9ee4 make: Tweak time variables
- 03c70b77 make: Add OPENPGP_CMD variable and some cosmetics
- 05119ba7 make: Use a list of words for DOCKER_BUILD_CHANNELS
- ae07c973 Make names of new channels plural
- bcedf998 Add recipe for rare-words (#9713)
- 0a16d7c3 Remove recipe for lsp-tailwindcss
- ff02973c Remove recipe for supergenpass
- 1715bc37 Update upstream of sketch-themes
- b5b0b336 Update upstream of copilot-chat
- bb59486e Update upstream of coc-dc
- c811127f Squashed 'package-build/' changes from 9ffd40c4f..de12829d7
- 687881e8 Merge Package-Build de12829d7
- b8bb0d76 Update org-elisp-help's repository
- f04da02b Update upstream of ox-rss
- 41d8e39c Update upstream of liberime
- d86b05b6 Add recipe for verdict-dart (#9996)
- 73d14d4e Add recipe for shannon-max (#9990)
- 6082e33d Add recipe for claude-code-context (#9983)
- 8f1634e6 Add recipe for nucleo-completion (#9981)
- 497d8e93 Add recipe for apple-container-tramp (#9980)
- 06df34c7 Add recipe for folgezett (#9975)
- a9c676b5 Add recipe for gnus-browse-url-in-article (#9972)
- 8dc4f277 Add recipe for indigo (#9970)
- a5c3eb10 Add recipe for har-viewer (#9945)
- eb0134db Update rimel  repository (#10001)
- 46c94001 Squashed 'package-build/' changes from 02cd758bd..9ffd40c4f
- 6086970f Merge Package-Build 9ffd40c4f
- ea3f23c8 Revert "make: Move most code to package-build.mk"
- 9df72a56 Remove recipe for powerline-evil
- 9fdde45d Update recipe for egison-mode
- 601f563f docker: No longer double down on WORKDIR
- 7c2751c5 make: Move most code to package-build.mk
- b1987503 make: Default to unstable channel
- d328de2f Update recipe for package-build
- 4273435d Squashed 'package-build/' changes from d0e278803..02cd758bd
- dc124413 Merge Package-Build 02cd758bd
- 11181b36 Add recipe for ttx-mode (#9995)
- c384223e Add recipe for simulacrum (#9985)
- 3e16dc8d Add recipe for tmux-csi-u (#9977)
- cad99860 Add recipe for outline-stars (#9966)
- 49fc1d7a Add recipe for emcp (#9964)
- 9e734f2a make: Cosmetics
- 10c84ed9 make: Rely on new default values for new channels
- f91f15b4 make: No longer double down on package-build-all-publishable's default
- 1f539ff3 Squashed 'package-build/' changes from 2db42a905..d0e278803
- 79270359 Merge Package-Build d0e278803
- c1d99b61 Add recipe for sql-bigquery (#9938)
- 81dfb7ff Add recipe for mysql (#9933)
- ad2ba82b Add recipe for grove (#9930)
- 7e31ba56 Update contributor guidelines re: AI assistance (#9982)
- 4b3677dd Add recipe for elfeed-web
- d006f2f4 Update elfeed's repository
- 34db1ff3 Merge pull request #9984 from martenlienen/codeberg-links
- 6d961f08 Remove unused line
- b7b544af Update geiser-racket's repository
- 243e7a58 Update geiser-mit's repository
- d66972d8 Update geiser-guile's repository
- bdd0a29c Update geiser-gambit's repository
- 9fff1d93 Update geiser-chicken's repository
- eff5e148 Update geiser-chibi's repository
- 8baa86e8 Update geiser-chez's repository
- 008e3547 Update geiser's repository
- 7c0803ce Show links to codeberg repositories on the webpage
- 98eeaf37 Remove recipe for zprint-format
- 887d577c Remove recipe for cljstyle-format
- ae45d94d Update upstream of zeno-theme
- 9c5fa988 Update upstream of pyim-smzmdict
- 46bd94ae Update upstream of pyim-cangjiedict
- 4a8119a4 Update upstream of exotica-theme
- bf967692 Update trust-manager's repository
- 555731d0 Update recipe for http-server (#9968)
- fc803a93 Add recipe for with-command-redo (#9967)
- 021a4e6c Add recipe for mozc-modeless (#9963)
- eaae76cb Add recipe for denote-wordcloud (#9956)
- 6f590a3e Add recipe for org-lark (#9955)
- 32c111d2 Add recipe for evil-ghostel (#9949)
- 99def78e Add recipe for verdict (#9935)
- ad07f43a Update contributor guides re: package maturity, AI assistance (#9965)
- eeeeea7b Squashed 'package-build/' changes from e8294cdb9..2db42a905
- ded37adf Merge Package-Build 2db42a905
- f8080802 make: Set package-build-directory
- 47af3281 Squashed 'package-build/' changes from c1f7ad004..e8294cdb9
- 51ecd060 Merge Package-Build e8294cdb9
- 779b9f02 Update upstream of ppcompile
- 038be2fb Add recipe for http-server (#9962)
- 6ff99a63 Add recipe for rocq-timing (#9960)
- 9071d1f1 Add recipe for rimel (#9959)
- 0e4256c9 Update README.md mention of package-filter (#9958)
- f0dc2855 Update recipes for ekg, ekg-agent (#9957)
- 44ef6cd0 Add recipe for citar-vulpea (#9952)
- 00d0501c Add recipe for hidepass (#9950)
- ed513f41 Add recipe for elixir-iex (#9948)
- dbca8509 Add recipe for sidebuf (#9947)
- 6deff573 Add recipe for go-prettify-mode (#9943)
- 02b81b5a Update recipe for sekka (#9941)
- a1351271 Add recipe for agent-recall (#9939)
- 02a68ff9 Add recipe for python-unicode-escape (#9937)
- ba6a664d Add recipe for comet-trail (#9931)
- 21d2caa9 Update recipe for yasnippet (#9925)
- 3217105e Add recipe for batppuccin (#9920)
- fffc98c3 Add recipe for modus-ewal-theme (#9892)
- f2431024 Squashed 'package-build/' changes from de5b4dada..c1f7ad004
- 94daf92e Merge Package-Build c1f7ad004
- e4d79aed Add recipe for popterm (#9940)
- 420c42f0 Add recipe for trust-manager (#9944)
- 8c63f6cc Add recipe for struct-completion (#9936)
- a099c936 Update recipe for mason (#9934)
- a5b095a1 Add recipe for taskjuggler-mode (#9922)
- d33bde29 Add recipe for tokyo-night (#9919)
- 354d1a22 Add recipe for agitjo (#9909)
- a8c8dab8 Add recipe for ben (#9898)
- 2be96378 Remove recipes for buck and gtea
- 6470b150 Remove recipe for evedel
- e0baa2db Update upstream of mason
- f092656c Add recipe for ghostel (#9927)
- f24e79c3 Add recipe for evim (#9891)
- ba50bf00 Add recipe for org-grimoire (#9926)
- c78f6870 Add recipe for meshmonitor-chat (#9923)
- 6909d0c8 Add recipe for corg (#9929)
- 3d2da2cc Add recipe for occult (#9924)
- 8d44a7e1 Add recipe for compilation-history (#9921)
- e3077035 Add recipe for org-invox (#9914)
- 3ed0179d Update recipe for ekg (#9906)
- a22f0e2a Add recipe for org-dynamic-templates (#9904)
- 77fae332 Remove recipe for jabber (#9928)
- e74998c6 Update upstream of highlight-indent-guides
- b974e508 Update upstream of ancient-theme
- c75bc0fa Tweak kubernetes recipe
- 6b8f6e4e Add back kubernetes-el package (#9918)
- 1b0cf4a2 Add recipe for fsharp-ts-mode (#9917)
- 24e1db14 Add recipe for tetris-60 (#9913)
- 328f3a01 Add recipe for flywrite (#9912)
- 501bbeae Add clj-doc-browse recipe (#9911)
- a36e96ae Add recipe for ros-face (#9910)
- 66298393 Add recipe for anju (#9908)
- 38ff946b Add recipe for ancient-theme (#9907)
- fae4dc16 Add recipe for llm-test (#9903)
- 70914049 Update recipe for eglot-python-preset
- 1164513f Add recipe for eglot-typescript-preset (#9900)
- 551a03a9 Add recipe for vterm-editor (#9886)
- 42f93817 Add recipe for tiles (#9901)
- e69dbea2 Update recipe for eglot-python-preset (#9899)
- 02251220 Add recipe for consult-symbol (#9895)
- b2b31075 Add recipe for flymake-zizmor (#9893)
- f2be16a7 Add recipe for org-tag-cloud (#9890)
- cd8a9337 Add recipe for org-snitch (#9874)
- 38c2f8a1 Add recipe for async-http-queue (#9863)
- 60910110 Add recipe for buffer-guardian (#9902)
- bbfcabf7 Remove recipes for whisper and easy-find (#9896)
- 713b74aa Split recipe for ekg into ekg, ekg-agent, ekg-denote (#9889)
- a6c4b7c3 Add recipe for stan-ts-mode (#9884)
- 2f8402b4 Add recipe for markdown-table-wrap (#9883)
- 8b0ca2cf Remove recipe for erk

* multiple-cursors.el (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/multiple-cursors.el
:BRANCH: master
:END:
** Remote-only commits
- 94b8b07 Remove redundant check of use-region-p that is already done in use-region-beginning/end
- 6f984c6 Also update furthest-cursor-before-point for transient-mark-mode inactive
- 47c27f1 Check use-region-p rather than mark-active to better support transient-mark-mode disabled

* nongnu-elpa (main)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/nongnu-elpa
:BRANCH: main
:END:
** Remote-only commits
- 2e12d173f9e * elpa-packages (loopy, loopy-dash):  Update :url
- 3253fbf4376 elpa-packages (flamegraph): Remove the now unnecessary properties
- 75687fd1d60 elpa-packages (flamegraph): New package
- 9a138bb3091 elpa-packages (elfeed): Use `README.org` as manual as well
- cfe12e4cf36 Consistently refer to the new ELPA repos
- a29333a1561 * elpa-packages (eprolog): New package
- d64ad23a24b * elpa-packages (elfeed, elfeed-web): New packages
- 76309a3b279 Point Geiser packages to Codeberg
- 2b137764563 * elpa-packages (simple-httpd): New package
- 9589680ec7c elpa-packages (jabber): New package
- ca486510165 elpa-packages (subatomic-theme): Update URL
- 8ef0a1e1a08 elpa-packages (afternoon-theme): Update URL again
- a9ecd3dc98a * elpa-packages (selected-window-contrast): New package
- 6adba33a4d7 * elpa-packages (eldoc-mouse-nov): New package

* org (main)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/org
:BRANCH: main
:END:
** Remote-only commits
- 5ce6801d3 ; org-colview: Use `rx' for checkbox count cookie match
- e2e20b64e testing/lisp/test-ob-clojure.el: Simplify check for Clojure support
- e5be37422 testing/lisp/test-ob-clojure.el: Add tests
- f9225c2b4 lisp/ob-clojure.el: Stop tangling code that is used for evaluation
- 8bd3c9192 ox-beamer: New keyword BEAMER_THEME_PRE
- 33162c3ab ol-docview: Fix docstring.
- 13a25caeb ox-beamer.el: add ltx-talk support
- 4f39f176d lisp/ol.el: Add option `org-link-default-file-link-description'
- 7f7c010db Use string-replace for easier reading
- fd5d3b1a5 ox-latex.el: Add DocumentMetadata
- 3e3699928 ox-beamer.el: Add myself as maintainer
- beb10b13b Merge branch 'bugfix'
- 114cd49f6 ; org-agenda: Ensure the correct tree is chosen for indirect buffers
- 4ec885840 ob-tangle: Fix jump-to-org when tangled code block contains headlines
- 123b797fc org: Clear flyspell overlays inside footnote definition
- 4125489b4 doc/org-manual.org: Fix Glenn Gould example
- 293bc2eae org-colview: Capture column view without rendering overlays
- 07c6db9bc org-colview: Add quick help for column view
- 1a8de87b6 org-buffer-list: Restrict "files" predicate to file-visiting buffers
- 7045a02b8 org-switchb: Exclude temporary buffers with names starting with space
- 25859098c Fix various typos in docs and code
- 4b36f18e2 org-deadline, org-schedule: Only remove DEADLINE/SCHEDULED from planning
- 24bef6062 ob-session-async-R-value-drawer: Alter the test after breaking change
- d18440c28 ob-session-async-R-value-drawer: Alter the test after breaking change
- 4a8e19faa org-forward-paragraph: Fix edge case
- 7e4966bb6 doc/org-manual.org (Macro Replacement): Clarify that macros cannot be nested
- 5f60453ec Testing: Fix test failures under other locales
- ad6b560a6 Merge branch 'bugfix'
- ea9d0453d list/org-feed.el: Fix typo in docstring
- 83bf5b965 Merge branch 'bugfix'
- 9376db894 org-manual: Add remark to :booktabs
- 23453dbf2 Ensure checkboxes are fontified in description list items
- f16038a03 lisp/org.el: Fix `org-display-outline-path' double separator
- 4a6caf2b5 ox-html: Fix broken links to klipse
- 31aa1ccc0 ox-html: Fix W3C validation for html4-strict doctype
- 0aa32bc3e ; org-colview: Adjust section nesting
- 647e8349a ; org-colview: Move `org-columns-summary-type-defaults' earlier.
- 487180e33 ; org-colview: Drop redundant agenda comment
- f7cd9977c Merge branch 'main' of ssh://git.savannah.gnu.org/srv/git/org-mode
- d524fb8d9 test-ox-beamer: Align test names with other exporters
- e293a2692 lisp/org-clock.el: Rewrite 'org-clock-timestamps-change'
- b07138cb5 org-element: Add match groups to `org-element-clock-line-re'
- 4df35652a ox-html: Fix export of descriptive items in non-descriptive lists
- ab037afb5 org-colview: Remove hard-coded summary depth limit
- cf4bb175f Merge branch 'bugfix'
- 337b26424 doc/org-guide.org, doc/org-manual.org: Fix multiple typos
- 0a7387385 ob-core: Obsolete `org-babel-remote-temporary-directory'
- 8cdb91e79 Testing: TRAMP integration: Use local PATH and tmpdir
- 76dc6e358 org-clock: Fix pipe char (|) in headings breaks clockreport
- a8e1aefa9 ; org-colview: Group summary value handling
- fb3e66a3d ; org-colview: Mark summary depth test as characterization
- 528b5baa6 Testing: New test 'test-org/org-timestamp-change/bad'
- d9dd63138 org.el: Fix 'org-timestamp-change' for timeranges
- c9523fd08 org-parse-time-string: Fix Emacs 28 test failures
- 51400979b ; org-colview: Clarify current summary level
- e52158819 ; org-colview: Skip tree walk for non-summarizable specs
- 5eca0604c ; org-colview: Simplify subtree-end lookup in compute-spec
- 8a13d1fef org-colview: Document summary depth limit
- 47327628c ; org: Move marker helper to org-macs
- d4355998e org-colview: Fix summary of inline tasks at the deepest level
- 2277d10ac Revert "lisp/ox.el: Added option to export file links verbatim"
- dcb8f00ec Update version number for the 9.8.6 release
- f19e41db3 ; org-colview: Remove no-op fallback cond clause
- 03bb8b2bf org-colview: Skip clock summaries when recalculation is inhibited
- d4ea3b33b ; org-colview: Call commands directly in `org-columns-redo'
- 0d77abc7f ; org-colview: Add `org-columns--ensure-and-move-marker'
- c0f3cc181 lisp/ox.el: Added option to export file links verbatim
- b470d813d ; org-colview: Modify column specifications with setf
- 7405c18af ; org-colview: Use spec accessor to detect checkbox operators
- 4707ac34f ; org-colview: Add `org-columns--spec-at-point'
- a7174bc30 ; org-colview: Handle face lists in line colors
- 4dc39c7eb org-agenda-timerange-leaders: Allow functions as element values
- 95a3c1e35 doc/org-manual.org: Mention that make repro may degrade performance
- deabba213 ; org-colview: Reuse entry to improve collection performance
- 7a92e19ed mk: Echo main compilation states when running make
- 61a4a59dc Makefile: Remove default suffix rules
- 2267e5c01 mk/targets.mk: Make githooks target more robust
- 3f9442e28 lisp/Makefile: Remove reference to long deleted file
- 0843a4d93 doc/Makefile: Replace empty pattern match
- a68548490 Merge branch 'bugfix'
- 0fe888b54 ; org-insert-structure-template: Don't include parts outside of region
- 1930a1507 Merge branch 'bugfix'
- 7c4f1978a test-ob/org-babel-examplify-region-block: Fix on Emacs 28
- 30b4f406f org.el: add save-excursion to hooks which should never move point
- c4e24c668 fixup! 3cc0c3269
- 990e8be1e fixup! 3cc0c3269
- 7d8755a98 Merge branch 'bugfix'
- 118e7cbcd ; org-babel-examplify-region: Correct insertion of blocks
- 72e35e467 Merge branch 'bugfix'
- 3cc0c3269 Fix LaTeX babel language detection regex
- d3f9ff884 org.el: fix `org-sort-entries' bug when heading at point not visible
- 7a134e7b2 org-element-cache: Add a debugging toggle to diagnose potential bug
- 3ea168273 Generate images in TOC for HTML export
- 4fc79630d org-element-babel-call-parser: Fix calls without ()
- 206f6b8f5 org-element--cache-log-message: Fix edge case
- c91aa22ff org-element-cache: Add more diagnostics checks for lists
- 03d17dbf7 ; org-colview: Cache column in column update
- ae865a359 ; org-colview: Clarify decompiled column operator
- 901b06139 ; org-colview: Extract display environment helpers
- 8634b8d22 ; org-colview: Simplify format and top-level helper
- 596f9abce ; org-colview: Sort special column edit actions
- bf6145354 ; org-colview: Clarify cell format construction
- d794a2307 ; org-colview: Clarify summary operator docstrings
- 08d115bac ; org-colview: Rename summary function helper
- b7f84a769 ; org-colview: Rename summary collect helper
- 75a136161 ; org-colview: Extract summary type lookup
- 5b985212e ; org-colview: Clarify property update predicate
- ff5ac40c8 ; org-colview: Simplify property spec update handling
- 7afdef7f3 ; org-colview: Clarify repeated property spec comment
- 3c8809b3d ; org-colview: Remove redundant row preparation time binding
- b1f8488fb ; org-colview: Extract level accumulator clearing
- 096818b00 ; org-colview: Test hierarchical custom summaries
- 7b2d3d400 ; org-colview: Simplify summary value collection
- eca2fb280 ; org-colview: Clarify current summary value
- 2da2bf2be ; org-colview: Extract summarizable operator check
- 9dec54dd2 ; org-colview: Clarify nonempty value predicate
- 03fbbd498 org-colview: Stop consulting undefined inline task max level
- b04348b47 ; org-colview: Clarify column summary variables
- a83cdeb78 ; org-colview: Extract summary property update
- d4a7b8350 ; org-colview: Clarify compute update flag
- dc8a7c500 ; org-colview: Reuse command action for ITEM edits
- 9839e26a9 ; org-colview: Deduplicate column edit command actions
- ea546ccb4 ; org-colview: Clarify overlay text docstring
- 036918671 ; org-colview: Reformat overlay text cases
- 1e6cedd8a ; org-colview: Extract tag text propertizing
- 893eb23c9 ; org-colview: Clarify overlay text local names
- 03caea21f ; org: Extract log note format regexp helper
- 1cc756900 ; org-colview: Simplify summary type completion
- fc0248d4e ; org-colview: Extract column width update
- 50a18e782 ; org-colview: Extract summary text property update
- 031f9da0d ; org-colview: Extract `org-columns--update-agenda-single-file'
- c2dc7a759 Merge branch 'bugfix'
- 0d02d7262 Update version number for the 9.8.5 release
- 22a724bb4 etc/ORG-NEWS: Announce about internal changes in org-colview
- a7f36181e ; org-colview: Test columnview dblock filtering
- adfb59f71 org-colview: Clear column markers without overlays
- ab4ae779f ; org-colview: Extract column row preparation
- 1743cb735 ; org-colview: Extract column row display
- 5cce673dd ; org-colview: Clarify repeated property summary computation
- fe6846def ; org-colview: Extract clock summary computation
- 53cec9e86 ; org-colview: Extract column row collection
- 11377d513 ; org-colview: Rename collected column data to rows
- ed71f2a0d ; org-colview: Remove redundant format initialization comment
- ece903d7c ; org-colview: Clarify overlay cell format names
- aacfdae51 ; org-colview: Clarify column format names
- d07b95f39 ; org-colview: Clarify format string names
- 5fcef6eb7 ; org-colview: Clarify collected value terminology in docstring
- 8cc7a7f91 ; org-colview: Clarify compiled format names
- 5de30ee68 ; org-colview: Clarify update overlay local names
- c750e0916 ; org-colview: Clarify header line local names
- 98de6ecca ; org-colview: Rename cell format string helper
- ea15ac8d6 ; org-colview: Clarify overlay value names
- c1e31b8b1 ; org-colview: Clarify cell format string names
- e3421b5ea ; org-colview: Rename line display helpers
- 02369d513 org-timer: Fix handling 3 universal args in `org-timer-set-timer'
- 5261e65d5 ; org-agenda: Fix errors in Emacs 28
- c2880e7e5 org-clock: Widen buffer when looking for clock in `org-clock-cancel'
- 1f5bba867 ; org-colview: Use singular column view terminology
- 62dc0d50d Fix typo in `org-assert-version' docs
- d34e34f3e org-colview: Treat empty COLUMNS value as unset
- 5f8fe990e ; ox-icalendar: Avoid overflow with singleton priority ranges
- 7be55af49 org-colview: Test COLUMNS keyword storage
- c0db350c3 org-colview: Extract `org-columns--insert-columns-keyword'
- 55cad367e org-colview: Extract `org-columns--replace-columns-keyword'
- 4d6dafdb6 Merge branch 'bugfix'
- 2c59745b8 org-agenda-redo: fix error on empty agenda
- 7c3c4cecc org-agenda-clock-goto: Jump to closest entry and respect filtering
- ff9df1430 Testing: Require org-test file in our test files
- a9bde9133 .dir-locals.el: Set `elisp-flymake-byte-compile-load-path' for tests
- a9109f6b3 Testing: Use the same default ERT test selector everywhere
- 7f2b89281 Testing: Conditionally skip some tests
- 70d41a57b org-colview: Declare default summary types before use
- 37408d051 org-colview: Simplify column overlay face construction
- a0089503e org-colview: Use cl-defstruct for column specs
- 70deed3c4 org-colview: Rename section to "Removing overlays / quitting"
- b27d9d3ec org-colview: Move `org-columns-summary-types-default' to Summary operators
- b6f415284 org-colview.el: Use `org-collect-keywords' instead of custom code
- 9e83e4254 org-colview.el: Extract `org-columns--get-columns-keyword'
- 245025b43 org-colview: Add column spec accessors
- 1c9a9aed9 org-colview: Improve `org-columns-compile-format' docstring
- 0eacab318 org-colview: Group related column view helpers
- 33ce75a72 org-colview: Organize code with section headings
- ba08383c7 Merge branch 'km/from-emacs-31' into bugfix
- 92aeb1d75 Backport commit ce3098752 from Emacs
- b55b15946 org-colview: Use canonical 'when' and 'unless' macros
- 7e8fdc2ff org-colview.el: Extract `org-columns--move-cursor` helper
- e989c60c2 fixup! org-colview: Move point advance out of cell overlay maker
- 13dd4b8dc org-colview: Move point advance out of cell overlay maker
- 3f7d1ed20 org-colview: Rename column display helpers
- fb411c67a org-colview: Reorder display functions for logical grouping
- d64357b81 org-colview: Don't show raw text properties in read-only error
- fb9ebffa9 org-colview.el: Extract `truncate-lines' suspend/resume helpers
- 004e0828b ox-latex.el: New variable org-latex-descriptive-environment
- 732610714 ob-tangle: Make tangling from archived trees optional
- d7e707103 org-colview.el: Skip org-columns-redo for non-special properties
- 6c5d87e6e Fix paragraph centering in ODT export
- f06c61e41 org-colview.el: Increase performance of column width computation
- fc61cbbcf Fix paragraph centering in ODT export
- b1d2093ed org-colview.el: Rename 'name' to 'title' in org-columns--set-widths
- f919c99fd org-colview.el: Use string-width instead of length for column widths
- 99a0ebdfb org-colview.el: Document CACHE parameter of org-columns--set-widths
- 296b1b034 test-org-colview.el: Add tests for org-columns--set-widths
- ea602e22a org-colview.el: Drop redundant face-remap checks and localize variable
- d2d70cf0d org-colview.el: Extract `org-columns--hide-rest-of-line'
- 313beb3b7 org-colview.el: Extract `org-columns--display-columns'
- e35ab4f6e org-colview.el: Extract `org-columns--remap-header-line'
- a4c2d2ce8 org-colview.el: Extract `org-columns--mark-line-read-only'
- 71f693e77 org-colview.el: Extract `org-columns--display-here-column'
- ae0118a21 org-colview.el: Extract `org-columns--display-here-face'
- b880799fc org-colview.el: Note AGENDA-MARKER's effect on ITEM
- 968d108c6 org-colview.el: Read duration once in `org-columns--agenda-effort-fallback'
- 8612073a4 org-colview.el: Bind iterated format to a local
- dd7b462ef org-colview.el: Stop overloading compiled-fmt as agenda flag
- 557c7ce09 org-colview.el: Drop redundant compiled-fmt parameter from agenda-effort-fallback
- fa70b19a0 org-colview.el: Shorten COMPILED-FMT comment
- 313fbca1a org-colview.el: Clean up `org-columns--collect-values' docstring
- 2774cc798 org-colview.el: Rename local v to value
- 847923de5 org-colview.el: Rename parameter p to property
- d749d99fb org-colview.el: Rename local p to property
- 47aed1d61 org-colview.el: Replace single-pattern pcase with let*
- a23f35c8d org-colview.el: Extract `org-columns--agenda-effort-fallback'
- 334bdb084 org-colview.el: Fix inconsistent formatting of the currency "$" column summary
- 0c0970ec7 org-colview.el: Extract function `org-columns--execute-and-update'
- 04ad0298a test-org-colview: Add coverage for `org-columns-edit-value'
- 4ff8dbb22 org-colview.el: Update `org-columns-compile-format' docstring
- 2ac7666fb org-colview.el: Rename inner `operator-fmt' to `format-string'
- 2fb9827ba org-colview.el: Replace `while' + `push'/`nreverse' with `cl-loop'
- 779d5d601 org-colview.el: Replace manual operator/format split with `seq-let'
- 4d6965a1f org-colview.el: Invert `if' in `org-columns-compile-format'
- 3371505bf org-colview.el: Rename inner `fmt' to `operator-fmt'
- 8497d4897 Merge branch 'bugfix'
- c9591be7d Merge branch 'bugfix'
- 7f2862d30 lisp/org-colview.el: Fix misleading docstring of `org-columns--display-here-title'
- 2b77c33a3 Merge branch 'km/from-emacs-master' into bugfix
- 224b957ae Backport commit 3c6c3f5a6 from Emacs
- f3ab15a71 lisp/org-colview.el: Refactor column overlay formatting
- dfa8f54d4 org-colview: Extract function `org-columns--pad-line-for-overlays'
- 33dbbaeb9 lisp/org-colview.el: Refactor conflicting minor modes management
- 143803bd5 testing/lisp/test-org-colview.el: Pin behavior of conflicting minor modes
- 5015c351d org-colview.el: Remove redundant code in `org-columns-quit'
- c6be9eb39 test-org-colview: Add coverage for org-columns-quit teardown
- cb20decc5 lisp/org-colview.el: Add Slawomir Grochowski as the maintainer
- 931bedb79 testing/lisp/test-ob-R.el (ob-session-async-R-value-drawer): Wait for more
- a4dd1d5bd Merge branch 'km/from-emacs-master' into bugfix
- a6725aeab Backport commit 4fcc8a473 from Emacs
- 6f1e91af2 Update ox-latex.el maintainer
- 2f6959386 Update version number for the 9.8.4 release
- 95d6f2199 org-id-update-id-locations: Prefer using file buffer in Emacs over file
- 7522e2850 Update ox-latex.el maintainer
- 068b65c75 Merge branch 'bugfix'
- 6d30f2bec lisp/ox-koma-letter.el: Remove Marco Wahl from maintainer line
- ee635fc85 Merge branch 'bugfix'
- 0aec56fcd org-table-recalculate: Fix modifications outside combine-change-calls range
- 367df3154 Add safe predicate to custom variables in ox-beamer
- 2bc1124e6 ox-latex.el: Fix bug which deleted class options
- 6e7a122a7 lisp/org.el: Error when timestamp shift hits DST gap
- 0f5bcc5b9 Declare intended major modes for all the Org commands
- 87792e66b Remove interactive spec from non-commands
- 5d711684f Update version number for the 9.8.3 release
- 30058ad76 Declare some latex package list variables safe
- d483e6065 lisp/ob-tangle.el: fix offset of jump
- eb102873c lisp/ob-tangle.el: fix offset of jump
- e0bc51db6 ox-icalendar: Add export blocks and keywords
- 9b3b5d098 testing/lisp/test-ob-tangle.el: Add comments
- ff3cd56d3 ox-html.el: support line numbers as a :before element in code blocks
- 2ae1e5802 doc/org-manual.org (Citation handling): Move list of processors up
- 730c780c5 org-habit-insert-consistency-graphs: Remove more properties when aligning columns
- faa3e05da org-habit: Fix spaces before habit graph inheriting link face
- 553d73955 Merge branch 'bugfix'
- aa3ef4c17 ox-html: Fix error when there is no :output-file during the export
- 2ef390bfa Update version number for the 9.8.2 release
- 77f0ff132 org-support-shift-select: Allow forcing shift-select-mode on timestamps
- 15f35712b org-manual: Document citation styles and locators
- 61455f835 Merge branch 'bugfix'
- aa70cc470 org-element: Fix not allowing dashes in dynamic block names
- 8ef185576 Merge branch 'bugfix'
- b65f875cb ox-texinfo: Prevent @comma inside @node
- 6db7124a5 ob-tangle.el: Fix incorrect trimming behavior on tangled blocks
- 56768d33b ob-core.el: Fix incorrect interposition of Noweb prefixes
- 3c78e2c53 org-html-fontify-code: Work around htmlize force-fontifying buffer
- ba8e112cc ob-plantuml.el: Fix body parsing to handle @start/@end keywords
- 62cad0194 ol: Allow targets as search option in id: links
- c1bb5ae59 Update version number for the 9.8.1 release
- 8c24e4bb5 org-babel: Fix and extend `org-babel-update-intermediate' variable
- 40ae6286e org-element: Move cache log message to more logical place
- 5fb78efda org-element-at-point: Add a fix for imaginary bug with buffer not being preserved
- 54cdae713 org-element-cache-reset: Log when resetting cache
- 972d5c2ba Query before attempting to overwrite existing attachment/DnD/yank file
- 6994fb7fd Merge branch 'bugfix'
- 09c585c60 org-fold-core: Update commentary for renamed function
- 13e035072 org-fold-core: Rename another function for American spelling
- b9ba12c2c Make sure to use `org-babel-process-file-name' in babel backends
- f57de9dac Merge branch 'bugfix'
- 849f24982 Fix incorrect values of :safe t and :safe nil
- c1b424711 etc/ORG-NEWS: Remove announcement about bugfix
- bf0df4898 Fixed org-babel-execute:awk to work on remote machines
- e01282323 etc/ORG-NEWS: Fix typo
- be7ad3124 ob-csharp: Fix a defcustom type
- 7ebccd950 lisp/ox-latex.el: Add safe declaration
- d26b3b7db ox-latex: Make brackets around LATEX_CLASS_OPTIONS optional
- 9c548504b test-org-clock/org-clock-timestamps-change: Add more test coverage
- 4f1bdf83e org-clock-timestamps-change: check location in timestamp
- 42e7e3356 test-org/org-timestamp-change: Test minute rounding
- 735d189d2 test-org/org-timestamp-change: Relax test requirements
- d6baddca2 org-timestamp-change: Fix time addition
- c9e26e8fa org-timestamp-change: Use proper time structure accessor functions
- fbef389fd Add a compatibility shim for the function decoded-time-add
- b9bd49bcf org-parse-time-string: Use `make-decoded-time'
- 4edd65c39 Use decoded-time accessor functions
- 15c1ca119 Use calendar accessor functions
- 645e4d4a6 Merge branch 'bugfix'
- 1cbeb7f85 lisp/org.el: Address compiler warnings from Emacs master
- 846e46fd6 org-latex-item: Fix description lists without description paragraph
- 7ed519bfa Testing: Fix many checkdoc warnings
- 22fab8589 Testing: Fix compile warnings
- 3609a72ef Testing: Add missing defvar's and declare-function's
- 6f37f685f Testing: Add missing requires
- e546047ce ob-scheme: Fix handling :colnames argument

* php-mode (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/php-mode
:BRANCH: master
:END:
** Remote-only commits
- e175393 Merge pull request #810 from emacs-php/dependabot/github_actions/actions/checkout-7
- 4087f27 Bump actions/checkout from 6 to 7
- a6a7941 Merge pull request #808 from emacs-php/dependabot/github_actions/actions/checkout-6
- 70c7144 Update AUTHORS.md
- 27e70f8 Bump actions/checkout from 5 to 6

* plantuml-mode (develop)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/plantuml-mode
:BRANCH: develop
:END:
** Remote-only commits
- a4a63ef 📏 Adjust indentation when break appears within an if condition
- 02d7211 🐛 Fix Activity Diagram indentation when using `endfork`
- 7a93f7c 🌉 More solid foundation for regexes
- 7d5186b 📦 Prepare for release
- 0b1c946 ⚙️ WIP using helpers for common regex sequences
- 8ef4b29 ⚙️ WIP using helpers for common regex sequences
- 3cdb2bb refactor: lift indent regex defvars to top level
- f375cc9 refactor: translate font-lock regexes to rx syntax
- 8adb6a8 refactor: translate indent regexes to rx syntax

* polymode (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/polymode
:BRANCH: master
:END:
** Remote-only commits
- 8cb72fa Merge pull request #367 from polymode/revert-359-master
- b8c5458 Revert "Use define-keymap instead of obsolete easy-mmode-define-keymap"

* popup-el (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/popup-el
:BRANCH: master
:END:
** Remote-only commits
- 3afe431 build(deps): bump actions/checkout from 6 to 7 (#149)

* projectile (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/projectile
:BRANCH: master
:END:
** Changelog diff (=CHANGELOG.md=)
#+BEGIN_SRC diff
diff --git a/CHANGELOG.md b/CHANGELOG.md
index 654862e..33722fe 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -4,12 +4,209 @@

 ### New features

+* Add reviewable read-only search commands, a search-only sibling of the reviewable replace UI.
+  * `projectile-search-review` (`s R`) searches for a literal string; `projectile-search-regexp-review` (`s X`) searches for an Emacs regexp, honoring full Emacs regexp syntax.
+  * Matches are gathered into a read-only `*projectile-search*` buffer, grouped by file, one `LINE:COL: CONTEXT` line per match with the matched span highlighted; there is no preview, no per-match toggle and no apply.
+  * The buffer reuses the replace reviewer's navigation, case/regexp toggles (`c`/`x`), line and file filters (`k`/`d`/`K`/`D`), re-search (`g`) and grep-mode export (`e`).
+  * `r` bridges the current search to the replace reviewer, carrying over the term, literal-ness and case setting and prompting only for the replacement.
+  * A literal `projectile-search-review` accelerates its scan with ripgrep when `rg` is installed, streaming matches in near-instantly on large projects.
+    * Controlled by `projectile-search-use-ripgrep` (default on); set it to nil to always use the pure-elisp scan.
+    * The ripgrep fast-path follows ripgrep's ignore rules plus Projectile's ignore globs, which can differ slightly from the elisp path's file set (e.g. hidden files, symlinks); regexp search and the whole replace reviewer always use the portable elisp scan.
+  * The commands are available from `projectile-dispatch` and the Projectile menu.
+* [#1924](https://github.com/bbatsov/projectile/issues/1924): Add reviewable project-wide replace commands that let you preview matches and choose which to apply, instead of the blocking, file-by-file `query-replace` walk of `projectile-replace`.
+  * `projectile-replace-review` (`R`) does a literal replace; `projectile-replace-regexp-review` does an Emacs-regexp replace whose replacement can reference capture groups.
+  * Matches are gathered in Emacs Lisp, so the regexp command honors full Emacs regexp syntax and the preview reflects exactly what will be edited, including unsaved changes in open buffers.
+  * The `*projectile-replace*` results buffer shows a per-file, per-match preview where each match can be toggled on or off; `!` (or `C-c C-c`) applies just the enabled ones, in any order.
+  * Applying edits each file from the bottom up, edits open buffers in place under a single undo step, writes closed files back preserving their coding system, and skips buffers modified since the search rather than corrupting them.
+  * The results buffer can reshape the search in place, each action re-scanning and re-rendering the previews:
+    * `c` toggles case sensitivity (seeded from `case-fold-search`).
+    * `x` toggles between literal and Emacs-regexp matching, refusing an invalid regexp rather than erroring.
+    * `k`/`d` keep or flush the matches whose line matches a regexp; `K`/`D` do the same by file name; re-searching with `g` restores anything filtered away.
+  * A status line at the top shows the term, replacement, match and file counts, the mode flags, and a note when the list has been filtered.
+  * `e` exports the shown matches to a `grep-mode` buffer so wgrep or Emacs 31's `grep-edit-mode` can turn them editable and write back; wgrep stays an optional integration and `!` remains the no-dependency apply path.
+  * Both reviewers scan the project asynchronously, so a large search no longer freezes Emacs.
+    * The results buffer opens right away, matches stream in as they're found (the header shows a "Searching..." progress note), and the scan is cancelable with `q`, `C-g`, or by killing the buffer.
+    * While a scan is still running, `!` (apply), `e` (export) and the filter keys (`k`/`d`/`K`/`D`) refuse until it finishes, so the write-back never runs against a partial match set and a filter can't be outrun by a later chunk; starting a new scan (`g`/`c`/`x`) cancels any in-flight one.
+    * Set `projectile-replace-async` to nil to force the old synchronous single-pass scan; batch runs always scan synchronously.
+  * The commands are available from `projectile-dispatch` and the Projectile menu, and the match cap is customizable via `projectile-replace-max-matches`.
+* Add `projectile-session-mode`, a global minor mode that gives each project its own `tab-bar` tab.
+  * Switching to a project selects its existing tab (restoring that project's window layout) when one is open, or otherwise opens a fresh tab named after the project and populated via `projectile-session-default-action`.
+  * Same-named checkouts get distinct tab names (e.g. `work/foo` and `home/foo`); customize the scheme with `projectile-session-tab-name-function`.
+  * `projectile-session-switch-to-buffer` completes over just the current tab's project buffers.
+  * Sessions persist across Emacs restarts: `projectile-session-save`, `projectile-session-restore` and `projectile-session-forget` write a project's window layout and buffers to a file under `projectile-session-directory` and read it back; `projectile-session-save-all` saves every open project at once.
+  * Switching to a project with a saved session restores it (gated by `projectile-session-restore-on-switch`); `projectile-session-autosave` saves sessions on switch-away and on exit.
+  * `projectile-session-restore-all` reopens every saved project's session, each into its own tab (skipping projects already open and dropping stale sessions whose files are gone); set `projectile-session-restore-on-startup` to run it automatically at Emacs startup.
+  * Which buffers are recorded, and how, is extensible via `projectile-session-buffer-serializers`; file-visiting and `dired-mode` buffers work out of the box.
+  * The session commands are bound under the `w` sub-prefix of the command map (`s-p w s`, `s-p w S`, `s-p w r`, `s-p w R`, `s-p w f`, `s-p w b`), and are also available from `projectile-dispatch` and the Projectile menu.
+
+### Changes
+
+* Add `projectile-frecency-max-projects` (default 100), which caps how many projects' frecency history is kept so the store can't grow without bound.
+* `projectile-replace-scan-chunk-size` is now a user-facing `defcustom` (was an internal variable).
+
+## 3.1.0 (2026-07-04)
+
+### New features
+
+* Add declarative "file kinds", letting a project type describe categories of files (e.g. Rails models, controllers and views) via the new `:file-kinds` keyword of `projectile-register-project-type`.
+  * `projectile-find-file-of-kind` (`j`) prompts for a kind and completes over just the project files of that kind.
+  * `projectile-toggle-related-file` (`J`) generalizes `projectile-toggle-between-implementation-and-test`: it jumps to related files of other kinds, jumping straight when there's one, prompting when there are several, and cycling through them on repeated presses.
+  * The `rails-test`, `rails-rspec` and `django` project types ship with ready-made `:file-kinds` tables. Related files are keyed by their namespaced path, so a top-level `UsersController` relates to the top-level `User` model rather than an `Admin::User`.
+* `projectile-find-file` and `projectile-find-file-dwim` now rank the files you work with first, ordering completion candidates by how often and how recently you've visited them (with decay).
+  * The ranking is applied through completion metadata, so it works with any completion UI and under every indexing method, including `alien` (which `projectile-sort-order` never reached).
+  * Controlled by `projectile-enable-frecency` (default on); the per-project history is persisted in `projectile-frecency-file` and capped by `projectile-frecency-max-files`.
+* [#1992](https://github.com/bbatsov/projectile/issues/1992), [#1587](https://github.com/bbatsov/projectile/issues/1587), [#1553](https://github.com/bbatsov/projectile/issues/1553), [#1794](https://github.com/bbatsov/projectile/issues/1794): Add named project tasks - shell commands (or functions returning them) that you can run by name.
+  * `projectile-tasks` maps task names to commands and can be set globally, per project type via the new `:tasks` keyword of `projectile-register-project-type`, or per project via `.dir-locals.el`.
+  * `projectile-run-task` (`c x`) picks a task with completion and runs it like the lifecycle commands, in a per-task compilation buffer; a prefix argument lets you edit the command first (e.g. to pass ad-hoc arguments).
+  * `projectile-repeat-last-task` (`c X`) re-runs the project's last task.
+* [#978](https://github.com/bbatsov/projectile/issues/978): Add `projectile-project-changed-functions`, run whenever the current project changes - including implicitly via visiting a file or directory of another project - with the new and previous project root as arguments.
+* [#1442](https://github.com/bbatsov/projectile/pull/1442): `projectile-sort-order` can now be set to a function that receives the list of project files and returns them in the desired order.
+* [#1984](https://github.com/bbatsov/projectile/pull/1984): The VCS markers are now customizable via `projectile-vcs-markers`, whose order breaks ties between markers in the same directory - so colocated `jj`+`git` repositories can be detected as `jj` by moving `.jj` first.
+* [#1890](https://github.com/bbatsov/projectile/pull/1890): Recognize osc (openSUSE Build Service) checkouts: `.osc` is now a VCS marker, a top-down-recurring root marker, and globally ignored; file listing uses the generic indexing command.
+* [#1694](https://github.com/bbatsov/projectile/issues/1694): Add `projectile-invalidate-cache-all`, which invalidates the caches of all known projects at once (handy when commands like `projectile-find-file-in-known-projects` serve stale results).
+* [#1075](https://github.com/bbatsov/projectile/issues/1075): Add experimental opt-in automatic cache updates via filesystem notifications, so files created, deleted or renamed outside Emacs update the cache without a manual `projectile-invalidate-cache`.
+  * Enable it with `projectile-auto-update-cache-with-watches`; only local, cached projects are watched.
+  * Each project uses one `file-notify` watch per directory, bounded by `projectile-watch-directory-limit`.
+* Add `projectile-other-window-command` (`s-p 4 4`) and `projectile-other-frame-command` (`s-p 5 5`), modeled after Emacs's `other-window-prefix`/`other-frame-prefix` (`C-x 4 4`/`C-x 5 5`): they display the buffer of the next command in another window or frame, keeping the Projectile keymap active for the next key.
+  * This works with any command, including ones without a dedicated `-other-window`/`-other-frame` variant, e.g. `s-p 4 4 x s` starts a project shell in another window.
+* Add `projectile-run-test-at-point` (bound to `c .`), which runs just the test around point, located via the buffer's tree-sitter parse tree (requires Emacs 29+ with tree-sitter).
+  * Rules for pytest (`python-ts-mode`), `go test` (`go-ts-mode`) and jest (`js-ts-mode`/`typescript-ts-mode`/`tsx-ts-mode`) are built in; other languages can be added via `projectile-test-at-point-rules`.
+
+### Changes
+
+* Project root detection and project-type detection now probe marker files with a single directory listing per directory level instead of one file stat per marker, collapsing dozens of sequential round-trips over TRAMP into one.
+  * Marker matching is exact-case as a result, even on case-insensitive filesystems. This corrected the `gnumake` type's marker to GNU make's actual `GNUmakefile` spelling, and the `make` type now recognizes a lowercase `makefile` too.
+* `projectile-auto-discover` now defaults to `t`, so setting `projectile-project-search-path` is enough to have those projects discovered (no change for anyone without a search path).
+  * The scan now runs once per session on the first project switch, rather than on every switch, and remote (TRAMP) search-path entries are skipped.
+* [#1771](https://github.com/bbatsov/projectile/issues/1771), [#740](https://github.com/bbatsov/projectile/issues/740): Hybrid indexing now applies the dirconfig glob patterns (`-`/`!` entries without a leading slash); previously they were silently ignored under `hybrid` and only `/`-prefixed path entries took effect.
+* [#1941](https://github.com/bbatsov/projectile/issues/1941): Dirconfig glob patterns now follow `.gitignore`-like rules, identical under `native` and `hybrid` indexing (previously `native` used loose string suffixes, so `-build` also ignored `mybuild`, and expanded globs per directory level, so matching differed from level to level):
+  * a slashless pattern matches the file name or any directory segment at any depth;
+  * a pattern containing a slash is anchored at the project root (prefix with `**/` to match anywhere);
+  * a trailing slash matches directories only, and a matched directory covers its subtree;
+  * `*` stops at `/` while `**` crosses it.
+* Remove `projectile-check-pattern-p` and `projectile-ignored-rel-p`, the old pattern matchers superseded by the compiled dirconfig matcher (nothing referenced them anymore).
+* `projectile-verify-file` now goes through `projectile-file-exists-p`, so cold project-type detection benefits from the remote file-exists cache instead of issuing a TRAMP round-trip for every marker file probed.
+* The mode-line updater is only added to `window-configuration-change-hook` when `projectile-dynamic-mode-line` is enabled; change the option via Customize or `setopt` for it to apply immediately.
+* The `recentf` and `recently-active` sort orders no longer rescan the full project file list once per recent file, making them usable on very large projects.
+* [#1953](https://github.com/bbatsov/projectile/issues/1953): Cache the git submodule listing instead of shelling out to `git submodule foreach` on every alien/hybrid file listing; the cache invalidates automatically when `.gitmodules` changes and is also cleared by `projectile-invalidate-cache`.
+* User-facing conditions (no project found, missing optional package, nothing to toggle to, etc.) now signal `user-error` instead of `error`, so they no longer trigger the debugger under `debug-on-error`.
+
+### Bugs fixed
+
+* [#1115](https://github.com/bbatsov/projectile/issues/1115): `projectile-replace` no longer skips replacements (or reports "All files processed" without replacing anything) when the project root is in abbreviated `~/...` form, or when a match's case differs from a lower-case input.
+* [#1677](https://github.com/bbatsov/projectile/issues/1677): `projectile-replace` and `projectile-replace-regexp` now scan buffers that already visit a project file from the beginning, so matches before point in those buffers are no longer missed.
+* [#1849](https://github.com/bbatsov/projectile/issues/1849): `projectile-skel-dir-locals` now exits its variable-entry loop on an empty variable name, keeping the entries made so far, instead of trapping the user in the prompt and discarding input on `C-g`.
+* CMake preset files referenced via `include` are now resolved relative to the file that includes them; previously the top-level `CMakePresets.json`'s includes resolved against `default-directory`, silently dropping the included presets when the current buffer was outside the project root.
+* [#1600](https://github.com/bbatsov/projectile/issues/1600): The default git submodule listing no longer depends on a Unix shell (single quotes, `tr`), fixing alien/hybrid indexing of projects with submodules on Windows.
+* Invalidating a project's cache now cancels its pending deferred cache flush; previously a flush scheduled before the invalidation could fire afterwards and recreate the just-deleted cache file with empty contents.
+* Frecency tracking now works for projects reached through a symlinked root; the visited file is resolved the same way as the project root, so its visits are no longer silently dropped.
+* `projectile-find-other-file` no longer treats the dot before a file's extension as a wildcard when matching, so `foo.el` won't match an unrelated `fooXel`-style name.
+
+## 3.0.0 (2026-07-01)
+
+### Changes
+
+* Remove the dedicated `ido`/`ivy`/`helm` completion systems (and the `auto` mode that detected `ido-mode`/`ivy-mode`/`helm-mode`). `projectile-completing-read` now uses Emacs's `completing-read` (the former `default`), which works with Vertico, Consult, Fido, `ido-completing-read+`, etc., and still tags candidates with the `project-file` category. `projectile-completion-system` now takes `default` or a custom function; the removed values degrade to `default`, so nothing breaks. `helm-projectile` and `counsel-projectile` are unaffected (they don't go through this path).
+* `projectile-find-references` now honours Projectile's ignore configuration (`.projectile` and the globally-ignored files/directories) and is scoped via the project's file set, like `projectile-grep`/`-ag`/`-ripgrep`. It previously went through the semantic-symref API, which searched the whole tree and ignored that configuration. It also now defaults the prompt from the active region (not just the symbol at point) and no longer carries a dead pre-27 display fallback. The search remains a backend-agnostic textual one; for semantic references use the built-in `xref-find-references` (which is also scoped to the Projectile project).
+* Remove the built-in tags support: `projectile-find-tag`, `projectile-regenerate-tags`, `projectile-visit-project-tags-table`, and the `projectile-tags-command`/`projectile-tags-backend` options (along with the ggtags/etags-select special casing). ctags/etags navigation has been largely superseded by `xref` and LSP (`eglot` is built in since Emacs 29). Use `xref-find-definitions` directly, or `projectile-find-references` for project-wide references. `projectile-tags-file-name` is kept, since it's still used to exclude a generated tags file from indexing.
+* Remove `projectile-browse-dirty-projects` and `projectile-vcs-dirty-state`. The implementation spun up `vc-dir` and busy-waited (`sleep-for`) up to 30 seconds per project across every known project, scraping status strings - slow, blocking, and niche. Use Magit, `vc-dir`, or a dedicated tool to find projects with uncommitted changes.
+* Remove the idle timer (`projectile-enable-idle-timer`, `projectile-idle-timer-seconds`, `projectile-idle-timer-hook`). It existed mainly to re-run `projectile-regenerate-tags` on an idle timer, which makes little sense in an LSP/xref world, and it was off by default. Use a plain `run-with-idle-timer` if you really want this behavior.
+* Remove `projectile-commander` (and `def-projectile-commander-method`), the single-key command dispatcher. It's superseded by `projectile-dispatch`, the `transient` menu added in this cycle. The `s-p m` binding and the `C-u s-p p` project-switch prefix now invoke `projectile-dispatch` instead (the prefix falls back to `projectile-switch-project-action` when `transient` isn't installed).
+* A cold `projectile-find-file` (and any command that lists project files) no longer freezes Emacs while the project is indexed under the `alien`/`hybrid` methods. The indexing command runs asynchronously and is awaited in a way that keeps Emacs responsive to redisplay and `C-g` (which aborts the indexing), instead of blocking until it finishes. The resulting file list is identical; only the responsiveness during indexing differs. Controlled by the new `projectile-async-indexing` (default on); it has no effect under `native` indexing, in batch mode, or while a keyboard macro runs, all of which index synchronously as before.
+* Speed up native indexing further: `projectile-index-directory` now reads each directory with `directory-files-and-attributes`, so an entry's type comes from the listing call instead of a `file-directory-p` stat per file. That stat was a separate filesystem round-trip each, which dominated the walk on large and remote (TRAMP) trees - it's now one round-trip per directory instead of one per file. Symlinks pointing at directories are still followed, matching the previous behaviour. Roughly 40% faster on a local 12k-file tree; the win is far larger over TRAMP.
+* Speed up native indexing's post-walk step: `projectile-dir-files-native` strips the project-root prefix with a single `substring` per file instead of `file-relative-name`, which paid for an `expand-file-name`/`abbreviate-file-name` per file. `projectile-project-files` also skips re-relativising the listing when the only directory walked is the project root itself (the common single-directory case). Together that removes roughly two seconds of overhead per 90k files indexed.
+* [#1872](https://github.com/bbatsov/projectile/issues/1872): Clarify in the `projectile-register-project-type` docstring and the manual that a list of `marker-files` must *all* be present for a type to match (logical AND), and that a predicate function should be used to match when any one of several files is present.
+* [#1935](https://github.com/bbatsov/projectile/issues/1935): The `emacs-eask` project type now has a `test` command (`eask test`), so `projectile-test-project` works out of the box for Eask projects. Eask auto-detects the test framework (buttercup, ERT, ...), so no framework-specific type is needed.
+* [#1638](https://github.com/bbatsov/projectile/issues/1638): Document in `projectile-svn-command` that it runs non-interactively and therefore needs SVN credentials to be cached up front for authenticated remotes.
+* Keep a separate command history per lifecycle command type (configure, compile, test, install, package, run), so the prompt's `M-p` history no longer mixes, say, test commands with compile commands. `projectile-repeat-last-command` still uses the combined per-project history and is unaffected.
+* Remove the unused private `projectile--init-known-projects` alias (a leftover compatibility shim for the old known-projects API; nothing in the codebase referenced it).
+* `projectile-get-immediate-sub-projects` skips the `git submodule foreach` shell-out for git projects with no `.gitmodules` file anywhere up the parent chain. Hot path for monorepos that index the project root often.
+* `projectile-discover-projects-in-directory` now uses `directory-files-no-dot-files-regexp` to skip `.` and `..` at the C level instead of doing the post-filter in Elisp - matches the indexing walker.
+* Document the anchored vs `*`-prefixed semantics of `projectile-globally-ignored-directories`, the `find` fallback's lack of common directory exclusions when `fd` isn't available, and how `fd`/`git ls-files` handle deleted-but-unstaged files differently.
+* Speed up native indexing on large trees: `projectile-index-directory` now hashes the ignored-files / ignored-directories / globally-ignored-directory-names lists once per indexing call (the per-file `member' scans were O(N*M)), expands dirconfig glob patterns once per directory level instead of once per (file, pattern) pair, and accumulates results into a shared cell so we no longer pay for an `apply append' at each recursion level.
+* `projectile-remove-ignored` (hybrid post-processing) now hashes the ignored-files basenames and pre-splits ignored-dirs into prefix-match and any-segment groups, so the per-file inner loops drop from O(M) `seq-some` walks to O(1) hash lookups (or O(segments) for `*`-prefixed entries).
+* Hybrid indexing now batches the external command into a single invocation when the project's `.projectile` declares multiple `+` keep entries, instead of shelling out once per kept subdirectory. The kept paths are passed to the indexing tool (e.g. `git ls-files`, `fd`, `find`) as positional pathspecs and submodule files outside those subdirectories are filtered out. Resolves the long-standing TODO in `projectile-project-files`.
+* `projectile-files-via-ext-command` now accepts an optional `pathspecs` argument; entries are shell-quoted before being appended to the command. `projectile-dir-files-alien` similarly accepts an optional `subdirs` argument that threads through.
+* Document the `hybrid` indexing method in the manual and add a feature matrix showing which Projectile knobs (dirconfig, global ignores/unignores, sort order, default caching) apply under `native`/`hybrid`/`alien`.
+* `projectile-dir-files-alien` now accepts an optional `vcs` argument so the dispatcher can thread through the already-resolved VCS instead of recomputing it. Existing single-argument callers are unaffected.
+* `projectile-index-directory` (native indexing) now relies on `directory-files-no-dot-files-regexp` to filter out `.` and `..` at the C level instead of walking past them in Elisp.
+* `projectile-project-root-cache` now keys entries on cons cells (`(FUNC . DIR)` for per-function results, `('none . DIR)` for the overall failure marker) instead of formatted strings. This is internal state, but third-party code that reaches into the cache directly will need to update.
+* `projectile-parse-dirconfig-file' now returns a `projectile-dirconfig' struct (with `keep', `ignore', `ensure', and `prefixless-ignore' slots) instead of a positional 3-tuple. External callers should use the accessors (`projectile-dirconfig-keep' etc.) rather than `car'/`cadr'/`caddr'.
+* Soft-deprecate prefix-less ignore entries in `.projectile'. Lines without a `+'/`-'/`!' prefix are still treated as ignore patterns for backward compatibility, but a one-time warning is now shown for each project that uses them. Set `projectile-warn-on-prefixless-dirconfig-lines' to nil to silence.
+* **[Breaking]** Remove the legacy single-letter lifecycle keybindings `C`/`K`/`L`/`P`/`u` (configure/package/install/test/run project). Use the `c` prefix instead (`c o`, `c p`, `c i`, `c t`, `c r`).
+* Remove two long-obsolete aliases: `projectile-global-mode` (use `projectile-mode`) and `projectile-project-root-files-functions` (use `projectile-project-root-functions`).
+* Pre-compute file timestamps in `projectile-sort-by-modification-time` and `projectile-sort-by-access-time` to reduce stat calls from O(n log n) to O(n).
+* Cache `projectile-parse-dirconfig-file` results per project root (invalidated by file modification time), avoiding redundant file reads during indexing.
+* Cache `file-truename` results in `projectile-project-buffer-p` when checking multiple buffers, reducing redundant symlink resolution.
+* Use a hash set for deleted file removal in `projectile-dir-files-alien`, improving performance from O(n*m) to O(n+m) when filtering deleted-but-unstaged files.
+* Avoid redundant `projectile-project-root` call in `projectile-detect-project-type` by passing through the already-resolved root.
+* Share `file-truename` cache across buffers in `projectile-open-projects`, matching the optimization already in `projectile-project-buffers`.
+* Remove unnecessary temp buffer creation in `projectile--cache-project-commands-p` (was creating a buffer and re-reading `.dir-locals.el` on every compile/test/run invocation).
+* **[Breaking]** Bump the minimum required Emacs version to 28.1 (from 26.1). This removes ~30 lines of compatibility code (fileloop fallback, `time-convert` fallback, `projectile-flatten` shim), makes `transient` (and therefore `projectile-dispatch`) an unconditional dependency, and fixes the `tags-query-replace` FIXME in `projectile-replace-regexp`.
+* Add `compat` as a dependency, enabling the use of modern Emacs APIs (e.g. `string-replace`) on older Emacs versions.
+* Replace most `cl-lib` sequence functions with `seq.el` equivalents (`seq-filter`, `seq-remove`, `seq-some`, `seq-find`, `seq-sort`, `seq-every-p`, `seq-difference`) and convert `cl-case` to `pcase`.
+* [#1971](https://github.com/bbatsov/projectile/pull/1971): Support `slnx` files for dotnet project types.
+* [#1958](https://github.com/bbatsov/projectile/issues/1958): Exclude `.projectile-cache.eld` from search results (ripgrep/ag/grep) by default.
+* [#1947](https://github.com/bbatsov/projectile/issues/1947): `projectile-project-name` should be marked as safe.
+* Set `projectile-auto-discover` to `nil` by default (to avoid startup slowdowns in some situations).
+* [#1943](https://github.com/bbatsov/projectile/pull/1943): Consider `projectile-indexing-method` to be safe as a dir-local variable if it is one of the preset values.
+* [#1936](https://github.com/bbatsov/projectile/issues/1936): Do not require selecting a project when using `M-x projectile-invalidate-cache`, since there is a global cache that is also cleared by that command, even when not operating on any specific project.
+* Add `build.mill` as an alternative project marker for the Mill project type, matching Mill's current recommended file extension.
+* Replace obsolete `when-let` and `cl-gensym` with `when-let*` and `gensym` for compatibility with Emacs 31+.
+
+### New features
+
+* [#1697](https://github.com/bbatsov/projectile/issues/1697): Add `projectile-add-and-switch-project`, which prompts for a directory, adds it to the known projects, and immediately switches to it.
+* [#1698](https://github.com/bbatsov/projectile/issues/1698): Support `%p` in project command strings (configure/compile/test/run/install/package); it is replaced with the project name when the command runs, so e.g. `:run "docker run %p"` works for both registered project types and interactively entered commands.
+* Add `projectile-run` (`s-p x r`), a single command that opens a shell, REPL or terminal in the project root over a *pluggable backend* (the same registry that powers `projectile-search`). Projectile ships `shell`, `eshell`, `ielm`, `term`, `vterm`, `eat` and `ghostel` backends; `projectile-shell-backend` selects which is used (default `eshell`). Register your own terminal with `projectile-register-shell-backend`. The dedicated commands (`projectile-run-eshell`, `projectile-run-vterm`, ..., and the `-other-window` variants) are kept as thin wrappers that force a backend.
+* Add `projectile-search` (`s-p s s`), a single search command that runs over a *pluggable backend*. Projectile ships `grep`, `ripgrep` and `ag` backends; `projectile-search-backend` selects which is used (`auto` by default, favouring ripgrep then grep). Register your own tool (deadgrep, consult-ripgrep, ...) with `projectile-register-search-backend`. `projectile-grep`, `projectile-ripgrep` and `projectile-ag` are kept as thin wrappers that force a specific backend. Note the keymap change: `s-p s s` is now `projectile-search` (it used to be `projectile-ag`), and `projectile-ag` moves to `s-p s a`. The generic registry behind this (`projectile-register-backend`) is designed to be reused for other command families later.
+* `projectile-dispatch` now exposes command modifiers as `transient` switches. A *Modifiers* group offers `--invalidate-cache` (`-i`, rebuild the file cache first, for the find file/dir commands), `--regexp` (`-r`, for `ag`/`ripgrep`), `--new-process` (`-n`, for the shells/REPLs), and `--display` (`-d`, cycle the display target through this window / other window / other frame, for the file/buffer/project commands). The `--display` switch replaces the old dedicated "Other window" and "Other frame" menu columns. (The `projectile-command-map` `4 <key>` / `5 <key>` bindings are unchanged.)
+* Add `projectile-consult.el`, an optional Consult integration distributed alongside Projectile (with a soft dependency on `consult`, so it's only loaded if you `require` it). `projectile-consult-find-file` drives a streaming Consult finder from Projectile's own indexing command (via `projectile-project-files-producer`), so candidates appear in the minibuffer as the project is indexed instead of blocking until it's done, while still honouring the project's VCS and indexing configuration. The module is self-contained so it can be split into its own package later; it requires Emacs 29.1+ (Consult's floor).
+* Add `projectile-index-project-async`, which indexes the current project in the background (via `make-process`) and populates the files cache without freezing Emacs, so a later `projectile-find-file` finds the cache already warm. Works with the `alien`/`hybrid` indexing methods (the `native` Elisp walk can't run off the main thread) and requires caching to be enabled. The underlying building blocks - `projectile-files-via-ext-command-async` and `projectile-dir-files-alien-async` - are public, so a streaming finder (e.g. one built on `consult`) can drive Projectile's indexing command asynchronously. Remote projects are handled via TRAMP. The result is identical to the synchronous indexing path.
+* Add `projectile-project-files-producer`, which returns a plist (`:directory`, `:vcs`, `:command`, `:separator`) describing how to list a project's files with its external indexing command. It gives an external asynchronous/streaming file finder (e.g. one built on `consult` or `affe`) a single, stable entry point for driving Projectile's own indexing command instead of stitching together the root, VCS and command builders by hand.
+* [#1956](https://github.com/bbatsov/projectile/issues/1956): Add `projectile-switch-project-other-window` and `projectile-switch-project-other-frame` (bound to `4 p` and `5 p`), which switch to a project and display it in another window/frame. The action they run is configurable via `projectile-switch-project-other-window-action` / `projectile-switch-project-other-frame-action` (find-file in the other window/frame by default).
+* [#1809](https://github.com/bbatsov/projectile/issues/1809): Track the project switched away from in `projectile-most-recent-project` and add `projectile-switch-to-most-recent-project` to jump back to it (repeated calls toggle between the two). Only switches made through Projectile's switch-project commands are tracked.
+* [#1861](https://github.com/bbatsov/projectile/issues/1861): Add `projectile-discard-command-cache` to drop the cached configure/compile/test/install/package/run commands for the current project, so a freshly edited `.dir-locals.el` (or project-type default) is picked up on the next run. Can be called manually or added to `after-save-hook`.
+* Add `projectile-uniquify-dirname-transform`, a project-aware value for `uniquify-dirname-transform` that disambiguates same-named buffers using the project name. Mirrors `project.el`'s `project-uniquify-dirname-transform`.
+* Add `projectile-dispatch`, a `transient` menu mirroring `projectile-command-map` for more discoverable command access. `transient` is an optional dependency (it requires Emacs 28+); the menu is only available when it's installed and is not bound to a key by default.
+* [#2008](https://github.com/bbatsov/projectile/issues/2008): Add `projectile-remove-project-type` to unregister a project type. This is the supported way to stop Projectile auto-detecting a type; clearing its `marker-files` does not work (see the related bug fix below).
+* [#1936](https://github.com/bbatsov/projectile/issues/1936): Add `projectile-discard-root-cache` command to clear `projectile-project-root-cache` without touching other Projectile caches. Useful after creating or removing a project marker, since the existing `projectile-invalidate-cache` either also drops the file list cache or prompts for a project depending on context.
+* Warn once per session when `projectile-indexing-method' is `alien' but the project has a non-empty `.projectile' file, so users notice their dirconfig rules are being bypassed. Controlled by the new `projectile-warn-when-dirconfig-is-ignored' option.
+* Warn when a `+' keep entry in `.projectile' contains glob metacharacters. The `+' prefix is for subdirectory paths only and globs are silently coerced into a non-matching directory name; the warning surfaces the misuse rather than letting it fail silently.
 * [#1964](https://github.com/bbatsov/projectile/issues/1964): Implement `project-name` and `project-buffers` methods for the `project.el` integration, so that code using `project.el` APIs returns correct results for Projectile-managed projects.
+* Implement the `project-ignores` method for the `project.el` integration, translating Projectile's global ignores, ignored file suffixes, and dirconfig (`.projectile`) ignore entries into the glob format `project.el` expects. This makes Projectile a more complete `project.el` backend for tools that rely on the protocol (e.g. `project-find-regexp`).
+* Add `projectile-forget-projects-under` to drop all known projects located under a directory (with a prefix argument it recurses into nested projects). Mirrors `project.el`'s `project-forget-projects-under`.
+* Add `projectile-forget-zombie-projects` as an alias for `projectile-cleanup-known-projects`, for discoverability and parity with `project.el`'s `project-forget-zombie-projects`.
+* `projectile-kill-buffers-filter` now also accepts a composable list of conditions (buffer-name regexps, predicates, and `major-mode`/`derived-mode`/`not`/`and`/`or` forms), modeled on `project.el`'s `project-kill-buffer-conditions`. The existing `kill-all`, `kill-only-files`, and predicate-function values keep working unchanged.
 * [#1837](https://github.com/bbatsov/projectile/issues/1837): Add `eat` project terminal commands with keybindings `x x` and `x 4 x`.
+* Add `ghostel` project terminal commands with keybindings `x G` and `x 4 G`.
 * Add keybinding `A` (in the projectile command map) and a menu entry for `projectile-add-known-project`.
+* [#1653](https://github.com/bbatsov/projectile/issues/1653): Add `projectile-compile-subproject` and `projectile-test-subproject` commands for building/testing individual modules in multi-module projects (e.g. Maven, Gradle). Bound to `c m c` and `c m t`.

 ### Bugs fixed

+* [#2005](https://github.com/bbatsov/projectile/issues/2005): Fix hybrid indexing with `fd` when the `.projectile` file has `+` keep entries. The kept directories were appended to the `fd` command as bare positional arguments, but `fd`'s grammar is `[pattern] [path...]` (so the first path was misread as the search pattern) and `fd` 9+ additionally rejects `--strip-cwd-prefix` alongside explicit paths (`error: ... '--strip-cwd-prefix' cannot be used with '[path]...'`). Projectile now passes the directories to `fd` via `--search-path` and drops `--strip-cwd-prefix` in that case. Other tools (`git ls-files`, `find`, etc.) still get plain trailing paths.
+* [#2042](https://github.com/bbatsov/projectile/issues/2042): Asynchronous indexing now runs its command under `/bin/sh` rather than the user's interactive `shell-file-name`. The async runner wraps the command as `{ ...; } 2>file`, which is POSIX-sh syntax and broke for users whose shell is `csh`/`tcsh`/`fish` (it produced bogus `{`-related output instead of a file list). The indexing command itself is a plain POSIX command, so a POSIX shell is the correct interpreter regardless of the login shell.
+* [#2046](https://github.com/bbatsov/projectile/issues/2046): Commands chosen from the `projectile-dispatch` menu now run in the project that was just selected, instead of the current buffer's project. The menu is a `transient`, so its suffix commands run after the project switch has unwound; the switched-to project is now kept current for the lifetime of the menu and restored when it exits. This applies whether the menu is reached via `C-u s-p p` or by setting `projectile-switch-project-action` to `projectile-dispatch` (or any other transient).
+* [#2037](https://github.com/bbatsov/projectile/issues/2037): `projectile-consult.el` now byte-compiles cleanly when `consult` isn't installed. Because the module ships inside the Projectile MELPA package, build systems (package.el, nixpkgs) try to compile it without `consult` present, which previously errored on its top-level `(require 'consult)`. `consult` is now loaded softly at the top (and hard-required inside the command, where it's actually needed), with the used functions forward-declared.
+* [#2042](https://github.com/bbatsov/projectile/issues/2042): A non-zero exit from the indexing command (`fd`, `git ls-files`, `find`, ...) no longer aborts `projectile-find-file` when the command still produced a file listing. External listers like `fd` routinely exit non-zero on benign conditions (e.g. an unreadable directory hit mid-traversal); that output is now used. A `user-error` is still raised when a non-zero exit produced no output at all, so a genuinely broken/missing command is still surfaced rather than mistaken for an empty project.
+* [#1663](https://github.com/bbatsov/projectile/issues/1663): Projects matched by `projectile-ignored-projects` (or `projectile-ignored-project-function`) are now excluded from `projectile-relevant-known-projects`, so they no longer show up in `projectile-switch-project` even when they were added to the known projects before being ignored. Previously the ignore list was only consulted when adding a project.
+* [#1909](https://github.com/bbatsov/projectile/issues/1909): A project type registered with a predicate `marker-files` function now receives the project root as its argument when detecting the current project's type. Previously it was passed `nil`, so such a function could never match the current project.
+* [#1829](https://github.com/bbatsov/projectile/issues/1829): `projectile-project-root` no longer errors with `(wrong-type-argument stringp nil)` when `default-directory` is nil (which can happen in some non-file buffers); it returns nil instead.
+* [#1946](https://github.com/bbatsov/projectile/issues/1946): `projectile-ripgrep` now builds its ignore exclusions as `--glob=!PATTERN` instead of `--glob '!PATTERN'`. The surrounding single quotes were only stripped by POSIX shells, so on Windows `cmd` they became part of the pattern and the exclusions silently failed.
+* [#2008](https://github.com/bbatsov/projectile/issues/2008): A project type with an empty `marker-files` list no longer matches every project. `projectile-verify-files` is vacuously true for an empty list, so a type whose markers were cleared (e.g. via `projectile-update-project-type ... :marker-files nil`) would be detected everywhere instead of nowhere. `projectile-detect-project-type` now treats an empty marker set as a non-match.
+* `projectile-discard-root-cache' and `projectile-invalidate-cache' now also clear `projectile-file-exists-cache'. Without this, after creating a new project marker (`.projectile', `.git', etc.) over TRAMP, the negative entries cached during earlier root-walks would keep reporting "not found" for up to `projectile-file-exists-remote-cache-expire' seconds even after the user explicitly invalidated the root cache.
+* `projectile-find-file-hook-function` no longer disables *all* of Projectile for remote buffers - the cheap operations (file caching, known-projects tracking, project buffer-count cap) now run regardless of remoteness, and only the genuinely slow ones (mode-line update, tags-table visit) stay gated. Previously the entire hook was a single `(unless (file-remote-p ...))', which meant remote projects never showed up in known-projects auto-tracking and never had their files cached on visit.
+* Stop stat'ing remote known projects in `projectile-keep-project-p`. The "remote and connected" branch used `file-readable-p`, which is a remote round-trip per project; with `projectile-auto-cleanup-known-projects' enabled, that turned every project switch into a serial network walk over all remote known projects. Remote projects are now always kept; users can drop dead ones with `projectile-remove-known-project'.
+* Skip `file-truename` for remote paths in `projectile-ignored-project-p` and `projectile-ignored-projects`. The known-projects tracker calls `projectile-ignored-project-p` from the find-file hook for every visit, and the previous behavior round-tripped to the remote on each call (and once per ignored entry) just to canonicalize symlinks.
+* `projectile-project-buffer-p` now skips the `file-truename` call for buffers visiting remote (TRAMP) files. Each truename was a remote round-trip and `projectile-project-buffers' iterates the full `buffer-list', so projects with N remote buffers in distinct directories were paying N round-trips on every call. Resolving symlinks across a TRAMP boundary is rarely meaningful in any case.
+* Replace the 10 sequential `file-exists-p` probes in `projectile-project-vcs` with a single `directory-files` listing, and cache the per-project result in `projectile-project-vcs-cache`. Also collapse the up-the-tree dominator walk so each ancestor directory is listed at most once instead of up to 10 times. Over TRAMP this turns "1 round-trip per VCS marker per probed directory" into "1 round-trip per probed directory", and the cache means follow-up callers (the indexing path, `projectile-project-info`, the project-cleanup loop) hit memory.
+* Cut down `projectile-project-root` round-trips on TRAMP. The cache-validity check used raw `file-exists-p` on the cached project root, which is a fresh remote stat on every call; switched to `projectile-file-exists-p` so the per-host TTL applies. Also memoize `(file-truename dir)` across the `projectile-project-root-functions' loop so cache-miss calls compute the truename once instead of once per probe (4-5 remote stats with the default function list).
+* [#1501](https://github.com/bbatsov/projectile/issues/1501), [#1275](https://github.com/bbatsov/projectile/issues/1275): Detect `fd`/`fdfind` on the remote host for TRAMP projects instead of reusing the locally-detected `projectile-fd-executable`. The locally-resolved value is meaningless on the remote, so when `fd` was installed on the local box but not on the remote (or vice-versa), git indexing would silently fall back to an empty file list. The new `projectile-fd-executable-for` returns the per-directory executable, with a per-host cache so the remote `executable-find` lookup runs at most once per host per session.
+* [#1898](https://github.com/bbatsov/projectile/issues/1898), [#1932](https://github.com/bbatsov/projectile/issues/1932): Surface non-zero exit codes from the indexing command in `projectile-files-via-ext-command` instead of silently returning an empty file list. The most common cause is `fd`/`git` being installed locally but not on the remote host of a TRAMP project — previously this manifested as an unexplained empty completion list (or `stringp, nil` crashes downstream); it now signals a `user-error` pointing at the `*projectile-files-errors*` buffer for the captured stderr.
+* [#1211](https://github.com/bbatsov/projectile/issues/1211): Fix file-local `projectile-project-root` overrides being ignored after the first buffer in a directory was visited. The cache used `(default-directory)` as part of the key but `projectile-root-local` reads a buffer-local variable, so two buffers in the same directory with different overrides got the first buffer's answer. Results from `projectile-root-local` are no longer cached.
+* [#1836](https://github.com/bbatsov/projectile/issues/1836): Memoize per-function `nil` results in the project root cache. Previously, when an early entry in `projectile-project-root-functions` returned nil, the nil was indistinguishable from a missing cache entry, so the function was re-run on every `projectile-project-root` call. With many entries this could cost several full directory walks per call. A `'none` sentinel is now stored for unsuccessful entries.
+* [#1508](https://github.com/bbatsov/projectile/issues/1508): Fix dirconfig parser silently treating lines as ignore patterns when the `+`/`-`/`!` prefix or the comment character is preceded by whitespace; leading spaces and tabs are now skipped before prefix dispatch.
 * Fix `projectile-files-via-ext-command` executing empty string as shell command for non-git VCS sub-projects.
 * Fix `projectile-select-files` crashing on filenames with regexp metacharacters by using `string-search` instead of `string-match`.
 * Fix `projectile-find-references` using internal `xref--show-xrefs` API whose signature changed across Emacs versions.
@@ -20,7 +217,11 @@
 * Fix `projectile--other-extension-files` sort comparator ignoring its second argument, producing undefined ordering; replaced with a stable partition.
 * Fix `projectile-toggle-project-read-only` operating on the wrong buffer after `add-dir-local-variable` by wrapping in `save-selected-window`.
 * Fix `projectile-cache-current-file` calling `projectile-project-root` twice instead of reusing the already-resolved value.
+* Fix `projectile-cache-current-file` queueing one idle timer per opened file, each capturing a stale snapshot of the file list. With persistent caching, opening many files in a session would result in N redundant disk writes after Emacs went idle. A pending flush is now coalesced per project and reads the latest in-memory cache at fire time.
+* Fix `projectile-load-project-cache` not recording a cache time, which combined with `projectile-files-cache-expire` made the TTL check immediately re-evict freshly loaded data — every call ended up re-reading the cache file from disk and the data was never reindexed. The cache file's mtime is now used to seed `projectile-projects-cache-time`.
 * Fix `projectile-load-project-cache` storing nil in cache on corrupt/empty cache files, preventing future reload attempts.
+* Fix `projectile-purge-dir-from-cache` only updating the in-memory cache; with persistent caching the purged directory's files would reappear on the next session. The on-disk cache is now updated as well, matching the behavior of `projectile-purge-file-from-cache`.
+* `projectile-invalidate-cache` now deletes the persistent cache file instead of overwriting it with a serialized `nil`.
 * Fix `projectile--cmake-command-presets` using `mapcar` instead of `mapcan`, producing nested lists for included presets.
 * Fix `projectile--eat` ignoring the `new-process` argument when generating buffer names.
 * Fix `projectile-check-vcs-status` hanging indefinitely by adding a 30-second timeout to its busy-wait loop.
@@ -62,28 +263,6 @@
 * Fix `projectile-find-dir-hook` not being cleaned up when disabling `projectile-mode`.
 * Use `display-warning` instead of `message` when cache serialization fails, making the failure visible in the `*Warnings*` buffer.

-### Changes
-
-* Pre-compute file timestamps in `projectile-sort-by-modification-time` and `projectile-sort-by-access-time` to reduce stat calls from O(n log n) to O(n).
-* Cache `projectile-parse-dirconfig-file` results per project root (invalidated by file modification time), avoiding redundant file reads during indexing.
-* Cache `file-truename` results in `projectile-project-buffer-p` when checking multiple buffers, reducing redundant symlink resolution.
-* Use a hash set for deleted file removal in `projectile-dir-files-alien`, improving performance from O(n*m) to O(n+m) when filtering deleted-but-unstaged files.
-* Avoid redundant `projectile-project-root` call in `projectile-detect-project-type` by passing through the already-resolved root.
-* Share `file-truename` cache across buffers in `projectile-open-projects`, matching the optimization already in `projectile-project-buffers`.
-* Remove unnecessary temp buffer creation in `projectile--cache-project-commands-p` (was creating a buffer and re-reading `.dir-locals.el` on every compile/test/run invocation).
-* **[Breaking]** Bump minimum required Emacs version from 26.1 to 27.1. This removes ~30 lines of compatibility code (fileloop fallback, `time-convert` fallback, `projectile-flatten` shim) and fixes the `tags-query-replace` FIXME in `projectile-replace-regexp`.
-* Add `compat` as a dependency, enabling the use of modern Emacs APIs (e.g. `string-replace`) on older Emacs versions.
-* Replace most `cl-lib` sequence functions with `seq.el` equivalents (`seq-filter`, `seq-remove`, `seq-some`, `seq-find`, `seq-sort`, `seq-every-p`, `seq-difference`) and convert `cl-case` to `pcase`.
-* [#1971](https://github.com/bbatsov/projectile/pull/1971): Support `slnx` files for dotnet project types.
-* [#1958](https://github.com/bbatsov/projectile/issues/1958): Exclude `.projectile-cache.eld` from search results (ripgrep/ag/grep) by default.
-* [#1957](https://github.com/bbatsov/projectile/pull/1957): Add `:caller` information to calls to `ivy-read` (used by packages like `ivy-rich`).
-* [#1947](https://github.com/bbatsov/projectile/issues/1947): `projectile-project-name` should be marked as safe.
-* Set `projectile-auto-discover` to `nil` by default (to avoid startup slowdowns in some situations).
-* [#1943](https://github.com/bbatsov/projectile/pull/1943): Consider `projectile-indexing-method` to be safe as a dir-local variable if it is one of the preset values.
-* [#1936](https://github.com/bbatsov/projectile/issues/1936): Do not require selecting a project when using `M-x projectile-invalidate-cache`, since there is a global cache that is also cleared by that command, even when not operating on any specific project.
-* Add `build.mill` as an alternative project marker for the Mill project type, matching Mill's current recommended file extension.
-* Replace obsolete `when-let` and `cl-gensym` with `when-let*` and `gensym` for compatibility with Emacs 31+.
-
 ## 2.9.1 (2025-02-13)

 ### Bugs Fixed
#+END_SRC

* rg.el (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/rg.el
:BRANCH: master
:END:
** Remote-only commits
- e46a16b Shell quote arguments to command line flags (#188)
- d22008f Allow optional quote in font lock regexp (#195)
- 82bec8c Ignore docs build output

* s.el (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/s.el
:BRANCH: master
:END:
** Remote-only commits
- 7393fa6 Fix #158 (create-docs.sh failing) (#168)

* shell-maker (main)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/shell-maker
:BRANCH: main
:END:
** Remote-only commits
- 5560742 Merge pull request #39 from alberti42/fix-source-block-copy-button-stale-markers
- cdf0573 Fix source-block copy button copying a stale, truncated range
- 2c9d4a4 Fixing when-let if-let Emacs 31 deprecation warnings
- 4e125a8 Bumping version
- 29190df Fixes auto-scroll regression (based on #36 by @angrybacon)
- 83f3712 Bumping version
- 55c383c Merge pull request #37 from dpom/fix/multi-line-input-truncation
- 2035cd7 Fix multi-line input truncation when submitting to comint
- 43ee9e1 Bumping version
- 1011eb4 Adds shell-maker-insert-end-of-prompt-marker
- 3580e32 Adds shell-maker-insert-end-of-prompt-marker
- adce7ba Bumping version
- 830e15c Bumping version
- ca94744 Enable getting history untrimmed
- 904f8dc Replacing when-let* with and-let*
- 0100df0 Merge branch 'main' of github.com:xenodium/shell-maker
- 80c8f42 Fixes M-p followed by M-n losing all but first input word
- e8bdf6d Merge pull request #34 from ewilderj/exp-tty-grapheme-width
- b1cf52c Fix TTY table emoji width for ZWJ and skin-tone sequences
- 11f4a99 Bumping version
- e845a7b Merge branch 'main' of github.com:xenodium/shell-maker
- a0cbfed Bump version
- 5412355 Tag markup https://github.com/xenodium/agent-shell/issues/548
- 5ac29f9 Merge pull request #33 from Scott-Guest/table-snag
- eba2900 Use display="" instead of invisible for overlays
- 3c97df6 Merge pull request #31 from ewilderj/fix-table-emoji-alignment
- e11a8dc Fix table emoji alignment via destination-buffer measurement
- 8fb4a30 Merge pull request #30 from smagnuso/pr/pause_scroll
- 31f8440 Merge pull request #29 from ccqpein/main
- 045a830 Make a utility function and check that instead of eobp. This helps if you scroll backwards but you still have your point when you scroll up so scrolling while data is flowing in
- 8a69c5e fix the shell-maker-restore-session-from-transcript error
- 6377cbd Merge pull request #28 from timfel/main
- e3b9736 Accept Windows paths with drive letters
- 5e6e304 Accept file links without lines
- 808bede Merge pull request #27 from ElleNajt/fix/image-cache-flush
- 661e8e0 Flush image cache to ensure fresh renders
- 55f829d Merge branch 'main' of github.com:xenodium/shell-maker
- 38951e8 Bumping version
- d53d7e1 Improve help command formatting for wrapping https://github.com/xenodium/agent-shell/issues/409
- e109a10 Merge pull request #23 from xenodium/table-optimization
- 09b6703 Merge branch 'main' of github.com:xenodium/shell-maker
- ea186d0 Bumping version
- 33eb46d Ensure no output is editable https://github.com/xenodium/agent-shell/issues/426
- 7918110 Merge pull request #24 from emil-e/main
- b18393f Apply face-run overlays instead of per-character overlays for code blocks
- 6adbe3d Consolidating further table optimizations

* show-font (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/show-font
:BRANCH: master
:END:
** Changelog diff (=CHANGELOG.org=)
#+BEGIN_SRC diff
diff --git a/CHANGELOG.org b/CHANGELOG.org
index d7d3748..ae726f0 100644
--- a/CHANGELOG.org
+++ b/CHANGELOG.org
@@ -1,5 +1,5 @@
 #+title: Change log of show-font
-#+author: Protesilaos Stavrou
+#+author: Protesilaos
 #+email: info@protesilaos.com
 #+language: en
 #+options: ':t toc:nil author:t email:t num:t
@@ -13,6 +13,131 @@ the manual: <https://protesilaos.com/emacs/show-font>.

 #+toc: headlines 1 insert TOC here, with one headline level

+* Version 1.0.0 on 2025-09-07
+:PROPERTIES:
+:CUSTOM_ID: h:ca591fac-9b02-4dd3-980e-23be5df17ef7
+:END:
+
+This major update introduces support for fonts that cover more than
+the Latin script. It also expands the available functionality with
+quality-of-life refinements.
+
+** Support for Arabic, Chinese, Greek, Japanese, Korean, Russian
+:PROPERTIES:
+:CUSTOM_ID: h:f0cb2b7f-d81a-4b47-bdde-d029afcee70a
+:END:
+
+The commands ~show-font-tabulated~ (alias ~show-font-list~) and
+~show-font-select-preview~ can now generate a preview for fonts that
+are optimised to display the aforementioned languages. Each language
+provides its own user option to control the sample text it displays.
+The naming pattern ~show-font-LANGUAGE-sample~.
+
+Of those, I only know Greek and thus wrote the value of
+~show-font-greek-sample~, namely: ="Πρωτεσίλαος ο φιλόσοφος του οποίου
+τα έργα βρίθουν αστειισμών"=. For the others I used translation
+software to get the equivalent of ="Protesilaos does not read
+LANGUAGE"=. Please let me know if there are any mistakes in this
+regard. I was thinking of writing something a bit more funny, but was
+concerned the joke may not translate well.
+
+** Checking for language support among known families
+
+While I have written functions that test if a given font can display a
+range of characters, this approach is computationally intensive if we
+need to check for many code points across multiple fonts.
+
+The alternative is to maintain lists of known font families that are
+meant to work with the given language. Those generally support Latin
+as well, but the idea is to let them shine in the language they are
+meant to be used for.
+
+For example, here is how we know that a font family is meant to
+display Arabic script:
+
+#+begin_src emacs-lisp
+(defconst show-font-arabic-families
+  '("AlArabiya" "AlBattar" "AlHor" "AlManzomah" "AlYarmook"
+    "Dimnah" "Hani" "Haramain" "Hor" "Kayrawan" "Khalid" "Mashq"
+    "Nagham" "Noto Kufi Arabic" "Noto Naskh Arabic" "Noto Sans Arabic"
+    "Rehan" "Sharjah" "Sindbad")
+  "List of families that specialise in Arabic.
+Also see `show-font-greek-families' for the rationale of grouping font
+families in distinct variables.")
+#+end_src
+
+The list is not exhaustive and I am always eager to expand it. Just
+let me know.
+
+I learnt about these font families through trial and error by (i)
+installing them on my Debian system and (ii) searching online for
+common samples of them. Do =apt search -n fonts-= to check the
+relevant packages.
+
+** Support for music notation, mathematics, and other symbols
+:PROPERTIES:
+:CUSTOM_ID: h:53718517-d46e-40b4-8368-9b5c36ddfa18
+:END:
+
+As with the natural languages, there are some fonts that specialise in
+displaying symbols. For example, MathJax has a bunch of fonts for
+showing those fancy formulas in the processed output of LaTeX
+documents. Again, there is a ~defconst~ for each of those types of
+font listing the known families. The concomitant user options are:
+
+- ~show-font-mathematics-sample~
+- ~show-font-music-sample~
+- ~show-font-symbols-sample~
+
+** Problematic fonts are hidden from the list view
+:PROPERTIES:
+:CUSTOM_ID: h:ebab3870-25d1-453b-a2d9-c3600dd8d21e
+:END:
+
+The ~show-font-hidden-families~ lists the fonts that are not known to
+cause problems. They do not render properly any of the supported
+samples and I am not sure even when they claim to support a certain
+set of characters (e.g. ~show-font--displays-latin-p~ returns
+non-~nil~). If you think there is a mistake here, please contact me.
+
+** Show a full preview from the list view
+:PROPERTIES:
+:CUSTOM_ID: h:731fb370-ad5c-48cb-a8b4-0ca3a818c2ba
+:END:
+
+While in the buffer produced by ~show-font-tabulated~ (alias
+~show-font-list~), type =RET= to get a complete preview of the font
+family of the current line. This is the same as invoking the command
+~show-font-select-preview~ and then selecting the given family.
+
+The command called by that key binding is ~show-font-tabulated-select-preview~.
+A hint of it is also shown in the tabulated list header.
+
+** Quickly copy the name of the font in the list view
+:PROPERTIES:
+:CUSTOM_ID: h:cbcc97bc-aedb-45e1-b8ea-fccec600db58
+:END:
+
+As above, type =w= in the tabulated view to copy the name of the font
+family to the ~kill-ring~. The command is ~show-font-tabulated-copy-name~
+and there is also a hint of it in the tabulated list header.
+
+* Version 0.4.0 on 2025-08-26
+:PROPERTIES:
+:CUSTOM_ID: h:d62cc2cd-16ec-4b6f-9d2d-37061c7fe177
+:END:
+
+This version expands the capabilities of the font preview mechanism to
+handle emoji and icon fonts (i.e. those with codepoints in the Unicode
+Private Use Area).
+
+Both the ~show-font-select-preview~ and ~show-font-tabulated~ commands
+now handle these types of font.
+
+Previews for emoji or icon fonts are done using the string of
+characters set to the variable ~show-font-emoji-sample~ or
+~show-font-icon-sample~. These symbols are user options.
+
 * Version 0.3.0 on 2025-04-26
 :PROPERTIES:
 :CUSTOM_ID: h:e57d0359-f7dc-4abd-802e-27b854d633bf
#+END_SRC

* spacious-padding (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/spacious-padding
:BRANCH: master
:END:
** Changelog diff (=CHANGELOG.org=)
#+BEGIN_SRC diff
diff --git a/CHANGELOG.org b/CHANGELOG.org
index e00f7e6..ce93d0a 100644
--- a/CHANGELOG.org
+++ b/CHANGELOG.org
@@ -1,5 +1,5 @@
 #+title: Change log of Spacious-Padding
-#+author: Protesilaos Stavrou
+#+author: Protesilaos
 #+email: info@protesilaos.com
 #+language: en
 #+options: ':t toc:nil author:t email:t num:t
@@ -13,6 +13,108 @@ the manual: <https://protesilaos.com/emacs/spacious-padding>.

 #+toc: headlines 1 insert TOC here, with one headline level

+* Version 0.8.0 on 2025-12-13
+:PROPERTIES:
+:CUSTOM_ID: h:4a3250fa-b905-4bc4-bd22-7cb5690aef1b
+:END:
+
+This release introduces some nice refinements and fixes a couple of
+subtle bugs.
+
+** Subtle mode and header line
+:PROPERTIES:
+:CUSTOM_ID: h:76f3c771-e0a4-46c4-9002-c88e73aa8b84
+:END:
+
+The new user option ~spacious-padding-subtle-frame-lines~ supersedes
+the ~~spacious-padding-subtle-mode-line~. It does the same thing,
+namely, of making the mode lines use only a thin line instead of a
+background. Though it extends this feature to header lines as well.
+
+The documentation string of ~spacious-padding-subtle-frame-lines~
+describes the technicalities and includes examples. In short, we can
+associate a keyword with either a face that has a foreground color or
+a color value directly. For the convenience of the user, the package
+also defines the faces ~spacious-padding-line-active~ and
+~spacious-padding-line-inactive~. Here is a sample configuration:
+
+#+begin_src emacs-lisp
+;; Read the doc string of `spacious-padding-subtle-mode-line' as it
+;; is very flexible.  Here we make the mode lines be a single
+;; overline, while header lines have an underline.
+(setq spacious-padding-subtle-frame-lines
+      '( :mode-line-active spacious-padding-line-active
+         :mode-line-inactive spacious-padding-line-inactive
+         :header-line-active spacious-padding-line-active
+         :header-line-inactive spacious-padding-line-inactive))
+#+end_src
+
+In the future, we might decide that other elements can benefit from
+this style.
+
+** The header line underline is spaced further away from the text
+:PROPERTIES:
+:CUSTOM_ID: h:fe3649fe-6c47-4d14-ae51-6583971fdb4c
+:END:
+
+As noted above, when ~spacious-padding-subtle-frame-lines~ is
+configured to cover header lines, those will be drawn with an
+underline. This will not intersect with the text of the header line.
+
+Normally, underlines cut through letters that descend below the
+baseline, such as the letters =g= and =y=. We choose to avoid that
+because it makes for a cleaner interface (though I personally think it
+is not a good style for paragraph text, because of the rivers of
+negative space it introduces).
+
+This specific design is available for Emacs version 29 or higher.
+Users of Emacs 28 must set ~x-underline-at-descent-line~ to a
+non-~nil~ value. Though note that this has a global effect: we cannot
+limit it to a single face.
+
+Thanks to Steven Allen for covering this in pull request 37:
+<https://github.com/protesilaos/spacious-padding/pull/37>. Steven has
+assigned copyright to the Free Software Foundation.
+
+** The ~spacious-padding-widths~ can affect Custom buttons
+:PROPERTIES:
+:CUSTOM_ID: h:240e1874-fc68-4e3d-890c-9fa27912edf9
+:END:
+
+This is about the buttons we find in buffers of the Custom system. For
+example, we get such a buffer after we do =M-x customize=.
+
+The relevant keyword is =:custom-button-width=. It is set to a number
+of spaces for padding. Here is the default value and, as always, the
+documentation cover the details:
+
+#+begin_src emacs-lisp
+(setq spacious-padding-widths
+      '( :internal-border-width 15
+         :header-line-width 4
+         :mode-line-width 6
+         :custom-button-width 3 ; the new one
+         :tab-width 4
+         :right-divider-width 30
+         :scroll-bar-width 8
+         :fringe-width 8))
+#+end_src
+
+** Fixes for the Emacs daemon
+:PROPERTIES:
+:CUSTOM_ID: h:902951ad-dd49-4716-ad67-dc7526f20ade
+:END:
+
+There were reports about incorrect face specifications that could
+happen when Emacs was started as a daemon process. I made the relevant
+changes. Also thanks to kaibagley for covering a line I had missed.
+This was done in pull request 43: <https://github.com/protesilaos/spacious-padding/pull/43>.
+
+Related threads:
+
+- Issue 35, as reported by Chinmay Dalal: <https://github.com/protesilaos/spacious-padding/issues/35>.
+- Issue 41, as reported by zealotrush: <https://github.com/protesilaos/spacious-padding/issues/41>.
+
 * Version 0.7.0 on 2025-06-01
 :PROPERTIES:
 :CUSTOM_ID: h:adc3093a-e4ae-4ce9-842b-16667a910cdd
#+END_SRC

* straight.el (main)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/straight.el
:BRANCH: main
:END:
** Remote-only commits
- e40a5b7 More correct package-enable-at-startup check (#1286)
- 05eac78 Propose fix some typos (#1285)
- 09b789a allow elpa/ directory to exist if there are no actual packages in it (#1279)
- 53f013e Fix a symlink bug on Windows where mklink would fail for a name ending in `=` (#1276)

* tablist (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/tablist
:BRANCH: master
:END:
** Remote-only commits
- 01f065e Release version 1.3
- d5000bc refactor: replace obsolete defadvice with define-advice
- 32c4c9e refactor: replace obsolete point-at-bol/point-at-eol with line-*-position
- 84f8126 fix: keep tablist mode-line entry idempotent on re-enable
- 30dfcbe Preserve existing 'mode-line-misc-info
- 4e9d943 Release version 1.2

* transient (main)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/transient
:BRANCH: main
:END:
** Changelog diff (=CHANGELOG=)
#+BEGIN_SRC diff
diff --git a/CHANGELOG b/CHANGELOG
index 21ade51..43a48d0 100644
--- a/CHANGELOG
+++ b/CHANGELOG
@@ -1,31 +1,111 @@
 # -*- mode: org -*-
-* v0.13.0    UNRELEASED
+* v0.13.5   2026-07-01

-This release adds features useful for blind users.  The relevant
-options and recommended settings are documented in the manual in
-the section "Accessibility Options".  To go there, evaluate this
-form: (info "(transient)Accessibility Options").
+- Started using LLama.

-- Selecting the menu window using the mouse is now supported.
-  7c46a093
+* v0.13.4   2026-06-01

-- Selecting the menu window using the command ~other-window~, is now
-  supported, but requires the following configuration.  e3346f7b
+- Thoughts and whitespace.

-    (define-key transient-predicate-map
-      [other-window] #'transient--do-move)
+* v0.13.3    2026-05-07

-- The region is now properly displayed, when using the mouse to
-  select text in the menu buffer.  7c46a093
+- Stopped needlessly using ~this-single-command-raw-keys~.  #442
+
+- Fixed a bug that prevented setting suffix levels for certain
+  sub-prefixes.  #443.
+
+- Started using new functions added in Emacs 31.
+
+* v0.13.2    2026-05-01
+
+- Fixed an incompatibility with development version of Compat.
+
+* v0.13.1    2026-05-01
+
+- Improved making a kludge subject only to affected Emacsen.
+
+* v0.13.0    2026-04-22
+
+This release adds features useful for visually impaired users.
+The relevant options and recommended settings are documented in
+the manual in the section "Accessibility Options".  To go there,
+evaluate this form: (info "(transient)Accessibility Options").
+Some of the new options are also potentially useful for other
+users.

 - The menu window can now be selected automatically when activating
   a menu, by setting the new option ~transient-select-menu-window~.
   Doing so is only encouraged for users of braille output devices.
-  59380806
+  dd383016

 - Added new option ~transient-navigate-to-group-descriptions~.
+  4087cf7b
+
+- Added new option ~transient-describe-menu~.  c9f8f398
+
+- Improve suffix navigation.  Support navigating to all cells and
+  fix restoring position after refreshing the menu for all cell
+  types.  5d4a7e71, 0bbd75bd, bf92c158, 250a3c34
+
+- Added new value ~verbose-force~ for ~transient-enable-menu-navigation~,
+  which causes a message to be echoed, even if it is identical to the
+  text displayed in the menu.  4d283706
+
+- Added new option ~transient-prefer-reading-value~.  4dfc3e78
+
+- Added new option ~transient-use-accessible-formats~.  63fca1f6
+
+- Added new option ~transient-use-accessible-values~.  1fc27186
+
+Also included in this release are the following changes, which do
+not (primarily) concern accessibility.
+
+- ~top-level~ did not exit transient's help and edit modes.  b484f759
+
+- Selecting the menu window using the command ~other-window~, is now
+  supported, but requires the following configuration.  5cd017e5
+
+    (define-key transient-predicate-map
+      [other-window] #'transient--do-move)
+
+- Selecting the menu window using the mouse is now supported.
+  c5e6e207
+
+- The region is now properly displayed, when using the mouse to
+  select text in the menu buffer.  c5e6e207
+
+- Added new ~transient-object~ base class, which all other classes
+  ultimately derive from.  8b057d82
+
+- Fixed initial input when reading the value for a suffix whose
+  ~always-read~ slot is non-nil.  cde0756f
+
+- Fixed exiting from ~recursive-edit~ to a transient menu.  #425
+
+- Added new function ~transient-read-string-from-buffer~.  0e65f5da
+
+- Addressed an incompatibility with third-party help extensions.
+  #431
+
+- Fixed how the list of suffixes is build, an issue which could
+  cause ~transient-suffix-object~ to return ~nil~ instead of the object.
+  https://github.com/magit/magit/issues/5528
+
+- When the value of a suffix contained newline characters, then all
+  but the first line was discarded.  #433
+
+- If an error occurred in a command implemented in C or in an
+  anonymous commands, that could cause the transient menu to enter
+  an inconsistent state, because we failed to adequately advise
+  such commands, to protect against that possibility.  63f90723 ff
+
+- If an error occurred because of a window misconfiguration by some
+  third-party code, that could cause the transient menu to enter an
+  inconsistent state, because we did not protect all the places where
+  that could result in an error.  8b142031, #429

-- Added new option ~transient-describe-menu~.
+- Restored compatibility with the ~emoji.el~ that ships with Emacs
+  29.4.  #417

 * v0.12.0    2026-01-01
#+END_SRC

* tree-sitter-langs (release)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/tree-sitter-langs
:BRANCH: release
:END:
** Changelog diff (=CHANGELOG.md=)
#+BEGIN_SRC diff
diff --git a/CHANGELOG.md b/CHANGELOG.md
index 917c3d0..8723cd5 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -2,6 +2,497 @@

 ## Unreleased

+## 0.13.69 - 2026-07-06
+
+## 0.13.68 - 2026-07-06
+
+## 0.13.67 - 2026-07-02
+
+## 0.13.66 - 2026-07-02
+
+## 0.13.65 - 2026-07-01
+
+## 0.13.64 - 2026-06-27
+
+## 0.13.63 - 2026-06-23
+
+## 0.13.62 - 2026-06-15
+
+## 0.13.61 - 2026-06-09
+
+## 0.13.60 - 2026-06-07
+
+## 0.13.59 - 2026-06-01
+
+## 0.13.58 - 2026-05-31
+
+## 0.13.57 - 2026-05-26
+
+## 0.13.56 - 2026-05-26
+
+## 0.13.55 - 2026-05-25
+
+## 0.13.54 - 2026-05-24
+
+## 0.13.53 - 2026-05-17
+
+## 0.13.52 - 2026-05-17
+
+## 0.13.51 - 2026-05-10
+
+## 0.13.50 - 2026-05-10
+
+## 0.13.49 - 2026-05-04
+
+## 0.13.48 - 2026-05-03
+
+## 0.13.47 - 2026-04-27
+
+## 0.13.46 - 2026-04-26
+
+## 0.13.45 - 2026-04-20
+
+## 0.13.44 - 2026-04-19
+
+## 0.13.43 - 2026-04-13
+
+## 0.13.42 - 2026-04-12
+
+## 0.13.41 - 2026-04-05
+
+## 0.13.40 - 2026-04-05
+
+## 0.13.39 - 2026-03-30
+
+## 0.13.38 - 2026-03-29
+
+## 0.13.37 - 2026-03-24
+
+## 0.13.36 - 2026-03-24
+
+## 0.13.35 - 2026-03-23
+
+## 0.13.34 - 2026-03-16
+
+## 0.13.33 - 2026-03-15
+
+## 0.13.32 - 2026-03-09
+
+## 0.13.31 - 2026-03-08
+
+## 0.13.30 - 2026-03-02
+
+## 0.13.29 - 2026-03-02
+
+## 0.13.28 - 2026-03-01
+
+## 0.13.27 - 2026-02-22
+
+## 0.13.26 - 2026-02-22
+
+## 0.13.25 - 2026-02-16
+
+## 0.13.24 - 2026-02-15
+
+## 0.13.23 - 2026-02-09
+
+## 0.13.22 - 2026-02-09
+
+## 0.13.21 - 2026-02-01
+
+## 0.13.20 - 2026-01-26
+
+## 0.13.19 - 2026-01-26
+
+## 0.13.18 - 2026-01-25
+
+## 0.13.17 - 2026-01-19
+
+## 0.13.16 - 2026-01-19
+
+## 0.13.15 - 2026-01-17
+- Fix `Crystal` highlighting for keywords
+
+## 0.13.14 - 2026-01-17
+- Add `Crystal` support
+
+## 0.13.13 - 2026-01-15
+
+## 0.13.12 - 2026-01-15
+- perf: Use hash table instead of alist
+
+## 0.13.11 - 2026-01-13
+- Add `GDShader` support
+
+## 0.13.10 - 2026-01-12
+
+## 0.13.9 - 2026-01-12
+- Add `WGSL` support
+
+## 0.13.8 - 2026-01-12
+- Add `WAT` support
+
+## 0.13.7 - 2026-01-11
+
+## 0.13.6 - 2026-01-11
+
+## 0.13.5 - 2026-01-04
+
+## 0.13.4 - 2026-01-03
+- Add `Git-Config` support
+
+## 0.13.3 - 2025-12-30
+
+## 0.13.2 - 2025-12-29
+
+## 0.13.1 - 2025-12-29
+
+## 0.12.359 - 2025-12-29
+
+## 0.12.358 - 2025-12-29
+
+## 0.12.357 - 2025-12-29
+
+## 0.12.356 - 2025-12-25
+- Add `Apertium` support
+
+## 0.12.355 - 2025-12-25
+
+## 0.12.354 - 2025-12-25
+
+## 0.12.353 - 2025-12-25
+
+## 0.12.352 - 2025-12-25
+
+## 0.12.351 - 2025-12-24
+
+## 0.12.350 - 2025-12-24
+
+## 0.12.349 - 2025-12-24
+
+## 0.12.348 - 2025-12-24
+- Add `diff` support
+
+## 0.12.347 - 2025-12-24
+- Add `Nim` support
+- Add `Vim` support
+
+## 0.12.346 - 2025-12-24
+
+## 0.12.345 - 2025-12-24
+
+## 0.12.344 - 2025-12-24
+
+## 0.12.343 - 2025-12-23
+
+## 0.12.342 - 2025-12-23
+
+## 0.12.341 - 2025-12-23
+
+## 0.12.340 - 2025-12-23
+
+## 0.12.339 - 2025-12-23
+
+## 0.12.338 - 2025-12-23
+
+## 0.12.337 - 2025-12-23
+
+## 0.12.336 - 2025-12-23
+
+## 0.12.335 - 2025-12-23
+
+## 0.12.334 - 2025-12-23
+
+## 0.12.333 - 2025-12-23
+
+## 0.12.332 - 2025-12-23
+
+## 0.12.331 - 2025-12-21
+
+## 0.12.330 - 2025-12-19
+
+## 0.12.329 - 2025-12-16
+- Add `RON` support
+
+## 0.12.328 - 2025-12-15
+
+## 0.12.327 - 2025-12-15
+
+## 0.12.326 - 2025-12-15
+
+## 0.12.325 - 2025-12-14
+
+## 0.12.324 - 2025-12-11
+
+## 0.12.323 - 2025-12-10
+
+## 0.12.322 - 2025-12-07
+
+## 0.12.321 - 2025-11-30
+
+## 0.12.320 - 2025-11-30
+
+## 0.12.319 - 2025-11-23
+
+## 0.12.318 - 2025-11-17
+
+## 0.12.317 - 2025-11-17
+
+## 0.12.316 - 2025-11-10
+
+## 0.12.315 - 2025-11-09
+
+## 0.12.314 - 2025-11-05
+
+## 0.12.313 - 2025-11-02
+
+## 0.12.312 - 2025-10-19
+
+## 0.12.311 - 2025-10-12
+
+## 0.12.310 - 2025-10-05
+
+## 0.12.309 - 2025-10-05
+
+## 0.12.308 - 2025-09-29
+
+## 0.12.307 - 2025-09-22
+
+## 0.12.306 - 2025-09-21
+
+## 0.12.305 - 2025-09-15
+
+## 0.12.304 - 2025-09-08
+
+## 0.12.303 - 2025-08-31
+
+## 0.12.302 - 2025-08-31
+- Add `GraphQL` support
+
+## 0.12.301 - 2025-08-24
+
+## 0.12.300 - 2025-08-17
+
+## 0.12.299 - 2025-08-10
+
+## 0.12.298 - 2025-08-09
+
+## 0.12.297 - 2025-08-08
+- Add slint tree sitter repo. This does not have an associated mode.
+
+## 0.12.296 - 2025-08-03
+
+## 0.12.295 - 2025-07-27
+
+## 0.12.294 - 2025-07-20
+
+## 0.12.293 - 2025-07-19
+
+## 0.12.292 - 2025-07-16
+
+## 0.12.291 - 2025-07-13
+
+## 0.12.290 - 2025-07-06
+
+## 0.12.289 - 2025-06-30
+
+## 0.12.288 - 2025-06-29
+
+## 0.12.287 - 2025-06-22
+
+## 0.12.286 - 2025-06-15
+
+## 0.12.285 - 2025-06-15
+
+## 0.12.284 - 2025-06-08
+
+## 0.12.283 - 2025-06-08
+
+## 0.12.282 - 2025-06-07
+
+## 0.12.281 - 2025-06-01
+
+## 0.12.280 - 2025-05-26
+
+## 0.12.279 - 2025-05-18
+
+## 0.12.278 - 2025-05-18
+
+## 0.12.277 - 2025-05-11
+
+## 0.12.276 - 2025-05-04
+
+## 0.12.275 - 2025-05-04
+
+## 0.12.274 - 2025-05-04
+
+## 0.12.273 - 2025-04-28
+
+## 0.12.272 - 2025-04-27
+
+## 0.12.271 - 2025-04-20
+
+## 0.12.270 - 2025-04-16
+
+## 0.12.269 - 2025-04-14
+
+## 0.12.268 - 2025-04-06
+
+## 0.12.267 - 2025-03-30
+
+## 0.12.266 - 2025-03-23
+
+## 0.12.265 - 2025-03-16
+
+## 0.12.264 - 2025-03-09
+
+## 0.12.263 - 2025-03-02
+
+## 0.12.262 - 2025-02-24
+
+## 0.12.261 - 2025-02-24
+
+## 0.12.260 - 2025-02-24
+
+## 0.12.259 - 2025-02-24
+
+## 0.12.258 - 2025-02-24
+
+## 0.12.257 - 2025-02-17
+
+## 0.12.256 - 2025-02-10
+
+## 0.12.255 - 2025-02-06
+
+## 0.12.254 - 2025-02-05
+
+## 0.12.253 - 2025-02-05
+
+## 0.12.252 - 2025-02-05
+
+## 0.12.251 - 2025-02-05
+
+## 0.12.250 - 2025-02-03
+
+## 0.12.249 - 2025-01-27
+
+## 0.12.248 - 2025-01-20
+
+## 0.12.247 - 2025-01-12
+
+## 0.12.246 - 2025-01-05
+
+## 0.12.245 - 2024-12-29
+
+## 0.12.244 - 2024-12-29
+
+## 0.12.243 - 2024-12-22
+
+## 0.12.242 - 2024-12-15
+
+## 0.12.241 - 2024-12-11
+
+## 0.12.240 - 2024-12-02
+
+## 0.12.239 - 2024-12-02
+
+## 0.12.238 - 2024-12-01
+
+## 0.12.237 - 2024-11-25
+
+## 0.12.236 - 2024-11-24
+
+## 0.12.235 - 2024-11-20
+- Add `EditorConfig` support
+
+## 0.12.234 - 2024-11-18
+
+## 0.12.233 - 2024-11-17
+
+## 0.12.232 - 2024-11-10
+
+## 0.12.231 - 2024-11-03
+
+## 0.12.230 - 2024-10-27
+
+## 0.12.229 - 2024-10-21
+
+## 0.12.228 - 2024-10-20
+
+## 0.12.227 - 2024-10-20
+
+## 0.12.226 - 2024-10-06
+
+## 0.12.225 - 2024-10-02
+
+## 0.12.224 - 2024-09-30
+
+## 0.12.223 - 2024-09-23
+
+## 0.12.222 - 2024-09-16
+
+## 0.12.221 - 2024-09-15
+
+## 0.12.220 - 2024-09-15
+
+## 0.12.219 - 2024-09-09
+
+## 0.12.218 - 2024-09-09
+
+## 0.12.217 - 2024-09-08
+
+## 0.12.216 - 2024-09-08
+
+## 0.12.215 - 2024-09-02
+
+## 0.12.214 - 2024-08-25
+
+## 0.12.213 - 2024-08-25
+
+## 0.12.212 - 2024-08-18
+
+## 0.12.211 - 2024-08-11
+
+## 0.12.210 - 2024-08-05
+- Add `GNAT Project` support
+
+## 0.12.209 - 2024-08-04
+
+## 0.12.208 - 2024-07-28
+
+## 0.12.207 - 2024-07-22
+
+## 0.12.206 - 2024-07-22
+
+## 0.12.205 - 2024-07-14
+
+## 0.12.204 - 2024-07-07
+
+## 0.12.203 - 2024-06-30
+
+## 0.12.202 - 2024-06-23
+
+## 0.12.201 - 2024-06-16
+
+## 0.12.200 - 2024-06-09
+
+## 0.12.199 - 2024-06-02
+
+## 0.12.198 - 2024-05-26
+
+## 0.12.197 - 2024-05-26
+
+## 0.12.196 - 2024-05-19
+
+## 0.12.195 - 2024-05-12
+
+## 0.12.194 - 2024-05-12
+
+## 0.12.193 - 2024-05-12
+
+## 0.12.192 - 2024-05-12
+- Update `CSS` highlights.scm
+
 ## 0.12.191 - 2024-05-06
 - Add `Purescript` support
#+END_SRC

* treemacs (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/treemacs
:BRANCH: master
:END:
** Changelog diff (=Changelog.org=)
#+BEGIN_SRC diff
diff --git a/Changelog.org b/Changelog.org
index d3301f2..d94ea7a 100644
--- a/Changelog.org
+++ b/Changelog.org
@@ -3,11 +3,23 @@

 * Changelog
 ** current master
+*** Added ~treemacs-buffer-name-function~
+*** Added ~treemacs-file-follow-ignore-functions~
+*** Added ~treemacs-buffer-name-prefix~
+*** Added ~treemacs-hide-dot-jj-directory~
+** v3.1
 - Deprecated ~treemacs-window-background-color~ in favour of ~treemacs-window-background-face~ and
   ~treemacs-hl-line-face~
 - Added ~treemacs-copy-absolute-path-at-point~
-- Made it possible to disbale workspace with a ~COMMENT~ directive
+- Added ~treemacs-start-on-boot~
+- Made it possible to disable workspace with a ~COMMENT~ directive
 - Added option to sort alphabetic-numerically (as with ~string-version-lessp~)
+- Added ~:on-expand~ and ~:on-collapse~ options to treelib nodes
+- Added options to define visit-actions in extensions api.
+- Added ~treemacs-after-visit-functions~.
+- Added option to disable moving files by dragging with your mouse.
+- Better performance when ~treemacs-collapse-dirs~ is used for many sub-directories.
+- Added ~treemacs-git-executable~.
 ** v3.1
 - Added ~treemacs-create-workspace-from-project~ command
 - Added ~treemacs-project-follow-into-home~ option
#+END_SRC

* treepy.el (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/treepy.el
:BRANCH: master
:END:
** Remote-only commits
- 806c000 Merge pull request #19 from dannywillems/fix/test-warnings
- 450ac61 ci: add GitHub Actions and dependabot; correct minimum Emacs to 27.1
- c69f7ed test: silence free-variable and docstring byte-compile warnings
- 28f0e2c Bump version to 0.1.3

* use-package (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/use-package
:BRANCH: master
:END:
** Remote-only commits
- 4b3484b Update use-package
- 3dda014 Update use-package

* web-mode (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/web-mode
:BRANCH: master
:END:
** Remote-only commits
- aeee2d4 fix native-compiler warning
- e93b3fb update min version required

* with-editor (main)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/with-editor
:BRANCH: main
:END:
** Changelog diff (=CHANGELOG=)
#+BEGIN_SRC diff
diff --git a/CHANGELOG b/CHANGELOG
index ace5a74..f4c6e75 100644
--- a/CHANGELOG
+++ b/CHANGELOG
@@ -1,4 +1,20 @@
 # -*- mode: org -*-
+* v3.5.2    2026-07-01
+
+- Added kludge to work around bug in "server.el", which was not fixed
+  until Emacs 31.1.  #139
+
+* v3.5.1    2026-06-01
+
+- Advice shell-command process filter instead of replacing it.  #138
+
+- Dropped support for Emacs 26 and 27.
+
+* v3.5.0    2026-05-01
+
+- Append newline to emacsclient commands as required by Emacs 31.1.
+  #137
+
 * v3.4.9    2026-03-01

 - Thoughts and whitespace.
#+END_SRC

* xref (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/xref
:BRANCH: master
:END:
** Remote-only commits
- bd31f9a Update xref
- bd9de35 Update xref
- cdbb34c Update xref
- d92b748 Update xref
- 7afae70 Update xref
- 2113694 Update xref
- 1b600d6 Update xref
- f2c6387 Update xref
- 26b277b Update xref
- 63b5875 Update xref
- 412a91f Update xref
- b547cca Update xref
- 512b921 Update xref
- 94f822d Update xref
- ed64fa4 Update xref
- e459adf Update xref
- 03bbbf8 Update xref
- 53c4abb Update xref
- 0e50194 Update xref
- 36020da Update xref
- d1403df Update xref
- b06843c Update xref
- 1c90df8 Update xref
- 994ec08 Update xref
- 0babdfe Update xref
- 77fcb56 Update xref
- 6b896f0 Update xref
- a2bf7b8 Update xref
- 437b34b Update xref
- 0606fa4 Update xref
- 5451a9d Update xref
- 936e7e1 Update xref
- 8f07142 Update xref
- d64563f Update xref
- adeb5cf Update xref

* yaml-mode (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/yaml-mode
:BRANCH: master
:END:
** Remote-only commits
- 96ef020 Merge pull request #121 from yoshiki/update-ci
- 77d1d9c Update testing emacs versions
- 62cbd80 Merge pull request #120 from mniestroj/fix-char-compare-nil
- 92c95f0 Fix wrong-type-argument error at beginning of buffer

* yaml.el (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/yaml.el
:BRANCH: master
:END:
** Remote-only commits
- 5546f36 Version bump
- 95efacd Merge pull request #66 from dannywillems/fix/unused-lexical-vars
- ff56e88 ci: byte-compile across more Emacs versions with warnings as errors
- 9e1d6c7 test: silence unused lexical variable warnings

* yasnippet (master)
:PROPERTIES:
:DIR:    /Users/ekkehard.kraemer/.emacs.d/straight/repos/yasnippet
:BRANCH: master
:END:
** Remote-only commits
- c1e6ff2 Drop use of `yas--version' in doc/yas-doc-helper.el
- dd570a6 yasnippet.el: Bump version to trigger release
- 272b606 Try and fix issue #1214
- 2384fe1 yasnippet.el (yas--version): Delete variable.
- 81a59d1 yasnippet.el: Bug fix release 0.14.2
- 2c20b10 Misc minor pending tweaks
- 5b315f1 Misc fixes found via compilation warnings
- 03b1b11 yasnippet.el (yas--font-lock-keywords): Avoid `font-lock-*-face` vars
- 37d0b27 yasnippet.el (yas-maybe-load-snippet-buffer): Don't move point (#1146)
- fe1f4e0 Merge pull request #1149 from pestctrl/quit-undo-tree
- ce54f19 Push modifications to buffer-undo-list, even if unwinding
- eb5ba26 Merge pull request #1196 from VitoVan/master
- e23a801 (yas--all-parents): delete-dups for fundamental-mode
- 33587a8 (yas-reload-all): Flush the `all-parents` caches (issue #1194)
- 15065c3 Merge pull request #1194 from gutron/master
- ab03a75 Undo all-parents memoization in test
- 5b565c5 NEWS: Give a few more details
- 1ec7e3b (yas--all-parents): Obey `derived-mode-extra-parents` (Issue #1193)
- 8e9ce76 yasnippet.el: Recognize `${N}` as an alternative syntax for `$N`
- 297546f yasnippet.el: Bug fix release 0.14.1
- b8f3f11 yasnippet.el (yas--indent-region): Workaround for issue #1183
- 47b38b2 yasnippet.el (yas-define-snippets): Remove costly no-op
- 5af84cd Merge pull request #1184 from martenlienen/local-condition-closure
- 9228fd9 * yasnippel.el (yas-buffer-local-condition): Check functionp before consp to allow closures
- 1d0966a * yasnippet.el (yas-buffer-local-condition): Accept functions
- 25f5d88 Avoid shadowing the `tab` => TAB remapping
- d7a79d4 Fix remaining failing tests
- 3e14a85 (yas--on-protection-overlay-modification): Fix thinko
- 9f253ba yasnippet.el: Work around Emacs bug#65929 (see also issue#1175)
- 23c7a6f yasnippet.el: Minor simplifications
- c6488ac README.mdown: Minor tweaks
- d124280 yasnippet-tests.el: Fix some failures
- 8ce506e * yasnippet.el (yas--all-parents): Don't add `fundamental-mode` to itself
- d7f55c7 (yas--all-parents): Fix stale cache issue #1180
- ae36504 yasnippet.el: Use `derived-mode-all-parents` when available
- bd2fdc8 * yasnippet.el (yas--snippet-create): Remove CC-mode workaround for #953
- 362e9b5 yasnippet.el (yas-about): Use `package-get-version`
- 66db827 yasnippet.el: Use `setq-local` and `defvar-local`
- bcefd0a * yasnippet.el: Remove compatibility for Emacs<24.4
- 2b328e5 * yasnippet.el (yas-wrap-around-region): Fix warning
- 9596631 * README.mdown: Tweak install instructions
- b86b44c (yas--modes-to-activate): Pay attention to `major-mode-remap-alist`
- 52a1c50 * yasnippet.el (debug-ignored-errors): Don't modify it
- 58c92ba * yasnippet.el: Cosmetic changes
- 043c52a * yasnippet.el (yas-minor-mode-map): Don't bind to `tab`
- 0093221 * yasnippet-debug.el: Use `advice-add` i.s.o `defadvice`.
- e991530 * yasnippet.el: Use `add-function` and remove left-over debug code
- 97002af * yasnippet.el: Use `lexical-binding`
- b09ab98 * yasnippet-tests.el: Minor cosmetic changes
- 6e4de31 * doc/yas-doc-helper.el: Use `cl-lib` and `lexical-binding`
- 946b27d * .gitignore: Add patterns for ELPA-generated files
- 76e1eee Merge commit '0650f1989b6da752f60a15aa6b5222e340388cc2' from elpa.git
- 3393975 Fix compilation problems of doc/yas-doc-helper.el
- 849a134 Unbreak link in org docs
- 50ba4f3 Replace Rakefile with Makefile
- 1ca316f Unbreak unit tests under Emacs 28.
- 0650f19 Fix tests failing because of lisp-data-mode
